# T-237 — calendar buttons on one row, dish amount on the name's line, "under ₹1", workforce figure on one line

**Files changed** (all frontend; none is a table file or `.kms-table` CSS, which T-236 holds):

- `frontend/app/calendar/page.tsx` — the day panel's two buttons in one row, both `sm`, correction in amber.
- `frontend/components/ds/Button.tsx` — new `warning` variant (tinted fill, amber text and border, built like `danger`). Needed for the amber button; written as utilities so `globals.css` (T-236 is in it) was not touched.
- `frontend/components/planner/MealComposer.tsx` — only the dish cell of step 3.
- `frontend/app/cost-per-serving/page.tsx` — `rupees()` says "under ₹1".
- `frontend/components/ds/StatTile.tsx` — figure `text-2xl` → `text-lg`, side padding `px-6` → `px-5`, for all four Today tiles (StatTile is used only on Today; the donations page has its own tiles, already `text-lg`).
- `frontend/app/today/page.tsx` — the workforce words `text-sm` → `text-xs`, and the comment.
- Tests: `frontend/__tests__/cost-per-serving.test.tsx` (new case), `frontend/__tests__/calendar-correction.test.tsx` (new case, pins `grid-cols-2` and `!px-2 py-1 text-center` on both), `frontend/__tests__/today.test.tsx` and `frontend/__tests__/meal-composer.test.tsx` (the existing markup pins, updated to the new classes).

`Button.tsx`, `StatTile.tsx` and the four test files were not named in the brief; each was needed for the change it sits beside.

**Commands run and what came back**

```
tools/work-lock.sh run verify 'cd frontend && npx tsc --noEmit && echo TSC-CLEAN && npx vitest run 2>&1 | grep -E "FAIL|×|Test Files|Tests " ; npx eslint <10 changed files>; echo ESLINT-EXIT $?'

TSC-CLEAN
   × items 10 and 11 — a label lines up with the words in its box > indents every field label to the inset, and sets it in the one label style
   × item 12 — a table row answers the pointer > puts one step of tone on every row of every table body
   × item 13 — the words > writes an apostrophe one way
 FAIL  __tests__/design-system.test.ts > items 10 and 11 ...
 FAIL  __tests__/design-system.test.ts > item 12 ...
 FAIL  __tests__/design-system.test.ts > item 13 ...
 Test Files  1 failed | 146 passed (147)
      Tests  3 failed | 2003 passed (2006)
ESLINT-EXIT 0
```

**The full suite is NOT green.** All three failures name `app/dev-buttons/page.tsx` (lines 399, 307, 260), an untracked throwaway mock page for T-238 ("THROWAWAY MOCK for T-238 — delete before any deploy"). Not mine and not in my files:

```
+   "app/dev-buttons/page.tsx:399 Shift title",
+   "app/dev-buttons/page.tsx:307",
+   "app/dev-buttons/page.tsx:260 entity",
```

Before my test updates, the first run also failed `today.test.tsx` (words class) and `meal-composer.test.tsx` (grid class). Both were pins on the markup I changed, and both pass now.

**Measurements** (local http://localhost:3000, signed in as Temple Admin, Chrome, the page loaded in a same-origin iframe of the stated width; DOM geometry, with screenshots used only to eyeball the result)

1. Calendar day panel, "Correct this date" / "Plan this day's menu":
   - Before: two rows. Correct 125×36 at y=361, Plan 290×44 at y=513 (1470 and 1920). At 1280: 125×36 at y=1127, 172×44 at y=1239. At 390: 125×36 at y=1302, 308×44 at y=1454.
   - First version (135 and 147 wide in the side panel) was sent back: not the same size. Rebuilt as a two-column `1fr 1fr` grid with an 8px gap. Both buttons are `sm` with the side padding cut to 8px (`!px-2`) and `py-1`. A label wraps inside its button only if half the row is too narrow.
   - After, width x height of each button (both on the same y at every width):
     - 1920: row 290, 141.0x36.0 and 141.0x36.0
     - 1470: row 290, 141.0x36.0 and 141.0x36.0
     - 1280: row 286 (fit-content under the month), 139.0x36.0 and 139.0x36.0
     - 1000: row 286, 139.0x36.0 and 139.0x36.0
     - 390: row 308, 150.0x36.0 and 150.0x36.0
     - 360: row 299, 145.6x36.0 and 145.6x36.0 (a 320px frame gave the same row, so the page does not narrow below this)
     - No label overflowed or wrapped at any real width. "Plan this day's menu" needs 139px at 8px padding, and the narrowest half is 141px.
   - Forced check of the wrap case: I set the row to 250px by hand in the 1470 frame. Both buttons came out 121.0x50.0, with "Plan this day's menu" on two lines. Equal widths and equal heights held.
   - Correction button computed style: background rgb(249,238,226), text and 1px border rgb(128,93,48), which are the warning tokens.
2. Meal composer, row heights (dish 0 ticked, dish 4 ticked and set by hand to 37):
   - Before: 1280 ticked rows 100, unticked row 52. 1920 the same. 390: ticked 100/101, unticked 53.
   - After: 1280 60 (dish 0, one-line name) and 72 (dish 4, "Arbi ki Sabzi (Haryana)" wraps to two lines in its 90px beside the box). Unticked 52. 1920: 60, 60, 52. 390: 60, 73, 53.
   - With the red error (60000 entered, form submitted, so the save was refused): 1280 138, 1920 126, 390 139. The error sits under the box (left edge = box's left edge, 0px). It is 160px wide, so this sentence takes 3 lines (66px). The before-change height with the error was not measured.
   - Two layout faults found while measuring, and fixed. (a) The error sentence set the width of the box and unit columns and squeezed the name to 0px. Fixed with `w-0 min-w-full` on the slot. (b) A ticked name sat 4px lower than its neighbours' (677 against 673). Fixed with `self-start` and `content-start`, and now all tops are 673.
3. "under ₹1": unit test only (0.3 and 0.49 show "under ₹1", 0.5 shows "₹1", 0 shows "₹0"). Not checked in the browser.
4. Today's "Working today" tile, one line = both halves have the same top:
   - Before (`text-2xl`, `px-6`), 1280: box 172px. "12 staff · 14 volunteers" is 166px (one line in my frame). "120 staff · 1400 volunteers" is 213px and takes 2 lines.
   - Alternatives measured at 1280: `text-xl` + `text-xs` words = 186px, against a 188px box even with `px-4` (too tight). `text-lg` + `text-xs` = 167px.
   - After (`text-lg`, `text-xs` words, `px-5`): 1280, box 180px: 12/14 is 136px, 120/1400 is 167px, both on one line. 1920, box 230px: both on one line. 390, box 316px: both on one line. All four tiles are 144px tall in a row at 1280.

**Acceptance criteria**

1. Exactly the same size, one row, amber correction, and a label wraps only if half the row can't hold it. Measured above, and the jsdom test pins the markup.
2. Ticked dish's box and unit on the name's line, and only the red error adds height. Measured above. A long name that wraps beside the box also adds 20px.
3. Under ₹0.50 but above 0 shows "under ₹1", and exactly 0 stays "₹0". Unit test.
4. "120 staff · 1400 volunteers" on one line at 1280 and 1920, the same on all four tiles. Measured above.

**Not done / needs Rajeev**

- The full vitest run is red on 3 tests, all caused by the T-238 mock page `app/dev-buttons/page.tsx`, not by this task.
- A long dish name now shares its cell with the box, so at 1280 "Arbi ki Sabzi (Haryana)" wraps to two lines (row 72, against 52 unticked).
- The red error is under the box as ruled. At 160px wide the long sentence takes 3 lines. Spanning the error across the whole cell measured 2 lines (cell 118 instead of 138 at 1280). Not done, because it would no longer sit under the box. His call.
- Every Today tile figure is now `text-lg` (18px) instead of `text-2xl` (24px). That was needed so the four tiles stay alike.
- Hand smoke test: I drove the calendar, the composer (tick, type, refused save) and Today in Chrome, as the Temple Admin only. The other roles were not checked in the browser.
- I measured the calendar as a Temple Admin on a future day. The non-admin case (Plan alone in the row) is covered only by the existing jsdom test. Not verified in the browser.
