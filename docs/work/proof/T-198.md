# T-198 — the frontend planner, recording, job cards and Today moved onto meal ids, with the D-27 volunteer shift inside the meal composer

## Files changed (all inside the contract)

- `frontend/lib/api.ts` — `MealView` (was `MealServiceView`) and `MealDishView` (was `MealPlanView`), required and nullable where the server always sends them, `volunteerShift: ShiftView | null` on the meal; `SaveMealInput` / `UpdateMealInput` / `MealDishDraft` / `MealShiftDraft` (`volunteerShift` required-and-nullable, like the pin, because the composer builds it with a spread); `SavedMeal`, `CancelledMeal {volunteersTold}`; `RecordMealInput` / `CorrectMealInput` on `dishId` with no date, kind or event name; `ShiftView.mealId/mealKind/mealEventName` required-nullable; `ShiftInput` without meal fields and `UpdateShiftInput = ShiftInput & {mealId}`; `mealId` added to `CommittedMeal` (`mealPlanId` → `dishId`), `MealSufficiency` (+`eventName`), `MealCrewView`, `TodayMeal` (+`eventName`), `OutsideCommitment`, `MenuHistoryView`. Calls: `meals`, `getMeal`, `saveMeal`, `updateMeal`, `cancelMeal`, `mealSummary`, `recordMeal(mealId,…)`, `correctRecordedMeal(mealId,…)`, `requestJobCard(mealId,…)`, `jobCardLanguages(mealId)`, `jobCardPrintUrl(mealId,…)`, `travelEstimate(mealId)`, `repeatEvent(mealId,…)`, `updateShift(id, UpdateShiftInput)`. Removed: `listMealPlans`, `createMealPlan`, `updateMealPlan`, `cancelMealPlan`, `mealServices`, `mealServiceSummary`, and every `/meal-plans/{id}…` and `/meal-services…` path.
- `frontend/app/planner/[date]/[kind]/page.tsx` — deleted.
- `frontend/app/planner/meal/[id]/page.tsx` — new: edit a meal by its id (recorded, past or cancelled meals read-only; 404 shown in the server's words).
- `frontend/app/planner/confirm-layer.tsx` — new: `ConfirmLayer` (the planner's alertdialog) and `useLeaveGuard` ("Leave without saving?" for in-app links and `beforeunload`).
- `frontend/app/planner/page.tsx`, `frontend/app/planner/catch-up/page.tsx` — `api.meals`, `MealView`, sufficiency by `dishId`, keys by `mealId`.
- `frontend/components/planner/DayView.tsx` — sufficiency by `dishId`.
- `frontend/components/planner/MealComposer.tsx` — one `saveMeal` / `updateMeal` request (dishes by id, `volunteerShift` draft); section 4 *Ask for volunteers* / *View volunteer shift*; the layer beside the form; times-changed warning before *Update this meal*; leave guard; crew row by `mealId`; grain confirmation names every offending preparation.
- `frontend/components/planner/MealServices.tsx` — `api.meals`; crew by `mealId`; the meal's own `volunteerShift` shown as a link to its meal (no layer, no Ask, no `MovedNotice`); *Cancel this meal* with the count warning; recording, correction, job card, travel estimate and repeat by `mealId`; Edit links to `/planner/meal/<id>`.
- `frontend/components/planner/ShiftLayer.tsx` — rewritten: no API call, button **Done**, hands back a `MealShiftDraft`; exports `timesChanged`, `timesChangedWarning`, `derivedTitle`.
- `frontend/app/volunteers/shift-form.tsx` — the label *Capacity* reads *Volunteers requested*.
- `frontend/app/today/page.tsx` — keys by `mealId` (two lists).
- `frontend/app/inventory/[id]/page.tsx` — claims keyed by `dishId`, linking to `/planner/meal/<mealId>`.
- Type-only: `frontend/app/volunteers/[id]/edit/page.tsx` sends `mealId: shift.mealId` in place of the three copied fields.
- Tests: `meal-composer`, `planner`, `planner-day-routes`, `planner-shift` (rewritten for the composer), `meal-recording`, `meal-correction`, `crew-pebble`, `today`, `inventory-correction`, `leave`; type-only: `shift-edit-keeps-meal-link` (asserts `mealId` now), `volunteer-shifts`, `shift-attendance` (three nulls on a fixture).
- Not source, stated plainly: the generated `frontend/.next/types/app/planner/[date]/[kind]/` was deleted (through the lock), because the stale file made `tsc` fail on the removed route; `next build` regenerates `.next/types`.
- Unchanged and checked: `nav.ts`, `routes.ts`, `AddressPicker.tsx`, `compose/page.tsx`, `[date]/page.tsx`, `reuse/page.tsx`, `cost-per-serving`, `leave` and `calendar` pages (they compile on the new types with no change), and the other volunteer pages. `routes`, `sign-out`, `issued-from-store`, `kitchens`, `kitchen-form`, `ingredient-request-new`, `unit-refusal-names-ingredient`, `reuse-plan`, `cost-per-serving`, `calendar`, `available-shifts`, `my-shifts`, `role-refusals` tests needed no change.
- Git index: a `git rm --cached` on the old route was run by mistake and undone at once with `git reset -- <path>`; nothing is staged.

## Commands run and what came back

Targeted, after the rewrite:

```
tools/work-lock.sh run verify 'cd frontend && npx tsc --noEmit; echo "TSC_EXIT $?"; npx vitest run __tests__/meal-composer.test.tsx __tests__/planner.test.tsx __tests__/planner-day-routes.test.tsx __tests__/planner-shift.test.tsx __tests__/meal-recording.test.tsx __tests__/meal-correction.test.tsx __tests__/reuse-plan.test.tsx __tests__/crew-pebble.test.tsx __tests__/today.test.tsx __tests__/cost-per-serving.test.tsx __tests__/inventory-correction.test.tsx __tests__/leave.test.tsx __tests__/calendar.test.tsx __tests__/volunteer-shifts.test.tsx __tests__/shift-edit-keeps-meal-link.test.tsx __tests__/shift-attendance.test.tsx __tests__/available-shifts.test.tsx __tests__/my-shifts.test.tsx __tests__/role-refusals.test.tsx'
```
```
TSC_EXIT 0
 Test Files  19 passed (19)
      Tests  343 passed (343)
work-lock: 'verify' released; command exited 0
```

(The run before that was red and is recorded: `meal-composer.test.tsx(1362,1): error TS1005: '}' expected` from a splice that duplicated a `beforeEach(` line, and one failing planner-shift test that pressed Done with the required Start box empty, so the form refused. Both fixed in the test files; the component was not changed for either.)

Negative controls — `tools/work-lock.sh run verify "bash <scratchpad>/control-T-198.sh"`, script and output `control-T-198.sh` / `control-T-198.out`, snapshot `control-T-198.MealComposer.tsx.snapshot`, restore on `trap … EXIT`:

```
== control: the meal save drops the drafted shift
anchor count: 1
mutated line: 941:      volunteerShift: null,
   × the shift is saved only with the meal > sends one Save this meal request carrying the drafted shift, and no shift request
 FAIL  __tests__/planner-shift.test.tsx > the shift is saved only with the meal > sends one Save this meal request carrying the drafted shift, and no shift request
 Test Files  1 failed (1)
      Tests  1 failed | 26 skipped (27)
== control: the times-changed warning is skipped
anchor count: 1
mutated line: 928:    if (false as boolean && warnTimes && !timesAcknowledged) {
   × new times on a shift people signed up for > warns before Update this meal saves, in the ruled words, and saves once confirmed
 FAIL  __tests__/planner-shift.test.tsx > new times on a shift people signed up for > warns before Update this meal saves, in the ruled words, and saves once confirmed
 Test Files  1 failed (1)
      Tests  1 failed | 26 skipped (27)
RESTORED: MealComposer.tsx identical to snapshot
work-lock: 'verify' released; command exited 0
```
and after the lock: `cmp -s MealComposer.tsx control-T-198.MealComposer.tsx.snapshot` → `post-check: identical`; `grep -c "      volunteerShift: shiftDraft,"` → `1`.

Full frontend check (acceptance 5), output in `full-T-198.out`:

```
tools/work-lock.sh run verify 'cd frontend && npx tsc --noEmit && echo "TSC_OK" && npm test && npx eslint . --max-warnings=0 && echo "ESLINT_OK" && npx next build'
```

tsc printed nothing; the first line of the log is the echo after it:
```
TSC_OK
```
vitest:
```
 Test Files  137 passed (137)
      Tests  1825 passed (1825)
   Start at  01:52:14
   Duration  14.63s (transform 5.97s, setup 4.39s, collect 18.16s, tests 66.76s, environment 23.16s, prepare 4.39s)
```
eslint printed nothing, then:
```
ESLINT_OK
  ▲ Next.js 14.2.15
```
next build, the planner routes and the tail of the table:
```
 ✓ Compiled successfully
├ ○ /planner                             5.23 kB         166 kB
├ ƒ /planner/[date]                      4.47 kB         163 kB
├ ○ /planner/catch-up                    2.4 kB          161 kB
├ ○ /planner/compose                     1.23 kB         164 kB
├ ƒ /planner/meal/[id]                   1.51 kB         164 kB
├ ○ /planner/reuse                       6.44 kB         152 kB
...
└ ○ /wishlist/new                        1.84 kB         151 kB
+ First Load JS shared by all            87.4 kB
  ├ chunks/2117-f1dd35d29830fd8c.js      31.8 kB
  ├ chunks/fd9d1056-35cc334abb0f71e3.js  53.6 kB
  └ other shared chunks (total)          1.97 kB

○  (Static)   prerendered as static content
ƒ  (Dynamic)  server-rendered on demand

work-lock: 'verify' released; command exited 0
```
The test log also carries 116 React "An update to MealComposer inside a test was not wrapped in act(...)" warnings from the composer's async effects; none is a failure.

## Acceptance criteria

1. **Composer, in `planner-shift.test.tsx`:** Ask for volunteers at needed 6 over rostered 5, absent at 5 (equal) and 4 and when needed is empty ("appears exactly when…", "is not offered while nobody has said…"). The layer: no checkbox or select, no "for a meal" text, date read-only (not disabled) with the meal's day, the eight `ShiftFields` boxes, button **Done** and no Post/Save button ("opens the volunteers' own form…"). *Volunteers requested* label, prefilled 3 (8 − 5), editable ("prefills Volunteers requested…"). Done sends no request of any kind and View volunteer shift replaces Ask ("sends no request when Done is pressed…"). Save this meal sends one `saveMeal` whose `Object.keys` include `volunteerShift`, the draft's own keys are exactly the seven of `MealShiftDraft`, and no shift call is made ("sends one Save this meal request…"). View volunteer shift on a meal that already has one, even fully crewed ("shows View volunteer shift on a meal that already has one…"), and an existing shift's change goes only with `updateMeal` ("opens a saved shift on its own values…"); an update that did not touch it sends `volunteerShift: null` by key. Leave without saving: the dialog appears on a link click, Leave pushes the day's address, and `saveMeal`, `updateMeal`, `createShift`, `updateShift`, `cancelShift` are all uncalled ("…abandoning it sends nothing — no orphan shift"); Stay keeps the draft; `beforeunload` is prevented only once something changed; an untouched meal leaves without asking. The same at route level in `planner-day-routes.test.tsx` ("asks before leaving with a change nobody saved…"). Cancelling a meal with a shift: the dialog reads "This meal has a volunteer shift. 3 volunteers are signed up and 1 is waiting. Cancelling the meal cancels the shift too, and they will be told." before anything is sent, then one `cancelMeal("meal-lunch")` and "Lunch was cancelled. 4 volunteers were told." ("warns with the count…"); Keep it sends nothing. Times changed on update with signups: the dialog text is exactly "3 volunteers are signed up. They’ll be told the new times.", nothing sent until confirmed, and "1 volunteer is signed up. They’ll be told the new times." for one; no warning when times are unchanged or nobody is signed up. "never calls it a move": `document.body.textContent` does not match `/\bmoved?\b/i` with the warning open. `grep -rnw moved components/planner app/planner` finds only a quotation in `reuse/page.tsx` (a comment, not T-198's file to edit).
2. **Recording, correction, job cards, reuse, Today, cost per serving, inventory link, leave:** `meal-recording` (record by meal id, body keys exactly `dishes`/`note`, dishes by `dishId`, job card and languages by meal id, events by their own id), `meal-correction` (correction sent to `meal-lunch` with `dishId`s, `consumedQuantity` key present and null), `planner-day-routes` (travel estimate and repeat by meal id, edit route by id, recorded/cancelled/404 states, sufficiency by `dishId`), `planner` (Edit links to `/planner/meal/meal-lunch`), `inventory-correction` (links to `/planner/meal/meal-1`, `/planner/meal/meal-2`), `today`, `leave`, `reuse-plan`, `cost-per-serving`, `crew-pebble` — all updated in place, none deleted; all pass in the full run.
3. **Keys, not `objectContaining`:** every absent/present assertion above reads `Object.keys` (`volunteerShift`, the draft's seven keys, `mealKind`/`planDate`/`mealKindId` absent, record body keys, `mealId` on the shift edit).
4. **Negative control:** two, both failing the matching test, file restored and `cmp`-checked — output above.
5. **Full frontend checks:** green — output above.
6. **Hand smoke test:** not done. The stub-auth / API-stub harness is not in the tree, and building it needs edits to `lib/firebase.ts`, `lib/auth-context.tsx` and `app/api/`, which are outside my paths.

## Not done / needs Rajeev

Decided inside the brief, reversible, and worth his eye when he tests:

- **No mockup exists** for section 4 or the layer (searched `docs/` for the button names and read the T-019/T-155 proofs), so these words and placements are mine: the two buttons are small secondary buttons under the People needed / Rostered row, with a line beside *View volunteer shift* ("Not saved yet. It is saved with this meal." / "2 of 5 signed up" / "…. Your changes are saved with this meal."); the layer keeps the headings *Post a shift* / *Edit a shift* and adds "Saved when you save the meal."; the leave dialog's buttons are *Stay on this page* / *Leave without saving*; the times dialog is titled "The volunteer shift has new times" with *Go back* / *Update this meal*.
- **Cancel this meal is new on the day's meal block.** The planner had no way to cancel a whole meal (only dropping dishes one at a time), and answer 5 presupposes one. It sits beside Edit on a planned, unrecorded meal; the dialog is "Cancel Lunch?" with *Keep it* / *Cancel this meal*.
- **The day's block no longer asks for volunteers or opens a shift layer.** Answer 7 says nothing in the planner saves before the meal does, and a layer on the day would have saved on its own. The shift pebble ("2 of 5 signed up") now links to the meal's edit page, where *View volunteer shift* is.
- **The *Volunteers requested* label is changed in the shared `ShiftFields`,** so Post a shift and the Volunteer shifts edit page read it too (answer 1 says both screens).
- **Leave without saving? does not catch the browser's Back button** inside the app: the App Router has no hook before a history pop. Reload, closing the tab and every in-app link are caught.
- **`DayCoverage.shortAtMealId` was not added to `api.ts`:** `__tests__/staff-schedule.test.tsx` (not in my paths) builds that type by hand, so a required field would break `tsc`. Nothing in my screens reads it.
- **A new meal's Rostered:** a new main meal borrows the crew row of that day's meal of the same kind (the save lands on it); a new event counts nobody until it is saved, so *Ask for volunteers* prefills People needed in full there.

## For T-199

- The meal's address is **`/planner/meal/<mealId>`** (`app/planner/meal/[id]/page.tsx`); link a meal shift's read-only meal there. "in the planner" can point at `/planner`.
- Reuse from `components/planner/ShiftLayer.tsx`: `timesChanged(before, after)` and `timesChangedWarning(signedUpCount)`, which returns the ruled sentence with the singular handled. Reuse `ConfirmLayer` from `app/planner/confirm-layer.tsx` for the warning before saving.
- `api.ts` is ready for you: `ShiftInput` (Post a shift) has no meal fields; `updateShift` takes `UpdateShiftInput` with a required `mealId`, and `app/volunteers/[id]/edit/page.tsx` already sends `shift.mealId`; `ShiftView.mealId/mealKind/mealEventName` are required and nullable.
- Still yours: `moved-notice.tsx`, `moved` / `movedUnderRoster` in `shift-form.tsx`, the `?moved=` flow in `app/volunteers/page.tsx` and the edit page, *Start*/*End* → *Start time*/*End time*, the field order, the pointer sentence, and the list label "For Lunch, 15 September".

## After-UAT lines

- A shift drafted in the planner cannot be thrown away on its own; the only way is to leave the meal without saving.
- The browser's Back button inside the app is not caught by "Leave without saving?".
- The staff schedule still names the short meal by kind; `shortAtMealId` is not on the frontend type yet.
- Today's meal card shows the kind rather than an event's own name, and links to the day rather than the meal.
- `meal-composer.test.tsx` logs about a hundred React act() warnings from the composer's async effects.
