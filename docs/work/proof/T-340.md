# T-340 (was T-310) — no repeat copy on a past date; test cleanup clears meal_series

> **Renumbered.** I built this as T-310 and wrote its proof to `docs/work/proof/T-310.md`. Another work stream had also allocated T-310, and its proof overwrote mine. The work manager renumbered this task T-340. This file was rewritten from my own record of the same session. Nothing was re-run for it, and every output below is pasted from the runs I made at the time. The scratchpad artefacts still carry the original number (`verify-T-310.sh`, `control-T-310.*`, `results-T-310`).

**Files changed** (all inside the contract):

- `backend/src/main/java/org/iskcon/kms/meal/MealPlanService.java` — `repeatWalk` only, plus its javadoc sentence about the dates. Candidates earlier than `clock.today()` (the temple's zone) are dropped before anything else looks at them: not made, not in `skippedFasting` or `skippedAlreadyPlanned`, not counted. Today is kept. The grid stays source date + k × everyWeeks. If nothing is left, the existing KMS-400177 applies. No API shape change. (The file already carried T-307's uncommitted changes; this task touched only the walk.)
- `backend/src/test/java/org/iskcon/kms/meal/MealFixture.java` — `deleteAll` now deletes `meal_series` straight after `meals`.
- `backend/src/test/java/org/iskcon/kms/meal/MealPlanIT.java` — `repeatForwardMakesASeriesOfOrdinaryMeals` moved from 2025 dates to "a week from today at the temple", with the same assertions (6 copies, series count 7, the 4th listed occurrence is 3 weeks on, editing the 3rd changes no other). Its own `DELETE FROM meal_series` was removed because the fixture now does it.
- `backend/src/test/java/org/iskcon/kms/meal/MealSeriesIT.java` — two new tests, and its own `DELETE FROM meal_series` removed. One existing test also had to change: `cancelThisAndLater` got its "last week" occurrence from a repeat of a source two weeks back, and the floor now stops that. That occurrence is now planned directly and joined to the series by SQL, which is what a series made two weeks ago would already hold. Every assertion in the test is unchanged.
- `docs/work/proof/T-340.md` — this file. (`docs/work/proof/T-310.md` now belongs to the other task and was not touched.)

## Commands run and what came back

Green run. Everything went through `tools/work-lock.sh run verify`, backgrounded, using the script `scratchpad/verify-T-310.sh`. The script runs `./gradlew test --tests 'org.iskcon.kms.meal.*'` plus BaseQuantityIT, UnitLabelAgreementTest, TempleClockTest, CommunicationSendGuardSourceTest, ErrorCodeTest, FieldErrorMessageTest, NextStepPermissionTest, RolePermissionsTest, RowLevelSecurityIT, TenantLoopMigrationIT, TenantDeletionIT and today.TodayIT.

The first run had 1 failure: `MealSeriesIT.cancelThisAndLater`, `EmptyResultDataAccessException: Incorrect result size: expected 1, actual 0` at `mealOn(TODAY.minusWeeks(1))`. That is the past copy the floor now refuses. I fixed it as described above and re-ran.

Second run, log tail:
```
BUILD SUCCESSFUL in 1m 4s
5 actionable tasks: 2 executed, 3 up-to-date
T-310 verify exit 0
```
Counts from `build/test-results/test/*.xml` (copied to `scratchpad/results-T-310`). Each line is tests, all with 0 skipped, 0 failures and 0 errors:
```
RolePermissionsTest 90 · CommunicationSendGuardSourceTest 2 · ErrorCodeTest 912 · FieldErrorMessageTest 6
NextStepPermissionTest 6 · UnitLabelAgreementTest 2 · BaseQuantityIT 7 · CateringMigrationIT 1 · DeliveryPinBackfillIT 1
EkadashiFlaggingIT 6 · EventIdentityIT 6 (+ nested "the event-name suggestions" 6) · MealCorrectionIT 15
MealCrewDefaultTest 4 · MealCrewIT 12 · MealKindIT 11 · MealPlanIT 20 · MealRebuildMigrationIT 10 · MealRecordingIT 9
MealSaveIT 11 · MealSeriesIT 15 · MenuHistoryIT 7 · ReusePlanIT 8 · SufficiencyIT 12 · TravelEstimateIT 15
RowLevelSecurityIT 30 · TempleClockTest 1 · TenantDeletionIT 9 · TenantLoopMigrationIT 1 · today.TodayIT 16
TOTAL tests=1251 skipped=0 failures=0 errors=0
```

Negative control, following README lesson 4. It ran through the lock with the script `scratchpad/control-T-310.sh`. The script uses `set -euo pipefail`, snapshots the file, and restores it through a `trap … EXIT`. It asserts the anchor count with `grep -cF`, proves the patch changed the file with `cmp -s`, and runs MealSeriesIT and MealPlanIT against the tests as written. The patch replaces `if (!d.isBefore(today)) {` with `if (true) {`. Log `scratchpad/control-T-310.log`:
```
control-T-310 started Sat Sep 19 12:02:34 PDT 2026
control-T-310: anchor count 1 (expected 1)
control-T-310: patch applied, file differs from snapshot
1242:			if (true) { // control-T-310: floor removed
MealSeriesIT > this and every later one cancels only the later ones still to cook — never a past, cooked or recorded one FAILED
MealSeriesIT > repeating a past event makes copies from today on, today included, on the source's own grid FAILED
MealSeriesIT > a past event whose every date up to the end date is gone makes nothing (KMS-400177) FAILED
35 tests completed, 3 failed
BUILD FAILED in 20s
control-T-310: gradle exit 1
control-T-310: restored, cmp identical
```
Counts from the control's XML:
```
<testsuite name="org.iskcon.kms.meal.MealPlanIT" tests="20" skipped="0" failures="0" errors="0"
<testsuite name="org.iskcon.kms.meal.MealSeriesIT" tests="15" skipped="0" failures="3" errors="0"
```
Failure messages from the XML:
- the past-source test: `Expecting empty but was: ["2026-09-06"]`. That is the gone Ekadashi date showing up in `skippedFasting`.
- the no-date test: `Status expected:<400> but was:<200>`.
- `cancelThisAndLater`: `["PLANNED", "PLANNED"] to contain exactly (and in same order): ["PLANNED"]`. Without the floor, the repeat also made a copy last week, on top of the one the test planned.

MealPlanIT stayed 20/20 under the control. That is expected: its repeat now starts in the future, so the floor never comes into play there. After the trap ran, `cmp snapshot MealPlanService.java` printed `CMP-IDENTICAL`.

## Live check

Local API :8080, database kms_verify, Flyway at V149. The conductor restarted it at 19:03Z with this change. The token was a Firebase custom token for `ikms.temple-admin.1` (Temple Admin). Today at the temple was 2026-09-20 IST (00:33).
```
$ POST /api/v1/meals  (Temple Admin: Event "T-310 live check - past reading", Sun 2026-08-30, Akki Rotti x30)
{"id":"27b8c4ab-50aa-4dcf-a028-2ea1bf79b2b3"} HTTP201

$ GET /api/v1/meals/27b8c4ab-50aa-4dcf-a028-2ea1bf79b2b3/repeat-preview?everyWeeks=1&until=2026-10-11
{"copies":4,"preparations":4,"dates":["2026-09-20","2026-09-27","2026-10-04","2026-10-11"],"skippedFasting":[],"skippedAlreadyPlanned":[],"lastDate":"2026-10-11","series":{"seriesId":null,"everyWeeks":1,"until":"2026-10-11","position":1,"count":5}} HTTP200

$ GET /api/v1/meals/27b8c4ab-50aa-4dcf-a028-2ea1bf79b2b3/repeat-preview?everyWeeks=2&until=2026-10-11
{"copies":2,"preparations":2,"dates":["2026-09-27","2026-10-11"],"skippedFasting":[],"skippedAlreadyPlanned":[],"lastDate":"2026-10-11","series":{"seriesId":null,"everyWeeks":2,"until":"2026-10-11","position":1,"count":3}} HTTP200

$ GET /api/v1/meals/27b8c4ab-50aa-4dcf-a028-2ea1bf79b2b3/repeat-preview?everyWeeks=1&until=2026-09-19   (every date on the grid already gone)
{"code":"KMS-400177","message":"No copies fit before that end date.","action":"Choose a later end date, or repeat more often.","fieldErrors":[]} HTTP400
```
Weekly from 30 Aug skips 6 and 13 Sep, and today (20 Sep) is made. Fortnightly keeps the grid: 13 Sep is gone, so the first copy is 27 Sep. It is not moved to "today plus two weeks".

Cleanup: the check created one meal, its one dish, the 2026-08-30 day row (its `created_at` was `2026-09-19 19:03:55.325878+00`, the same instant as the meal's `MEAL_PLANNED` audit row) and that one audit row. All four were deleted as the local `kms` superuser:
```
BEGIN
DELETE 1
DELETE 1
DELETE 1
COMMIT
 t310_meals 
------------
          0
(1 row)

DELETE 1
```
No repeat was run live, so no series was created.

## Acceptance criteria

- **A past event makes copies only from today onward, and today is made.** Verified by `aPastSourceMakesCopiesOnlyFromToday` (weekly from 3 weeks ago: dates are today, +1w, +2w; the gone fast and the gone already-planned date are not named as skips) and live.
- **The preview agrees.** The same test compares the preview field by field with the repeat. The fortnightly case in the same test checks that the grid is kept (first copy +4 days from a source 24 days back).
- **A past event whose whole grid up to `until` is gone gets 400177.** Verified by `aPastSourceWithNoDateLeftIsRefused`: both the repeat and the preview, both when `until` is yesterday and when `until` is in the future but the next grid date falls after it. Row counts are unchanged and there is no series. Also checked live.
- **`MealFixture.deleteAll` clears `meal_series`.** Both test classes now clean up with the helper alone and pass (TenantDeletionIT and the whole meal package are green).
- **MealPlanIT no longer relies on past copy dates.** The repeat test was moved onto future dates with the same assertions.
- **The new tests fail without the floor.** Shown by the control above: 3 red, applied and restored.

## Not done / needs Rajeev

- `cancelThisAndLater` in MealSeriesIT had to change its setup, as described above. The brief named only MealPlanIT, but the file is inside the contract.
- The tests compute today at class load in Asia/Kolkata. A run that crosses midnight IST could fail spuriously. The existing T-307 tests already have the same exposure.
- Backend only, so there is nothing to smoke-test by hand. The live API check above takes its place. The screen side belongs to T-308.
