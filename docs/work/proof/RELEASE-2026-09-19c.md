# Release 2026-09-19c — Epic 12: which kitchen is cooking

Third release of 19 September, after the procurement release (`RELEASE-2026-09-19b.md`). Rajeev
cleared it on the local worktree stack the same afternoon: *"Everything looks good. Clear to deploy
to check in and CI green and Deploy to cloud… No approval needed for cloud deploy"* (terminal,
~17:25 PDT). Deploy is to **staging only**.

## What shipped

| Commit | |
|---|---|
| `e75e582` | `fix(web): saving a bill waits for the upload, and a new ingredient request says so` (T-370) |
| `596ce9a` | `feat: a meal says which kitchens are cooking it, and every cook belongs to one` (T-350…T-358, T-361…T-363) |
| `779b7b9` | `feat(web): the menu in the order Rajeev settled, and two screens renamed to say what they are` |
| `78dd9eb` | `docs: the Epic 12 record — proofs, the UAT test, traceability, and the staging findings` |

170 files changed, 14573 insertions, 1259 deletions, against base `762a560`.

Migrations **V150** (meal/dish/staff kitchen columns and the per-temple backfill to the main
kitchen), **V151** (People needed moves to each kitchen), **V152** (the job card's version moves to
each kitchen). New error codes **KMS-400180 to KMS-400187**.

## How the tree was assembled

The kitchen work was built in the worktree `agent-a93967b8b0080da44` on base `97b8a2b`, as 159
uncommitted files. Main had moved on two docs commits. The worktree's changes were snapshotted and
cherry-picked onto main rather than merged, to keep history linear.

**One conflict, in `docs/uat/TRACEABILITY.md`** — both sides added rows to the same table. Resolved
by keeping both, per instruction.

**One collision that was not a text conflict and would have shipped broken.** The worktree added
`docs/uat/UAT-087-kitchens-in-the-meal-planner.md`; UAT-087 was the next free number when it was
written. The procurement release reached main first and took UAT-087 through UAT-092, so two
different files claimed UAT-087 and git merged them without complaint because they are separate
paths. The Epic 12 file is renumbered to **UAT-093**, with every Epic 12 reference moved with it in
`TRACEABILITY.md`, `README.md`, `EPIC-12-…-DESIGN.md` and proofs T-360/T-362. The procurement
references to UAT-087 were left alone — they point at `UAT-087-one-ingredient-not-five.md` and are
correct. `T-360.md` records why the number changed.

## What was kept out

The outside-events work was parked and must not ship. Confirmed absent three ways: no
`scratchpad/T-363.patch` exists in the worktree; the outside-commitments card is still wired up at
`frontend/app/planner/page.tsx:255` and `frontend/lib/api.ts:5679`; and after deploy
`/api/v1/meal-plans/outside-commitments` still answers (401, not 404) on staging. The `TodayService`
diff is kitchen-naming only — no nudge change.

## Verification on the committed tree

`git archive HEAD` into a clean directory, then `git init && git add -A` so the design-system test
can run `git ls-files`.

```
backend   3563 tests, 0 failures, 0 errors, 7 skipped   BUILD SUCCESSFUL in 7m 23s
frontend  tsc --noEmit clean
          182 test files, 2589 tests passed
          next build OK
```

The counts are one and six above the worktree's own run (3562 / 2583) — T-370's one backend test and
six frontend tests, which the worktree did not have.

`tools/check-ignored-sources.sh` **fails in the working tree and passes in the archive.** The two
files it names, `frontend/app/dev-kitchen-meal/page.tsx` and `frontend/app/dev-menu/page.tsx`, are
the local playgrounds Rajeev used for the menu rounds, excluded by `.git/info/exclude` — which is
machine-local and never committed. CI checks out a repository that has neither the files nor the
rule, and the `Repository` job passed.

## CI

https://github.com/rajeevkatamaneni/iskcon-kitchen-management/actions/runs/35480272338 on `78dd9eb`
— **success**: Repository ✓, Frontend (Next.js) ✓, Backend (Spring Boot) ✓.

## Backup

On-demand Cloud SQL backup of `kms-staging-5325bd0d` taken before the deploy, described
`pre Epic 12 kitchens release 78dd9eb V150-V152`. The create returned `backed up.` Its id was not
read back: `gcloud sql backups list` is not a command this session is permitted to run.

## Deploy

`infra/deploy.sh iskcon-kms-2026 staging`, tag `20260919-182016`. Builds 10m12s, rollouts 2m02s,
total 12m17s — slower than the 5m49s warm figure, so this was not a warm cache.

The wrapper's exit code is not evidence, so each of these was checked directly.

| | before | after |
|---|---|---|
| api | `kms-staging-api-00170-c62` | **`kms-staging-api-00171-wwf`** |
| web | `kms-staging-web-00158-m8q` | **`kms-staging-web-00159-qj2`** |
| worker | `kms-staging-worker-00152-z26` | **`kms-staging-worker-00153-wtc`** |

Image digests genuinely changed, not just the tag:

```
api  sha256:3358b190…1559e  ->  sha256:073629c0…5616a
web  sha256:8f971ec3…1cfb9  ->  sha256:62955e45…9fd4b9
```

**Flyway**, from the `kms-staging-api-00171-wwf` startup log:

```
Current version of schema "public": 149
Migrating schema "public" to version "150 - which kitchen is cooking"
Migrating schema "public" to version "151 - crew is per kitchen"
Migrating schema "public" to version "152 - card version is per kitchen"
Successfully applied 3 migrations to schema "public", now at version v152
```

**Health**: `{"status":"UP","db":"UP","scheduler":"STANDBY","worker":"RUNNING"}`. Web returns HTTP 200.

**The new code answers, not just a new revision.** `/api/v1/staff/kitchen-checks` returns 401 rather
than 404, so the Epic 12 controllers are live. The served frontend bundle
(`/_next/static/chunks/9179-1c0df797341c92c5.js`) contains "Issued to kitchens" and "All kitchens",
and "Issued from store" appears nowhere in it.

## Where Rajeev can see it

https://kms-staging-web-bnpkv5hfrq-el.a.run.app — meal planner (kitchen sections and **+ Add another
kitchen**), Staff (the kitchen field and **Check these kitchen assignments**), Today (kitchen names
on each meal), and the left-hand menu in the new order with **Issued to kitchens** and **All
kitchens**. The script is `docs/uat/UAT-093-kitchens-in-the-meal-planner.md`.

## Outstanding

- Nothing here has been driven on staging by a person. The suites and the probes above are all that
  back it; **UAT-093 has not been walked**.
- The V150 backfill put every existing staff member in their temple's main kitchen. That is a guess
  by design, and the **Check these kitchen assignments** list is how it gets corrected — it needs a
  Temple Admin to go through it on staging data.
- Defect 5 from `STAGING-E2E-2026-09-19.md` (`whatsappEverSent`) was judged not a defect; `T-370.md`
  explains why and that judgement is still waiting on Rajeev.
