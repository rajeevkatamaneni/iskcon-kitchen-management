# T-239 — temple recipe page: Scale removed, language picker and PDF icon on the ingredients heading line

**Files changed:** (both inside the contract)
- `frontend/app/recipes/[id]/page.tsx`: removed the Scale control and its state (`scaled`, `targetYield`, "Scaled to" / "This recipe makes" facts, scaled quantities, `targetYield` PDF parameter) and the whole toolbar card. Added an "Ingredients" h2 line with, on the right, a language `<select>` (English default, all 23 languages, accessible name "Language"), a `role="status"` busy pot with sr-only "Translating…", and a 44×44 icon button (`ti ti-download`, `aria-label` and `title` "Download recipe as PDF"). Choosing a language translates at once. English clears the translation with no request. A stale answer is dropped (request counter), so Hindi-then-Kannada ends on Kannada. On failure the page goes back to English, shows the ErrorNotice plus "The recipe is shown in English.". The PDF request uses `{ language }` of the translation on screen. Table markup and columns untouched, apart from dropping the scaled-quantity branch in the Quantity cell.
- `frontend/__tests__/recipe-detail.test.tsx`: Scale/Translate-button assertions replaced, T-218 "Scale to box" block deleted, and 4 tests added: translate on choose, then back to English with no second call; busy status + latest choice wins; failure falls back to English and says so; PDF request is `{language: undefined}` in English, then `{language:"hi"}`.

Library page `app/recipes/library/[id]/page.tsx`: grep for translat/pdf/download found nothing, so it was left alone.
`api.scaleRecipe` / `ScaledRecipe` are now unused in the frontend, but they live in `lib/api.ts`, which is off-limits to me, so they were left in place.

**Commands run and what came back:**

`tools/work-lock.sh run verify 'cd frontend && npx tsc --noEmit && echo TSC-CLEAN && npx vitest run ... ; npx eslint "app/recipes/[id]/page.tsx" __tests__/recipe-detail.test.tsx && echo ESLINT-CLEAN'`
```
TSC-CLEAN
 FAIL  __tests__/design-system.test.ts > items 10 and 11 — a label lines up with the words in its box > indents every field label to the inset, and sets it in the one label style
 FAIL  __tests__/design-system.test.ts > item 12 — a table row answers the pointer > puts one step of tone on every row of every table body
 FAIL  __tests__/design-system.test.ts > item 13 — the words > writes an apostrophe one way
 Test Files  1 failed | 146 passed (147)
      Tests  3 failed | 2007 passed (2010)
ESLINT-CLEAN
```
The three failures, from `npx vitest run __tests__/design-system.test.ts`. All three are in app/dev-buttons, which was expected:
```
+   "app/dev-buttons/page.tsx:399 Shift title",
+   "app/dev-buttons/page.tsx:307",
+   "app/dev-buttons/page.tsx:260 entity",
```
Earlier, `npx vitest run __tests__/recipe-detail.test.tsx`: `Tests  12 passed (12)` (before the 13th test was added; the full run above includes all 13).

**Browser (local http://localhost:3000, signed in as Temple admin, Basmati Ghee Rice, id 2bdce9b5-…):**
- Height at 1470 wide: before 1588px (table top 363, toolbar card 102px + 24 margin). After 1514px (table top 289). The page is 74px shorter.
- Height at 390 wide (iframe): before 2344px (card 246px, table top 541). After 2126px (table top 323). The page is 218px shorter. scrollWidth is 390 before and after, so nothing scrolls sideways.
- 390 geometry after: h2 16–105, select 178–322, icon button 330–374, all on one row (top 270). With the longest label "Manipuri (Meitei)" selected, it is still one row with the same x positions and scrollWidth 390.
- 1470 geometry: the icon's right edge is 1438 and the Quantity column's right edge is 1437, so the picker sits above Quantity.
- Hindi: choosing it sent `GET /api/v1/recipes/…/translations/hi` → 200. The h1 changed to "[hi] Basmati Ghee Rice", the first ingredient to "[hi] Basmati rice", method step 1 to "[hi] 1. Boiling Water 120 L", and the category to "[hi] Rice". The tag read "Hindi · machine translation". The local provider is a stub that prefixes the code; this is not real Hindi.
- English back: the text returned to the original and the tag disappeared. The network log showed no new request.
- Download in English: `POST /api/v1/recipes/…/pdf` (no query) → 202, followed by polling `GET /api/v1/documents/188c930b…`. After Hindi: `POST …/pdf?language=hi` → 202. The icon turned into the busy pot and was disabled while it polled.

**Acceptance criteria:**
1. Scale removed, Makes stays. Verified by test ("scale to" text absent) and in the browser (`/Scale to/` absent; the Makes fact shows 100 L).
2. Translate row removed; the picker is at right above Quantity, English by default, and translates on choose with busy/tag/fallback. Verified by tests and in the browser as above.
3. Download icon: name and tooltip are "Download recipe as PDF", and it requests the on-screen language. Verified by test and by the browser network log.
4. Toolbar card removed and the page is shorter. Measured above.

**Not done / needs Rajeev:**
- DESIGN_SYSTEM §6 says Download is a text label and an icon never carries meaning alone. Rajeev's decision asked for an icon, so I followed him. The icon carries an aria-label and a tooltip. §6 is locked, so this is a documented exception; recorded in a page comment.
- I added a visible "Ingredients" heading on the left of the picker line, sized like the Method heading below, so the line is not empty on the left. The decision did not mention it and it is easy to remove.
- The saved PDF file itself is not verified. Locally the document stayed pending for more than 40s of polling, and I stubbed the anchor click, so no file was saved.
- The busy pot during translation was not seen in the browser, because the local stub answers too fast. It is covered by a test.
- The failure fallback was checked by test only, not in the browser.
- The window was not resized; 390 was checked in an iframe.
