# Release 2026-09-19 — the local build, T-217 to T-241

Release agent. Staging only. Rajeev verified the work locally on 2026-09-18/19 and approved commit,
CI and a staging deploy.

## Commits (straight to main, named paths only)
- `9702d32` backend: portion unit must match the recipe's unit family, dish amount capped at 50,000, message text, "Catering sabjis" to "Bulk sabjis" with V143, tests.
- `ddb8daa` design system and shell: `components/ds/**`, `globals.css`, `tailwind.config.ts`, Sidebar and phone drawer, theme tokens.
- `323d064` screens, lib and tests: the rest of `frontend/`.
- `83b75b0` docs: DESIGN_SYSTEM v1.9 to v1.13 with snapshots and CHANGELOG, proofs T-217 to T-241, CLAUDE.md, DISPATCH entry.
- `6ed6451` local tooling: `tools/local-*`, Spring DevTools (developmentOnly) in `build.gradle.kts`, `.claude/skills/ux-writing`.
- After staging, `git status --porcelain` was empty. `.DS_Store` and `frontend/.env.local` are ignored and not shipped (`.env*.local` is in `frontend/.gcloudignore`).

## Gate on the committed tree
`git archive HEAD` (6ed6451) into `/tmp/kms-verify`, then `git init && git add -A`:
- frontend: `npm ci` ok; `tsc --noEmit` exit 0; `vitest run` 147 files / 2,010 tests passed; `npm run build` exit 0.
- backend: `./gradlew compileJava compileTestJava` exit 0 (JDK 21). The full `./gradlew test` was run green in the working tree just before, and CI ran it again.

## CI
https://github.com/rajeevkatamaneni/iskcon-kitchen-management/actions/runs/35415072193: **success**. Repository, Backend (Spring Boot), Frontend (Next.js) all success.

## Backup (taken before deploy)
Cloud SQL instance `kms-staging-5325bd0d`, on-demand backup id **`1789784791420`**, 2026-09-19T02:26:31Z, SUCCESSFUL, description "Before release 2026-09-19 (V143 Catering sabjis to Bulk sabjis), commit 6ed6451".

## Deploy
`./deploy.sh iskcon-kms-2026 staging` from a clean tree at `6ed6451` (checked in the same command). Total 10m35s. No error or fail line in the log.

- Before: api `kms-staging-api-00168-zjc` and worker `kms-staging-worker-00150-lnh` `sha256:dd6a9716…7755`; web `kms-staging-web-00156-25b` `sha256:99cb7076…45fb`.
- After, each at 100% traffic:
  - api `kms-staging-api-00169-46s` `sha256:c545e8a32e5cd84656c370a02078e375a9516336f60789ae29266a5c843ed67b`
  - worker `kms-staging-worker-00151-zfx`, same api image
  - web `kms-staging-web-00157-qdb` `sha256:f873ec32f3bc9c5d69904d920f724f7e3a6d394811fab34fb0781ec514fdb38a`

Evidence that it landed, not just the exit code: both digests changed; `/actuator/health` answers `{"status":"UP"}`; `/sign-in` answers 200; the served stylesheet `8f7f823fdbe4aa79.css` has 30 `kms-table` rules, which only the new table rule adds. No ERROR log entries on the three new revisions.

## Migration (API startup log, api-00169)
```
Successfully validated 137 migrations
Current version of schema "public": 142
Migrating schema "public" to version "143 - catering sabjis become bulk sabjis"
DB: V143: renamed 1 "Catering sabjis" categor(y/ies) to "Bulk sabjis"; 0 temple(s) already had a "Bulk sabjis" and were left alone.
Successfully applied 1 migration to schema "public", now at version v143
```

## Not done
- Not tested in a browser on staging. That is Rajeev's test.
- Nothing was removed from `docs/OUTSTANDING_BUILD_LIST.md`.
