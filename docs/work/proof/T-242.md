# T-242 — the hand-made buttons from T-240's list take the standard button look and press; the idle sign-out limit reads from an env var, 480 minutes on this machine only

**Files changed** (all named by the task, plus this proof):
- `frontend/lib/session-timeout.ts` — `DEFAULT_IDLE_LIMIT_MINUTES` (60), `MIN_`/`MAX_IDLE_LIMIT_MINUTES` (5/720), `idleLimitMinutesFrom(raw)`; `IDLE_LIMIT_MS` now reads `process.env.NEXT_PUBLIC_KMS_IDLE_LIMIT_MINUTES` (the literal expression, so Next inlines it). The comment says staging and production keep 60 because the variable is not set there. `WARN_BEFORE_MS` (1 minute) unchanged.
- `frontend/__tests__/session-timeout.test.ts` — the 60-minute test now also asserts the variable is unset in the test run; a new block tests the default (unset, empty, blanks), plain numbers, garbage (`abc`, `8h`, `480m`, `1e3`, `-30`, `12.5`, `0x20`, `Infinity`, `NaN` all give 60), and the clamp (0 and 1 give 5; 100000 gives 720).
- `frontend/.env.local` — added `NEXT_PUBLIC_KMS_IDLE_LIMIT_MINUTES=480`. Gitignored (`git check-ignore -v` → `.gitignore:45:.env.local  frontend/.env.local`). Not in `.env.local.example` (grep count 0). It cannot reach staging: both `frontend/.gcloudignore` and `frontend/.dockerignore` exclude `.env*.local`, so `deploy.sh`'s upload leaves it out.
- Buttons: `app/donations/page.tsx`, `app/library/page.tsx`, `app/tenants/new/page.tsx`, `app/tenants/[id]/page.tsx`, `app/tenants/[id]/edit/page.tsx`, `app/notices/page.tsx`, `app/shopping-list/page.tsx`, `app/recipes/page.tsx`, `app/recipes/[id]/page.tsx`, `app/sign-in/page.tsx`, `app/orders/[id]/page.tsx`, `app/shifts/page.tsx`, `components/PurchaseOrderEditor.tsx`, `components/RecipeForm.tsx`, `components/staff/Ban.tsx`, `components/staff/PayPanel.tsx`.
- `docs/work/proof/T-242.md` — this file.

**What each listed element became.** The rule I used: `Button`/`ButtonLink` (md, 44px) where the neighbours are Button-sized; the `btn` classes where the neighbouring primary is itself hand-made with the `btn` classes, so the pair keeps the same size.
- donations:338 Export CSV → `Button secondary`.
- library:123 Load the books → `Button secondary`.
- tenants/new:401 Cancel → `ButtonLink secondary`.
- tenants/[id]:75 Edit details → `ButtonLink secondary`; :264 Refresh from Meta and :391 Download data export → `Button secondary busy=…`; :512 Cancel → `Button secondary`; :519 Delete temple → `Button danger` (was solid red, now the standard tinted danger).
- tenants/[id]/edit:270 Cancel → `ButtonLink secondary`.
- notices:157 Withdraw → `Button danger sm` (36px, level with the 36px reason box). Also changed "Keep it" next to it, which was bare text, to `Button secondary sm`, so the pair is two buttons of one size. Not on the list.
- shopping-list:663 Add to list → `Button secondary`.
- recipes:153 Glossary → `ButtonLink secondary`.
- recipes/[id]:159 Edit → `ButtonLink secondary`; :166 Restore → `Button secondary`; the header Delete next to it (not on the list, same row) → `Button danger`; :215 Delete recipe → `Button danger` and the Cancel beside it (not on the list) → `Button secondary`; :309 Archive it instead → `Button secondary`; :355 download icon → stays an icon (DESIGN_SYSTEM §6 allows it), takes `btn btn-secondary`.
- sign-in:132 Continue with Google → kept its accent tint and added the base `btn` class only, for the corner and the press. Its comment records the tint as a deliberate choice, and no Button variant gives that look.
- orders/[id]:773 Generate PDF, :774 Print → `btn btn-secondary min-h-touch px-4` to match the hand-made `btn btn-primary` buttons in that row. Edit and Receive delivery in the same row (not on the list) got the same. :1443 Cancel order → `Button danger`.
- shifts:136 Leave waitlist, :143 Join waitlist → `btn btn-secondary min-h-touch px-4 text-sm`, the exact size of the hand-made Sign up button that takes the same slot.
- PurchaseOrderEditor:460 Add line, :515 Add described line → `Button secondary`.
- RecipeForm:350 + Add ingredient → `Button secondary`.
- Ban:170 Don't hire them → `btn btn-secondary min-h-touch px-5`, same as the Hire them anyway button beside it.
- PayPanel:265 Record advance → `Button secondary` with its `w-full sm:w-auto xl:w-full`.
- Left alone, they are not buttons: RecipePeek:105 (the recipe layer's close X), PeriodNav:142 (the prev/next arrows), composer:387 (bold/italic toolbar keys). All three are borderless icon controls in a toolbar or a header, not buttons in the look this change covers.

**Commands run and what came back:**

```
tools/work-lock.sh run verify 'cd frontend && npx tsc --noEmit; echo TSC_EXIT=$?; npx vitest run 2>&1 | tail -30; npx eslint <the 18 changed .ts/.tsx files>; echo ESLINT_EXIT=$?'
TSC_EXIT=0
 ✓ __tests__/session-timeout.test.ts  (16 tests) 5ms
 ...
 Test Files  147 passed (147)
      Tests  2014 passed (2014)
   Start at  20:04:48
   Duration  18.58s (transform 4.86s, setup 7.46s, collect 18.13s, tests 85.75s, environment 31.32s, prepare 5.58s)

ESLINT_EXIT=0
work-lock: 'verify' released; command exited 0
```
(I also ran one `npx tsc --noEmit` outside the lock before that, by mistake: exit 0.)

Dev server restart, as LOCAL-STACK.md says:
```
lsof -ti tcp:3000 | xargs kill; NEXT_PUBLIC_API_URL=http://localhost:8080 nohup npm run dev > ~/Library/Logs/kms-local/frontend.log 2>&1 &
sign-in HTTP 200
  ▲ Next.js 14.2.15
  - Local:        http://localhost:3000
  - Environments: .env.local
```
The 480 is in the served bundle. Fetched every script on the signed-in page and searched them: `layout.js: idleLimitMinutesFrom(\"480\")`.

**Browser check** (local, signed in as the temple admin, 1470px wide, my own tab, closed at the end). DOM geometry and computed style, before → after:
```
/recipes      Glossary   before: h44 w84  fs14 border 1px hairline, no shadow
                         after:  btn h44 w92 fs16 bg rgb(252,253,255) border 1px rgb(107,109,113) / 2px bottom, shadow 0.14
                         New recipe beside it: h44 fs16, same top (32)
/recipes/[id] Edit       before: h44 w57 fs14   after: btn h44 w61 fs16 style E
              Delete     before: h44 w72 fs14 (outline, red text)   after: btn h44 w78 fs16 bg rgb(254,235,233) border 1px rgb(131,74,67)
              Download   before: h44 w44   after: btn h44 w44 style E
/donations    Export CSV before: h44 w107 fs14   after: btn h44 w109 fs16 style E
/shopping-list Add to list before: h44 w102 fs16   after: btn h44 w102 fs16 style E
/orders/[id]  Generate PDF, Print, Receive delivery: btn h44 fs16 style E, top 78, same as Send on WhatsApp (h44, top 78)
```
Heights did not change anywhere measured. The font went from 14px to 16px on the ones that were `text-sm`. That now matches the primary buttons beside them, which were already 16px. Widths grew by 2–8px.

I looked at a screenshot of the order header. It shows the language picker, Generate PDF, Print, Send on WhatsApp and Receive delivery on one line, all the same height, with the secondaries in style E.

Real press on "Receive delivery": a trusted mouse down and up from the extension, with the click stopped before React and transitions off on that element. Read 30ms after mousedown: `[:active=true, "matrix(0.96, 0, 0, 0.96, 0, 1)"]`. After release: `:active=false, transform none, h44`.

**Acceptance criteria:**
1. The listed elements use Button or the `btn` classes with the right variant, and behaviour and labels are unchanged. Done for 28 of the 31 lines on T-240's list (T-240's own count said 29). The other 3 are icon toolbar controls, left alone with the reason above. Measured on the screens listed; the rest were checked only by tsc, vitest and reading the code.
2. Design-system tests green: 147/147 files, 2014 tests.
3. The idle limit reads `NEXT_PUBLIC_KMS_IDLE_LIMIT_MINUTES`. It falls back to 60 when unset or invalid and is clamped to 5–720. The 1-minute warning is unchanged. Unit-tested. The value is 480 only in the gitignored `.env.local`, and the served bundle shows 480.

**Not done / needs Rajeev:**
- Not verified in the browser, because each needs a state or role I did not set up: the tenant pages (super admin), notices Withdraw/Keep it, shifts waitlist buttons, Ban, PayPanel, RecipeForm, PurchaseOrderEditor, the recipe delete-confirm pair, Cancel order, and the sign-in Google button (I was signed in and did not sign out). The library header button did not render in the state I loaded.
- I did not watch the local 480-minute sign-out happen. Only the bundle value and the unit tests were checked.
- The two danger buttons that were solid red (Delete temple, Delete recipe, Cancel order) are now the standard tinted danger. That is the design system's danger, but it looks quieter than before.
- The two dashed "add" tiles (planner:474, DayView:74) found by the wider scan are tiles, not buttons, and are left alone.
- Checked at desktop width only. Phone width was not checked.

## Follow-up: the 16 elements T-240's scan missed (coordinator, same approval)

**Files changed in this pass** (new): `app/inventory/[id]/page.tsx`, `app/my-donations/page.tsx`, `app/my-shifts/page.tsx`, `app/profile/page.tsx`, `app/staff-schedule/page.tsx`, `components/IngredientRequestForm.tsx`. Changed again: `app/library/page.tsx`, `app/recipes/page.tsx`, `app/sign-in/page.tsx`, `app/tenants/[id]/page.tsx`, `components/RecipeForm.tsx`.

I found them with a regex over every .tsx: `<button|Link|a>` whose className has a border or tinted fill, a min-height and rounding, but no `btn`. Arrow functions in attributes are allowed for, which T-240's scan did not do.
- inventory/[id]:420 Adjust stock / Record what's on the shelf → `Button secondary`.
- library:191 the row's icon-only "take out of the library" → keeps its icon and 44px square, takes `btn btn-secondary`.
- my-donations:107 Download receipt → `Button secondary`.
- my-shifts:124 Release my spot and :172 Leave waitlist → `Button secondary`.
- profile:389 Cancel (leave withdraw confirm) → `btn btn-secondary`, sized like the `Button` beside it. It stays a plain `<button>` because it takes a ref for focus and `Button` does not forward refs. The code has a comment saying so.
- recipes:318 the "+" add-to-my-recipes on library rows → keeps its glyph and 44px column, takes `btn btn-secondary`.
- sign-in:365 Use a different number → `btn btn-secondary min-h-touch w-full px-6`, the same shape as the hand-made Sign in primary above it.
- staff-schedule:207 ← Prev and :209 Next → → `Button secondary`.
- tenants/[id]:115 the outlined Delete temple that opens the confirm → `Button danger`.
- IngredientRequestForm:361 and :420 Remove, and RecipeForm:345 Remove → `btn btn-secondary min-h-touch px-3 text-sm`. This is the old size, so the tight form grids do not move.
- IngredientRequestForm:371 + Add ingredient and :430 + Add dish → `Button secondary`.
- Left alone: planner:474 and DayView:74, the dashed "add a meal" tiles. They are tiles in the meal grid, not buttons.

**Commands run:**
```
tools/work-lock.sh run verify 'cd frontend && npx tsc --noEmit; echo TSC_EXIT=$?; npx vitest run 2>&1 | tail -8; ...'
TSC_EXIT=0
 Test Files  147 passed (147)
      Tests  2014 passed (2014)
   Start at  20:15:23
   Duration  20.16s (transform 5.27s, setup 6.50s, collect 23.20s, tests 91.60s, environment 32.73s, prepare 5.39s)
work-lock: 'verify' released; command exited 0
```
That run's eslint step got an empty file list because of a path mistake. I re-ran it on all 24 changed .ts/.tsx files, listed explicitly:
```
tools/work-lock.sh run verify "cd frontend && npx eslint <24 files>; echo ESLINT_EXIT=$?"
ESLINT_EXIT=0
work-lock: 'verify' released; command exited 0
```

**Measured, before → after** (local, temple admin, 1470px wide, DOM geometry and computed style, my own tab, closed after):
```
/inventory/[id]  Record what's on the shelf  before: h44 w219 fs16 border 1px rgb(206,207,208), no btn
                                             after:  btn h44 w213 fs16 border 1px rgb(107,109,113)/2px bottom, shadow 0.14
/staff-schedule  ← Prev   before: h44 w75 fs16 border 1px rgb(223,224,225)   after: btn h44 w83 fs16 style E, top 130
                 Next →   before: h44 w76 fs16                              after: btn h44 w85 fs16 style E, top 130
```
Height is unchanged. Prev/Next grew 8–9px wider (px-3 → px-4).

Not measured, because they need a state or role I did not set up: my-shifts and my-donations (no rows for this account), the library and recipes row icons (nothing loaded or no library rows), profile's withdraw confirm, sign-in (I was signed in), the tenant page (super admin), and the IngredientRequestForm and RecipeForm rows. These are checked by tsc, the full vitest run and reading the code only.

After this pass the wider scan finds only the two dashed tiles.
