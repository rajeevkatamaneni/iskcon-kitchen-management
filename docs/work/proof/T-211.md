# T-211 — TRACEABILITY.md section 1 brought up to 2026-09-14

**Files changed:**
- `docs/uat/TRACEABILITY.md`, section 1 only. Inside the contract.
- `docs/work/proof/T-211.md`, this file. Inside the contract.

Nothing else was opened for writing. The other paths in `git diff --stat` below belong to T-200 and
the other Phase B builders, not to this task.

## What was done

- **Missing story rows added.** 23 stories that were built but had no row: E2-S8 to E2-S17, E3-S8,
  E4-S9 to E4-S14, E6-S10 to E6-S14, E7-S10, E9-S1 and E9-S2. There is also an E12 row saying it is
  a design with nothing built.
- **Short notes added to seven existing rows** (E1-S5, E3-S6, E4-S4, E4-S7, E4-S8, E5-S9, E6-S1).
  Each says where the named script was written before a later change and now describes the old
  behaviour. Nothing else in those rows changed.
- **Work with no story or task id, 2026-08-20 to 2026-09-06**, got rows keyed by commit hash or by
  the 2026-08-21 brief's group. That covers the kit, the focus screen, themes, hints in an "i",
  Reuse a plan, the temple clock, the Kannada fix, the review of 2026-08-23 and the deploy pipeline.
- **Every task T-000 to T-216** has a row keyed by task id, grouped by area, naming the story it
  changes. That includes Phase A (T-195 to T-199), its fixes (T-213 to T-215) and Phase B (T-200 to
  T-210, T-212, T-216). Stopped tasks, dropped ids, folded ids and ids never used are named in two
  rows at the end.
- **T-200 is described as briefed:** "the purchase order PDF goes with the vendor's WhatsApp
  message", plus the App ID box its proof title names.
- **The "This table is behind" note is removed**, because every story and task now has a row. In
  its place are three short paragraphs:
  - what "complete" covers, and the one class left out: commits from 08-20 to 09-06 that only fixed
    a defect in a story already listed;
  - that the old "every surface is covered" sentence no longer holds for rows added since;
  - one line saying the table had no pattern for task-keyed rows, and why they were added.

**A judgement call to check.** The table's only precedent for work with no story is appending a
decision to its story's row, as with D13 on E1-S15 or BL-9 on E11-S2. I did not stretch that to
about 200 tasks. I used task-id rows instead, as the brief allows when there is no pattern. The
table says so in one line.

**How coverage was judged.** A UAT script counts as covering a task only when a step in it touches
the change. For every UAT file I checked the date of its last edit (`git log -1`). Since 2026-09-05
only four commits have edited UAT scripts:
- `ad509f7`: the error codes;
- `02b0b88`: the sattvic withdrawal;
- `9458969`: recurring donations;
- `dd0d765`: T-134 and T-153.

So most task rows say *No UAT test* or *No UAT step*, and name the script that predates the change.
Every step number cited was checked with grep: UAT-005 step 5, UAT-030 step 7, UAT-032 steps 1–2 and
32–35, UAT-039 step 10, UAT-043 step 4, UAT-047 step 16, UAT-064 step 17, UAT-066 step 21 and UAT-074
steps 33–34.

## Commands run and what came back

**UAT references in section 1, checked against `ls docs/uat`:**

```
$ awk '/^## 1\./{f=1} /^## 2\./{f=0} f' docs/uat/TRACEABILITY.md > $S/s1.txt
$ grep -oE 'UAT-[0-9]{3}' $S/s1.txt | sort -u > $S/refs.txt; wc -l < $S/refs.txt
      85
$ ls docs/uat | grep -oE '^UAT-[0-9]{3}' | sort -u > $S/files.txt
$ comm -23 $S/refs.txt $S/files.txt
UAT-054
$ grep -n "UAT-054" docs/uat/TRACEABILITY.md; git show HEAD:docs/uat/TRACEABILITY.md | grep -n "UAT-054"
109:| ~~E7-S1~~ | ~~Public temple donation page~~ — **withdrawn 2026-08-29** with unauthenticated giving; UAT-054 was deleted with it | *No test — the story no longer exists* |
87:| ~~E7-S1~~ | ~~Public temple donation page~~ — **withdrawn 2026-08-29** with unauthenticated giving; UAT-054 was deleted with it | *No test — the story no longer exists* |
```

The one reference with no file is UAT-054. It sits in the E7-S1 row, which is unchanged from HEAD
and says the file was deleted. It records the deletion rather than pointing at a test. The other 84
references all match a file.

**Sections 2 to 4 byte-identical to HEAD:**

```
$ diff <(git show HEAD:docs/uat/TRACEABILITY.md | sed -n '/^## 2\./,$p') <(sed -n '/^## 2\./,$p' docs/uat/TRACEABILITY.md) && echo "IDENTICAL"
IDENTICAL
$ cmp <(git show HEAD:docs/uat/TRACEABILITY.md | sed -n '/^## 2\./,$p') <(sed -n '/^## 2\./,$p' docs/uat/TRACEABILITY.md) && echo "sections 2-4 byte-identical"
sections 2-4 byte-identical
$ diff <(git show HEAD:docs/uat/TRACEABILITY.md | sed -n '1,/^## 1\./p') <(sed -n '1,/^## 1\./p' docs/uat/TRACEABILITY.md) && echo "IDENTICAL"
IDENTICAL
```

(The last check covers the header above section 1, which is also unchanged.)

**`git diff --stat`, my file among the tree's changes:**

```
$ git diff --stat
 ...(46 backend files and 44 frontend files from T-200 and the other Phase B builders)...
 docs/uat/TRACEABILITY.md                           | 124 +++++++++-
 docs/work/AFTER-UAT.md                             |  22 ++
 docs/work/DISPATCH.md                              | 174 ++++++++++++--
 docs/work/NEXT-SESSION.md                          |   2 +
 ...
 92 files changed, 4225 insertions(+), 425 deletions(-)
$ git status --short docs/uat docs/work/proof/T-211.md
 M docs/uat/TRACEABILITY.md
```

Of the changed files, only `docs/uat/TRACEABILITY.md` is mine. `AFTER-UAT.md`, `DISPATCH.md` and
`NEXT-SESSION.md` were not touched by this task. This proof file is untracked, so it does not show in
`--stat`.

**Every task id and story id has a row:**

```
$ for t in $(ls docs/work/proof | grep -oE '^T-[0-9]{3}[ab]?' | sort -u) $(grep -oE '^#{3,4} T-[0-9]{3}[ab]?' docs/work/DISPATCH.md | grep -oE 'T-[0-9]{3}[ab]?' | sort -u); do grep -qE "$t([^0-9ab]|$)" $S/s1.txt || echo "MISSING $t"; done | sort -u; echo "(end)"
(end)
$ for s in $(grep -hoE '^#{2,3} (~~)?E[0-9]+-S[0-9]+' docs/stories/*.md | grep -oE 'E[0-9]+-S[0-9]+' | sort -u); do grep -qE "\| (~~)?$s(~~)? \|" $S/s1.txt || echo "MISSING $s"; done; echo "(end)"
(end)
```

The first run of the task-id check reported T-161 to T-166, T-169 and T-171 as missing. They were
written as ranges ("T-160 to T-167"), which grep cannot see. They are now spelled out, and the rerun
above is clean.

**No suites run.** This task changes one document and no code. Per the brief, the backend and
frontend suites were not run and the work lock was not taken, since T-200 needs it. The work
manager's run on the merged tree covers the tree.

## Acceptance criteria

1. **Every story or task shipped since 2026-08-20, including Phase A and Phase B, has a row or is
   named as automated only.** Met. Both loops above come back empty.
   - Ids with no proof file were traced through DISPATCH.md by grep:
     - T-020 and T-073 went into T-110;
     - T-063 is in T-097's proof, and T-065 in T-064's;
     - T-067, T-074 and T-123 were dropped;
     - T-101 was built as T-106;
     - T-109 was dissolved by T-124;
     - T-133 was merged into T-132, T-136 into T-135, T-138 into T-137, and T-182 into T-200;
     - T-077 and T-143 are in T-108's proof.
   - T-011, T-181 and T-183 appear nowhere in `docs/work/` and are listed as never used.
2. **No row points at a UAT file that does not exist.** Met, with UAT-054 explained above.
3. **Only TRACEABILITY.md among my files, and sections 2 to 4 byte-identical.** Met. See the diff,
   cmp and status output above.

## Not done / needs Rajeev

- No browser test, because this is a document.
- Two rows depend on things I could not fully confirm from the allowed sources:
  - `5f3d177` is described from its commit subject and the file it changed (the Settings page). I
    did not confirm which error code it serves.
  - The "Review of 2026-08-23" row is described from commit subjects. `OUTSTANDING_BUILD_LIST.md` is
    not in this task's source list, so I did not read it.
- T-200 is still being finished. Its row describes it in the brief's words, which hold either way.

## After-UAT lines

- Section 2 still shows G3 (no screen for festival occasions) as open, though T-004 built one. G6
  (staff cannot see their own schedule) looks answered by T-006, and G8 (no manual purchase order)
  by T-026. Section 2 was out of this task's contract.
- UAT-005 step 5 expects kitchen staff to see My shifts. T-031 removed it, so the step is now wrong.
- UAT-035 still has testers press a per-dish Cook. E4-S10 replaced that, and D-27 rebuilt meals
  since.
- UAT-077 says on-time is scored per order. T-124 made it per item.
- UAT-032 steps 32–35 test *Duplicate last week*, which was written before Reuse a plan.
- UAT-032, 035, 048, 062, 078 and 086 all describe meals as they were before the D-27 rebuild.
- No UAT script covers WhatsApp settings or templates (eleven tasks).
- No UAT script covers voiding bills, payments or gifts, attendance, leave, staff pay, the notice
  board, or the check at hire.
- E2-S8 says "UAT to be written"; none was.
- The Commandment 6 sentence under the table ("every surface is covered") was true to 2026-08-20. I
  qualified it in place rather than deleting it.
