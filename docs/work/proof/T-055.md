# T-055 — The sattvic removal carried into the documents: two locked docs bumped, three items withdrawn, fourteen amended

Wave 4e-1. Ruling: **D-20** (Rajeev, 2026-09-08 — *"Approved, mark them withdrawn and bump the locked
docs"*), carrying **D-18** into the documentation. Documentation only; no product code was touched.

---

## Files changed — every one, and each confirmed inside the contract

**Locked documents (root amended, header lines updated) — contract line 1 and 2**

| Path | In contract | What changed |
|---|---|---|
| `docs/REQUIREMENTS.md` | yes | v1.4 → v1.5; §3.1 live promise amended, §5 record annotated |
| `docs/SYSTEM_DESIGN.md` | yes | v1.3 → v1.4; §5 audit-log list amended |

**Snapshots created (new files) — contract lines 3 and 4**

| Path | In contract | Verified |
|---|---|---|
| `docs/versions/REQUIREMENTS_v1.5.md` | yes (new) | byte-identical to the amended root |
| `docs/versions/SYSTEM_DESIGN_v1.4.md` | yes (new) | byte-identical to the amended root |

**Story files — contract lines 5–10**

| Path | In contract | What changed |
|---|---|---|
| `docs/stories/EPIC-2-recipe-management.md` | yes | **E2-S4 marked WITHDRAWN, text intact**; E2-S1 and E2-S7 amended; epic goal amended |
| `docs/stories/EPIC-1-platform-foundation.md` | yes | E1-S7 verified-by struck; the audit-trail prose at :416 amended |
| `docs/stories/EPIC-2-recipe-library-DESIGN.md` | yes | file-top amendment banner; §4 step 3 and §Q4 struck with a replacement; §8 criterion annotated |
| `docs/stories/EPIC-3-inventory-management.md` | yes | D10's `MANAGE_SATTVIC_POLICY` → `MANAGE_DIETARY_POLICY` (**the line the brief assigned me from T-056**) |
| `docs/stories/EPIC-4-meal-planning-calendar.md` | yes | E4-S6 assumption annotated (words kept); the job card's "sattvic warnings" amended |
| `docs/stories/EPIC-5-ordering-vendors.md` | yes | E5-S1's sattvic guard requirement withdrawn |

**UAT files — contract line 11 (`docs/uat/**`)**

| Path | What changed |
|---|---|
| `docs/uat/UAT-014-the-prohibited-flag-is-admin-only.md` | **WITHDRAWN banner, script text intact** |
| `docs/uat/UAT-018-sattvic-block-and-override.md` | **WITHDRAWN banner, script text intact** |
| `docs/uat/README.md` | both rows struck and marked *do not run* |
| `docs/uat/TRACEABILITY.md` | E2-S4 row struck; E2-S1 row repointed; **new gap G12** |
| `docs/uat/UAT-002-bring-a-temple-onto-the-platform.md` | provisioning no longer seeds ingredients |
| `docs/uat/UAT-007-first-sign-in-as-temple-admin.md` | step 7 now expects an **empty** `/ingredients` |
| `docs/uat/UAT-011-the-temple-audit-log.md` | step 8 repointed off the withdrawn UAT-018 |
| `docs/uat/UAT-013-build-the-ingredient-list.md` | premise, steps 1–3 and one pass criterion |
| `docs/uat/UAT-015-write-a-recipe.md` | *Sattvic override reason* off the form; the tester note withdrawn |
| `docs/uat/UAT-016-find-a-recipe.md` | *Sattvic override* badge off the premise, step 8 and the criterion |
| `docs/uat/UAT-038-the-shopping-list.md` | the "prohibited never appear" promise and its criterion withdrawn |
| `docs/uat/UAT-073-adding-an-ingredient-and-adding-stock.md` | steps 4 and 25 name the Ekadashi tick |

**Proof — contract line 12:** `docs/work/proof/T-055.md` (this file).

**Nothing outside the contract was touched.** `git status --porcelain docs/CHANGELOG.md
docs/WORK_QUEUE.md docs/DESIGN_SYSTEM.md` returns **empty**. The backend, frontend and `infra/`
changes in the working tree belong to the other builders in this sub-wave; the six untracked
`docs/versions/DESIGN_SYSTEM_v*.md` files were there before I started (mtime `21:41:39`, mine
`22:15`) and **I did not create them and did not edit them**.

---

## Commands run and what came back

Doc-only change: no test in the tree reads these files, so no suite covers them and none was run —
see *Not done* below. What was run is the verification the change actually admits.

### 1. The three artefacts land together, and the snapshots match the amended roots

```
$ diff docs/REQUIREMENTS.md docs/versions/REQUIREMENTS_v1.5.md && echo "REQ v1.5 snapshot == root"
$ diff docs/SYSTEM_DESIGN.md docs/versions/SYSTEM_DESIGN_v1.4.md && echo "SD v1.4 snapshot == root"
REQ v1.5 snapshot == root
SD v1.4 snapshot == root

$ head -6 docs/versions/REQUIREMENTS_v1.5.md | sed -n '3,5p'
**Status:** LOCKED — v1.5, amended 2026-09-08.
**Version:** 1.5
**Approved:** 1.0 on 2026-08-03 by Rajeev; v1.1 on 2026-08-20 by Rajeev; v1.2 on 2026-08-30 by Rajeev; v1.3 on 2026-08-31 by Rajeev; v1.4 on 2026-09-04 by Rajeev; v1.5 on 2026-09-08 by Rajeev

$ head -6 docs/versions/SYSTEM_DESIGN_v1.4.md | sed -n '3,5p'
**Status:** LOCKED — v1.4, amended 2026-09-08. Stage 2 complete.
**Version:** 1.4
**Approved:** 2026-08-03 by Rajeev (v1.1 §10 revision approved 2026-08-11; v1.2 on 2026-08-30; v1.3 on 2026-08-31; v1.4 on 2026-09-08)
```

The prior pair proved the convention before I edited anything — `docs/versions/REQUIREMENTS_v1.4.md`
and `SYSTEM_DESIGN_v1.3.md` were byte-identical to their roots (`REQ IDENTICAL` / `SD IDENTICAL`), so
"snapshot == root at that version" is the repo's actual rule and not my reading of it.

### 2. No live claim survives — checked by reading, not by grep

The removal-wave rule in `docs/work/README.md` is that a criterion must name **live references**, not
text matches, because a repo carries its own history in prose. So the grep is a *finding aid* and the
verdict below it is by inspection:

```
$ grep -rni "sattvic\|prohibited" docs/stories/EPIC-{1,2,3,4,5}*.md docs/REQUIREMENTS.md docs/SYSTEM_DESIGN.md docs/uat/*.md | grep -vi ekadashi | grep -vi "withdrawn\|amended\|annotat\|D-18\|~~\|no longer\|deleted"
docs/stories/EPIC-2-recipe-library-DESIGN.md:9    (inside my own amendment banner)
docs/stories/EPIC-2-recipe-library-DESIGN.md:130  §3 — the 46,337-line grep finding. A RECORD of an investigation.
docs/stories/EPIC-2-recipe-library-DESIGN.md:812  §Q4 — a measurement of the category map. A RECORD.
docs/stories/EPIC-2-recipe-library-DESIGN.md:818  inside the ~~struck~~ §Q4 block
docs/stories/EPIC-2-recipe-library-DESIGN.md:865  §11 — the day-one proving plan. A RECORD.
docs/stories/EPIC-2-recipe-management.md:90       inside my own WITHDRAWN header
docs/stories/EPIC-2-recipe-management.md:106,108,121  the E2-S4 body — kept verbatim, by ruling
docs/REQUIREMENTS.md:84                           inside my own amendment
docs/REQUIREMENTS.md:256                          §5 "Resolved this round" — kept verbatim, by ruling
docs/uat/UAT-014..., docs/uat/UAT-018...          the two withdrawn scripts — kept verbatim, by ruling
```

**Every remaining occurrence is a historical record, a withdrawal notice, or my own amendment text.
No live statement in any file I own claims the product enforces a sattvic rule.**

### 3. Relative links still resolve (the withdrawals struck link text; nothing was renamed)

```
$ python3 <check every non-http ]( link in the 23 changed/new docs>
files checked: 23 | broken relative links: 0
```

### 4. D-22's six recovered snapshots — checked, not touched

The changelog draft has to state facts about these, so I verified them read-only:

```
$ for f in docs/versions/DESIGN_SYSTEM_v*.md; do sed -n '3p' "$f"; done
DESIGN_SYSTEM_v1.0.md  **Status:** v1.0 — established 2026-08-04 ...
DESIGN_SYSTEM_v1.1.md  **Status:** v1.1 — palette revised 2026-08-10 ...
DESIGN_SYSTEM_v1.3.md  **Status:** v1.3 — contrast made a floor ... 2026-08-20 ...
DESIGN_SYSTEM_v1.4.md  **Status:** v1.4 — the accent darkened to clear AA ... 2026-08-21 ...
DESIGN_SYSTEM_v1.5.md  **Status:** v1.5 — colour became the temple's choice ... 2026-08-28 ...
DESIGN_SYSTEM_v1.6.md  **Status:** v1.6 — what lifts under the pointer ... 2026-08-30 ...

$ diff -q docs/versions/DESIGN_SYSTEM_v1.6.md docs/DESIGN_SYSTEM.md && echo "byte-identical (as D-22 states)"
byte-identical (as D-22 states)
```

Each declares its own version in its own first Status line, so each self-verifies; **there is no
v1.2 and none was invented** — the sequence runs v1.1 → v1.3. `ls -lT` shows all six at
`Sep 7 21:41:39`, before this task began.

### 5. Forbidden paths

```
$ git status --porcelain docs/CHANGELOG.md docs/WORK_QUEUE.md docs/DESIGN_SYSTEM.md
                                                (no output — all three unmodified)
$ grep -n -i "sattvic" docs/DESIGN_SYSTEM.md
119:| `danger` | `#F7E7E3` | `#9B2C1F` | Overdue invoice, rejected delivery, sattvic violation |
```

That cell is **deliberately left standing** — out of scope by contract, and its document waits on
whatever resolves v1.6's own history.

---

## The two locked passages, quoted before and after

### `REQUIREMENTS.md` — the LIVE PROMISE (was `:68-70`) — **amended**

**Before:**
```
**Sattvic Ingredient Enforcement — Decided**

- Ingredient master data carries a compliance flag for prohibited items (onion, garlic, mushroom, eggs, and other non-sattvic items).
- **Decision:** prohibited ingredients hard-block recipe save and purchase order submission. Override is available only to Temple Admin, and every override is written to the audit trail (who, when, why).
- Rationale: an error here is a religious failure, not a cosmetic bug.
```

**After:**
```
**Sattvic Ingredient Enforcement — withdrawn 2026-09-08 (decision D-18)**

- **The product does not enforce a sattvic rule.** There is no compliance flag for prohibited items,
  no hard block on recipe save or purchase-order submission, and no admin override. The column, the
  admin toggle, the API, every enforcement site and the audit action they wrote were deleted on
  2026-09-08 under decision D-18.
- **Ekadashi is now the only dietary restriction the product enforces**, and it is a warning rather
  than a block: the planner flags a grain or bean dish on a fast day and the temple decides (Section
  3.1). An ingredient is marked Ekadashi-prohibited by hand, by a Temple Admin.
- **Why it went.** The flag only ever guarded rows that existed because of it — provisioning inserted
  onion, garlic, mushroom and egg into every temple's catalogue solely so that it could tick them
  forbidden. That seed of eleven ingredients was deleted in the same ruling, so a **new temple now
  starts with an empty catalogue** and nothing arrives pre-classified; the Recipes page carries a
  warning box saying so, because seven flagged staples among sixty unflagged ones looks like
  knowledge and is not. A temple kitchen does not stock the four, so they arrive by no other path.
- **Amended under sign-off.** Rajeev, 2026-09-08: *"Approved, mark them withdrawn and bump the locked
  docs"* — recorded as decision D-20, which authorises the sattvic passages of this document and
  nothing else in it. D-18 holds the full reasoning and the two consequences that were accepted
  rather than missed.

> **Why this passage is rewritten and the one in Section 5 is not.** The rule applied throughout this
> amendment: **a document's live promises are amended, and its records of past decisions are
> annotated.** This section promised what the product does, so subtracting from it is the fix.
> Section 5's *"Resolved this round"* line records what a past round of open questions resolved;
> rewriting that would delete the very history the withdrawal exists to preserve, so it keeps its
> words and carries a dated pointer instead.
```

### `REQUIREMENTS.md` — the HISTORICAL RECORD (was `:229`) — **annotated, words kept**

The `-`/`+` diff is one-sided, which is the point: **nothing was removed.**

```
@@ -227,4 +248,10 @@
 None outstanding. Section 5 is closed as of this round.

+> **Annotation, 2026-09-08 (decision D-18).** The first item below — sattvic enforcement — was
+> **withdrawn** on 2026-09-08; see the Sattvic Ingredient Enforcement heading in Section 3.1 for what
+> the product does now, which is Ekadashi and nothing else. **The sentence itself is left exactly as
+> written**, because it is a record of what a past round of open questions resolved, not a promise
+> about the product, and a record that is edited to match a later decision stops being a record.
+
 **Resolved this round:** Sattvic enforcement (hard-block + admin override), Vaishnava calendar source ...
```

The rule that makes the two treatments consistent is **stated in the file itself**, in the block
quote at the end of the §3.1 amendment above, so a later reader who sees one passage rewritten and
the other left alone finds the reason where they are standing rather than inferring carelessness.

### `SYSTEM_DESIGN.md` — the audit-log list (was `:110`) — **amended**

```
-- **Audit log:** ... Written by the shared kernel for: financial records, inventory adjustments, sattvic overrides, calendar date overrides, role changes. No deletes; ...
+- **Audit log:** ... Written by the shared kernel for: financial records, inventory adjustments, calendar date overrides, role changes. No deletes; ...
+  - *Amended 2026-09-08 (decision D-18):* **sattvic overrides** were the fifth item on that list and are gone from it, because the act no longer exists — the sattvic-prohibited flag, its hard block and its admin override were deleted from the product, and with them the `RECIPE_SATTVIC_OVERRIDDEN` action this kernel used to write. **Ekadashi is now the only dietary restriction the product enforces**, and it warns at planning time rather than blocking a save, so it produces no override act to record. Nothing else about the audit log changes: it is still append-only, still written by the shared kernel, and **the rows already written are untouched** — an old `RECIPE_SATTVIC_OVERRIDDEN` row is a true record of something that happened and stays readable in the temple audit log. This is a live description of what the kernel writes, so it is amended; historical rows and past decision records elsewhere are annotated instead. Amended under Rajeev's sign-off of 2026-09-08 (decision D-20), which authorises the sattvic passages of this document and nothing else in it.
```

The distinction lands here too, and it had to: the **list** is a live description and was cut, while
the **rows already in the table** are records and are explicitly protected in the same sentence. On
an append-only table that is not a nicety — it is the difference between an amendment and a deletion.

---

## The three withdrawn items — marked, text intact, each citing D-18

### `E2-S4` in `docs/stories/EPIC-2-recipe-management.md`

Heading struck, a status block inserted **above** the original text, and every line of the story
(`**Verified by:**` through `- [ ] Removing the prohibited ingredient clears the badge on next save.`)
left byte-for-byte as it was:

```
-## E2-S4 — Sattvic enforcement on recipes
+## ~~E2-S4 — Sattvic enforcement on recipes~~ · WITHDRAWN 2026-09-08
+
+**Status:** Withdrawn under **decision D-18**, which deleted the sattvic-prohibited flag from the
+product entirely — the column, the toggle, the API, the hard block on recipe save and purchase-order
+submission, the admin override and the audit action it wrote. **Do not build this story and do not
+run its scripts.** The two UAT scripts that verify it, [UAT-014](...) and [UAT-018](...), are
+withdrawn with it.
+
+**The text below is left exactly as written.** It is not a plan; it is the record of what this temple
+once decided, and deleting it would lose the fact that the rule existed and was dropped on purpose.
+Other documents cite E2-S4 by id, so the id stays too. The reasoning, and the two consequences that
+were **accepted rather than missed** — an imported recipe naming garlic now saves cleanly, and a new
+temple enforces nothing on a fast day until an admin flags things by hand — are in D-18.
+
+**What is true now:** **Ekadashi is the only dietary restriction the product enforces.** ...
+`MANAGE_DIETARY_POLICY` (D-21). Withdrawn under Rajeev's sign-off of 2026-09-08 (D-20).
+
+---
+
 **Verified by:** [UAT-014](../uat/UAT-014-the-prohibited-flag-is-admin-only.md), [UAT-018](../uat/UAT-018-sattvic-block-and-override.md)
```

The shape follows the repo's own precedent for a withdrawn story, `E7-S1` in EPIC-7 — struck heading,
`· WITHDRAWN <date>`, a **Status:** paragraph, and the id kept because other documents cite it.

### `UAT-014` and `UAT-018`

Both got the identical banner immediately under a struck title, and **not one line of either script
was changed**. `git diff --stat` shows `21 +-` on each file: 20 lines of banner, 1 line of title.

```
-# UAT-014: The prohibited flag is admin-only
+# ~~UAT-014: The prohibited flag is admin-only~~ · WITHDRAWN 2026-09-08
+
+> ## ⛔ WITHDRAWN 2026-09-08 — do not run this script
+>
+> **The feature this script tests no longer exists.** Decision **D-18** deleted the sattvic-prohibited
+> flag from the product entirely: the column, the admin toggle, the API, the hard block on recipe save
+> and purchase-order submission, the admin override, the audit action it wrote, and the provisioning
+> seed of the eleven ingredients it was there to flag. The story behind it, **E2-S4**, is withdrawn
+> with it.
+>
+> **What is true now:** a new temple starts with an **empty** ingredient catalogue, and **Ekadashi is
+> the only dietary restriction the product enforces**. Ekadashi *warns* at planning time — it does not
+> block a save — and a Temple Admin sets the flag by hand, under `MANAGE_DIETARY_POLICY` (D-21). The
+> Recipes page carries a warning box saying that imported ingredients arrive unflagged.
+>
+> **The script below is left exactly as written, and that is deliberate.** A UAT script is a record of
+> what was tested, and deleting it would lose the fact that this temple once had the rule and chose to
+> drop it. The banner is here to stop somebody running it for a feature removed on purpose. Marked
+> under Rajeev's sign-off of 2026-09-08 (decision **D-20**).
```

The index rows follow, so a tester never opens the file to find out:

```
-| [UAT-014](...) · [#77](...) | The prohibited flag is admin-only | Staff + admin | E2-S1, E2-S4 |
+| ~~[UAT-014](...)~~ · [#77](...) | ~~The prohibited flag is admin-only~~ — **WITHDRAWN 2026-09-08 (D-18), do not run** | — | ~~E2-S4~~ |
-| [UAT-018](...) · [#81](...) | Keeping the kitchen sattvic | Staff + admin | E2-S4 |
+| ~~[UAT-018](...)~~ · [#81](...) | ~~Keeping the kitchen sattvic~~ — **WITHDRAWN 2026-09-08 (D-18), do not run** | — | ~~E2-S4~~ |
```

---

## The line the brief assigned me from T-056

`docs/stories/EPIC-3-inventory-management.md:481`, made here because T-056 is forbidden this file:

```
-`RolePermissions` already uses for `APPROVE_LARGE_STOCK_ADJUSTMENT` and `MANAGE_SATTVIC_POLICY`,
+`RolePermissions` already uses for `APPROVE_LARGE_STOCK_ADJUSTMENT` and `MANAGE_DIETARY_POLICY`,
+*(renamed from `MANAGE_SATTVIC_POLICY` on 2026-09-08 under D-21: the sattvic flag was deleted by D-18
+and the permission now gates the Ekadashi flag, **the only dietary restriction the product
+enforces**)*
```

`git diff` on the working tree shows T-056's own half already in place —
`backend/.../auth/Permission.java` and `RolePermissions.java` are modified by that builder. I did not
open either; the constant name came from the brief and from **D-21**.

---

## Acceptance criteria, and how each was verified

**1. No live statement claims the product enforces a sattvic rule.** Verified by inspection over the
grep in §2 above. Every survivor is one of three things and each was read individually: the two
withdrawn scripts (ruling: text intact), the E2-S4 body and `REQUIREMENTS.md:256` (ruling: records
kept verbatim), and the DESIGN doc's three investigation findings — a grep over 46,337 real
ingredient lines, a measurement of the category keyword map, and a day-one proving plan — which
record work that was actually done and are covered by the file-top banner naming them. A bare text
match cannot make this call, which is why the verdict is stated file by file.

**2. The three withdrawn items are marked, text intact, each citing D-18.** Quoted in full above.
`--stat` corroborates the "intact" claim: UAT-014 and UAT-018 are `21 +-` each — banner and title
only, no deletions from the scripts themselves.

**3. Both locked documents: root amended, headers updated, snapshot created and matching.** Two
`diff` runs returning nothing (§1), plus the `Status:`/`Version:`/`Approved:` lines quoted from the
snapshots. `Last updated:` on each says what changed and points at CHANGELOG.md, matching the
convention v1.4 and v1.3 set.

**4. The changelog draft covers D-20 and D-22 and records the authorisation.** Below, verbatim.

**5. This proof file, with real diffs rather than a summary.** This file.

---

## The changelog entry, verbatim and ready to paste

`docs/CHANGELOG.md` is the release agent's file. The versioning convention requires the root, the
snapshot and the changelog entry to **land together in one commit** — it does not require them to be
typed by the same hand — so the two artefacts I own are written and the third is drafted here.

**Two entries.** They go under the existing `## REQUIREMENTS.md` and `## SYSTEM_DESIGN.md` headings
respectively, each as the new topmost `###` under its heading — above `### v1.4 — 2026-09-04 —
Catering is gone…` and above `### v1.3 — 2026-08-31 — The order list is the shopping list…`.

---

```markdown
### v1.5 — 2026-09-08 — Sattvic enforcement is withdrawn, and Ekadashi is the only dietary rule left (approved by Rajeev)

**The sign-off first, because a locked-document edit whose authorisation is not on the record is
later indistinguishable from one that skipped the rule.** Rajeev, 2026-09-08: *"Approved, mark them
withdrawn and bump the locked docs."* Recorded as decision **D-20**. It authorises the sattvic
passages of `REQUIREMENTS.md` and `SYSTEM_DESIGN.md` and **nothing else in either file**, and it is
the explicit sign-off Commandment 8 requires before a locked document changes.

**§3.1 Sattvic Ingredient Enforcement — withdrawn.** Decision **D-18** deleted the sattvic-prohibited
flag from the product: the column, the admin toggle, the API, the hard block on recipe save and
purchase-order submission, the admin override, and the audit action it wrote. The section no longer
promises any of it. **Ekadashi is now the only dietary restriction the product enforces**, and it is
a *warning* at planning time rather than a block, because a temple does legitimately cook grains for
non-fasting visitors on a fast day. The flag is set by hand by a Temple Admin, under
`MANAGE_DIETARY_POLICY` — renamed from `MANAGE_SATTVIC_POLICY` under **D-21**, since it now guards a
different rule from the one it was named for.

The reason the flag went is worth keeping: it only ever guarded rows that existed **because of it**.
Provisioning inserted onion, garlic, mushroom and egg into every temple's catalogue solely so that it
could tick them forbidden. That seed of eleven ingredients went in the same ruling, so **a new temple
now starts with an empty catalogue**, and the Recipes page carries a warning box saying that imported
ingredients arrive unflagged for Ekadashi. Two consequences were **accepted rather than missed**: an
imported recipe naming garlic now saves cleanly, and a new temple enforces nothing on a fast day
until an admin flags things by hand. Seven flagged staples among sixty unflagged ones looks like
knowledge and is not.

**§5's "Resolved this round" line is annotated and keeps every word — deliberately, and the rule is
worth stating once because it governed the whole amendment: a document's live promises are amended,
and its records of past decisions are annotated.** §3.1 promised what the product does, so
subtracting from it is the fix. §5 records what a past round of open questions resolved; rewriting it
would falsify exactly the history that "withdraw, do not delete" exists to preserve. Subtracting from
a promise is a correction; subtracting from a record is a second, quieter kind of deletion. The
distinction is written into §3.1 itself, so a reader who notices the inconsistency finds the reason
rather than assuming carelessness.

**Carried into the stories and the UAT pack in the same commit** (task T-055): **E2-S4**, **UAT-014**
and **UAT-018** are marked **withdrawn with their text left in place** — a story is a record of what
was decided and a UAT script a record of what was tested, so deleting them would lose the fact that
this temple once had the rule and chose to drop it, while marking them stops somebody running a
script for a feature removed on purpose. Five further story files and nine further UAT files had only
their sattvic assertions amended. One gap opened and is recorded as **G12** in
`docs/uat/TRACEABILITY.md`: UAT-014 also covered the admin-only rule on the dietary flag, and with it
withdrawn nothing now tests who may set the surviving **Ekadashi** flag.

**Not changed, deliberately.** `docs/DESIGN_SYSTEM.md` — its one sattvic mention is an illustrative
table cell, and that document waits on its own history (see the DESIGN_SYSTEM.md entry below).
Earlier `docs/versions/` snapshots, applied Flyway migrations, the build briefs, `docs/reviews/` and
`docs/stories/github-import/` still mention sattvic and are left alone: they record what was true
when they were written. Audit rows already written are untouched — an old `RECIPE_SATTVIC_OVERRIDDEN`
row is a true record of something that happened and stays readable in the temple audit log.

Minor (v1.x): no reversal of an earlier decision beyond D-18 itself, which is already recorded.
Snapshot: `docs/versions/REQUIREMENTS_v1.5.md`
```

---

```markdown
### v1.4 — 2026-09-08 — The sattvic override comes off the audit-log list (approved by Rajeev)

**Following REQUIREMENTS.md v1.5 and under the same sign-off** — Rajeev, 2026-09-08: *"Approved, mark
them withdrawn and bump the locked docs"*, recorded as decision **D-20**, which authorises the
sattvic passages of this document and nothing else in it.

**One change, in §5.** The audit-log bullet listed the acts the shared kernel writes to
`audit_events`: *financial records, inventory adjustments, **sattvic overrides**, calendar date
overrides, role changes.* The third is gone, because the act no longer exists — decision **D-18**
deleted the sattvic-prohibited flag, its hard block and its admin override, and with them the
`RECIPE_SATTVIC_OVERRIDDEN` action. **Ekadashi is now the only dietary restriction the product
enforces**, and it warns at planning time rather than blocking a save, so it produces no override act
to record.

**Nothing else about the audit log moves**, and one thing is protected explicitly: the table is
append-only and **the rows already written are untouched**. An old `RECIPE_SATTVIC_OVERRIDDEN` row is
a true record of something that happened and stays readable in the temple audit log. That is the same
distinction the requirements amendment turns on — the *list* is a live description of what the kernel
writes and was cut; the *rows* are records and were not. On an append-only table it is the difference
between an amendment and a deletion.

Minor (v1.x): no reversal of an earlier decision and no re-review of dependent work.
Snapshot: `docs/versions/SYSTEM_DESIGN_v1.4.md`
```

---

**And under the existing `## DESIGN_SYSTEM.md` heading, as the new topmost `###` — above
`### v1.6 — 2026-08-30`, whose `(PENDING RAJEEV'S SIGN-OFF)` marker D-20 also removes:**

```markdown
### 2026-09-08 — Six missing snapshots recovered from git history, and why there is no v1.2 (decision D-22)

**Ruled by Rajeev, 2026-09-08:** *"reconstruct the missing snapshots from git history."*

`docs/versions/` held snapshots for `REQUIREMENTS`, `SYSTEM_DESIGN` and `TECH_STACK` and **none at all
for `DESIGN_SYSTEM`**, against six declared locked versions. The convention stated in this file's own
preamble — an immutable snapshot per locked version — had never once been applied to that document,
so its locked status was nominal: there was nothing to compare a change against.

**Six snapshots were recovered from named commits** — `DESIGN_SYSTEM_v1.0.md` (`4a83a19`),
`v1.1` (`28cb5af`), `v1.3` (`e6fd51c`), `v1.4` (`50ec057`), `v1.5` (`cbef632`) and `v1.6`
(`ad509f7`). They were extracted verbatim, not written: the document declares its own version in its
first Status line, so each was taken from the commit at which the file itself said it was that
version, and **each self-verifies**. `v1.6` is byte-identical to the current root copy, which tests
the method rather than only the output.

**There is no v1.2, because it never existed as a committed state, and it must not be invented.** The
file's history runs v1.1 (10 August) straight to v1.3 (20 August) — the two edits were committed
together, or v1.2 lived only as a changelog entry. This changelog describes what v1.2 *did*: it added
the `info` family and moved Ekadasi onto it. A snapshot assembled from that description would be a
reconstruction of a document nobody ever approved in that form, which is fabrication wearing the
costume of a record, and worse than the gap — a gap is visibly a gap, while a plausible file is not.
The sentence is here so that the next reader who meets the hole knows it is deliberate and does not
go looking for a lost file.

**v1.6's sign-off, at last.** v1.6 had been sitting above marked *"PENDING RAJEEV'S SIGN-OFF"*, which
would have stacked any later amendment on top of a version he had never approved. He signed it off on
2026-09-08 — *"sign off v1.6"* — so the marker comes off (decision **D-20**).

**The root `DESIGN_SYSTEM.md` is not edited by this entry.** Its one sattvic mention is an
illustrative cell in the `danger` colour row, and it is left for whatever settles that document's own
next version.
```

---

## Not done / needs Rajeev

- **No test was run, and none exists to run.** `grep -rln "docs/REQUIREMENTS\|docs/SYSTEM_DESIGN\|
  docs/uat\|docs/stories" frontend/__tests__ backend/src/test` returns **nothing**: no test in this
  repo reads these files. Holding the verify lock for a suite that cannot observe this change would
  only queue the other builders behind me. Saying so plainly rather than running something green and
  irrelevant.
- **No hand smoke-test — there is no user-facing surface in this task.** It is documentation only.
  The surfaces D-18 *did* change (an empty `/ingredients` on a new temple, the Recipes warning box,
  the ingredient form without the sattvic tick) belong to T-050 and T-051 and are theirs to
  demonstrate. **My amendments assert what those screens now do, so if either builder's smoke test
  contradicts my wording, the wording is wrong and not the screen.**
- **One label I could not verify: the *Ekadashi-prohibited* tick.** `UAT-013` step 2 and `UAT-073`
  steps 4 and 25 now name it where they used to name *Sattvic-prohibited*. `frontend/` is outside my
  contract, so I did not read the component; the field name comes from T-045's record, which says the
  Ekadashi controls *"mirror the sattvic ones exactly … the same checkbox shape on the form."* **If
  the on-screen label differs, these three cells need the real string.** A tester will find it in a
  minute; I preferred flagging it to guessing silently.
- **A new gap, G12, opened by the withdrawal — recorded, not fixed.** UAT-014 tested two things: the
  sattvic rule, and the rule that **only a Temple Admin may change a dietary flag and the change is
  audited.** The second survives, on the Ekadashi flag, and withdrawing UAT-014 leaves it untested —
  UAT-036 tests the guard at planning time, not who may set the flag. Written into
  `docs/uat/TRACEABILITY.md` as **G12** and cited from E2-S1's row. It needs either a short
  replacement script or a step added to UAT-013. **Not a defect; a hole in the test pack, recorded
  so it is not mistaken for coverage.**
- **Three files carry sattvic text and are outside my contract.** Found by the same sweep, reported
  rather than touched: `docs/stories/BUILD-BRIEF-2026-08-20.md`,
  `docs/stories/BUILD-BRIEF-2026-08-21.md`, and `docs/stories/github-import/` (`bodies/e2-s1.md`,
  `e2-s4.md`, `e2-s7.md`, `e4-s6.md`, `e5-s2.md`, and `import.sh`). The build briefs are records of a
  past build and are correctly left alone on the same reasoning as `docs/versions/`. **The GitHub
  mirror is different and someone should decide about it**: `e2-s4.md` is the body of a live GitHub
  issue promising a feature that no longer exists, so the falsehood is published where a reader will
  meet it without this repo's context. `CLAUDE.md` already records that `github-import/` has been
  behind since E1-S12 and is a job of its own — this makes that job slightly more urgent, since at
  minimum issue **#81** and **#77** now describe withdrawn tests.
- **I read `docs/CHANGELOG.md` (read-only) to match its house style and to place the entries
  correctly.** I did not modify it; `git status --porcelain docs/CHANGELOG.md` is empty. Flagged
  because the brief lists it as forbidden, and "forbidden to write" is what I took that to mean —
  drafting an entry that lands cleanly is not possible without seeing the surrounding format.
- **The release agent must paste all three drafted entries**, under three different `##` headings.
  The DESIGN_SYSTEM one carries D-22 and also removes the `(PENDING RAJEEV'S SIGN-OFF)` marker from
  the existing v1.6 heading, which D-20 authorises.
