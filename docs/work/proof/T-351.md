# T-351 — the meal composer gets one band per kitchen ("What each kitchen cooks"), "+ Add another kitchen", and × with an inline confirm

**Files changed.** All are inside the contract. The paths are relative to the worktree `.claude/worktrees/agent-a93967b8b0080da44`.
- `frontend/components/planner/KitchenSections.tsx` (new): KitchenBand, AddKitchen (the ARIA 1.2 combobox), and Counter/Readout with `onBand`, plus ROW_LABEL/NARROW_TWO_UP. The last four moved here from MealComposer.tsx rather than being copied.
- `frontend/components/planner/MealComposer.tsx`: new `kitchens` prop, Band state, per-band crew, and dishes carry `kitchenId`. Steps 3 and 4 are now one step 3, and Notes is step 4.
- `frontend/app/planner/compose/page.tsx`: loads `listKitchens(false)`, waits for it, shows ErrorNotice if it fails, and passes `kitchens`.
- `frontend/app/planner/meal/[id]/page.tsx`: loads `listKitchens(false)` without waiting for it and passes `kitchens`. The meal's own bands come with the meal.
- `frontend/__tests__/meal-composer.test.tsx` and `frontend/__tests__/planner-shift.test.tsx`: fixtures now have kitchens, recipes are picked through the search instead of tick boxes, and payload assertions include `kitchenId` and `kitchens`.
- `frontend/__tests__/meal-composer-kitchens.test.tsx` (new, 26 tests).
- `docs/work/proof/T-351.md`.
- `frontend/__tests__/planner-day-routes.test.tsx`: added to my contract later by the work manager. The grant is recorded under "Not done" item 3.

I read `frontend/lib/api.ts` but did not edit it. I read its diff and the `Kitchen` type, because there was no other way to see the shapes of the reserved types. DISPATCH names them but does not give their fields.

**Commands run and what came back**

`tools/work-lock.sh run verify "frontend/node_modules/.bin/tsc --noEmit -p frontend/tsconfig.json"` (whole frontend):
```
work-lock: 'verify' released; command exited 0
```
Before I added the `kitchens` prop to the test harnesses, the only 3 errors were in my own two test files (TS2322/TS2741, missing `kitchens`). tsc reported no errors in other builders' files at the time of this run.

`tools/work-lock.sh run verify "frontend/node_modules/.bin/vitest run --root frontend __tests__/meal-composer.test.tsx __tests__/meal-composer-kitchens.test.tsx __tests__/planner-shift.test.tsx"`:
```
 ✓ __tests__/meal-composer-kitchens.test.tsx  (26 tests) 480ms
 ✓ __tests__/planner-shift.test.tsx  (36 tests) 752ms
 ✓ __tests__/meal-composer.test.tsx  (65 tests) 4602ms
 Test Files  3 passed (3)
      Tests  127 passed (127)
```
The first run failed 1 of 127. That was a bug in my own test: `/move/i` matched "Remove Main kitchen". I fixed it to `/\bmove\b/i` and reran, which gave the result above.

`tools/work-lock.sh run verify "frontend/node_modules/.bin/eslint --max-warnings=0 <the 7 files above>"`:
```
work-lock: 'verify' released; command exited 0
```

Negative control: `scratchpad/control-T-351.sh`. It removes `kitchenId: d.kitchenId,` from the dishes in the save body, runs the payload tests, restores the file with a trap, and checks the restore with `cmp`:
```
patched lines matching 'kitchenId: d.kitchenId': 0
   × what the save sends > carries the kitchens in band order with their own People needed, and every dish with its kitchen
     → expected [ 'id', 'recipeId', 'targetYield' ] to deeply equal [ 'id', 'kitchenId', 'recipeId', …(1) ]
   × editing a meal two kitchens cook > updates with the kitchens in the same order and each kept dish under its own kitchen
      Tests  2 failed | 2 passed | 22 skipped (26)
RESTORED: cmp identical
```
After the run, `grep -c "kitchenId: d.kitchenId"` returns 1, so the line is back.

I also ran a check outside my contract: `vitest run __tests__/planner-day-routes.test.tsx` (T-352's file):
```
   × editing one meal, at its own id > opens that meal and nothing else, with one pair of buttons at the top
      Tests  1 failed | 35 passed (36)
```
This is expected. The test asserts `getByRole("checkbox", { name: /bisi bele bath/i })`, and the tick-box list was deliberately removed. Its fixture also has no `MealView.kitchens` and its api mock has no `listKitchens`. The other 35 tests pass.

**Acceptance criteria**, all checked in meal-composer-kitchens.test.tsx unless noted:
- Default first band. Tested cases: the person's own kitchen (Sweets); no kitchen of their own gives Main; an own kitchen that is store-only or archived gives Main; a Main that is store-only gives the first planner kitchen (Bakery); and kitchens that load after the composer mounts.
- Add-kitchen combobox. Tested: it offers only ACTIVE kitchens with usesMealPlanner that are not already on the meal, in listKitchens order, each showing "N staff"; typing narrows the list and a query with no matches says so; ArrowDown moves `aria-selected` and `aria-activedescendant`; Enter adds a band at the end without submitting the form; Escape adds nothing; picking with the pointer works; the control is hidden once every planner kitchen is on the meal. A recipe on one band is not offered in another.
- Remove. Tested: the only band has no ×; an empty band is removed at once and the one left loses its ×; a band with dishes asks inside itself ("Remove Sweets kitchen and its 1 dish?" / "…its 2 dishes?"); Keep it keeps everything; Remove takes the band and its dishes. No "move" appears anywhere on the page.
- Save payload. Checked with Object.keys on every kitchen (`crewRequired`, `kitchenId`) and on every dish (`id`, `kitchenId`, `recipeId`, `targetYield`). Kitchens and dishes come out in band order, not in the order they were clicked. There is no top-level `crewRequired`.
- Editing a meal: bands open in `MealView.kitchens` order (Sweets before Main, as the server sent it). Each band keeps its own dishes and its own People needed, and the update keeps that order.
- countVolunteers: the Main band is asked with `true` even when it was added second, and Sweets with `false`. With no Main on the meal, the first band gets `true` and the added band `false`. The readouts are "3 staff · 2 volunteers" and "2 staff", with names from `staffNames`. The band that is short turns amber and gets Ask for volunteers; the covered band does not.
- Everything the composer already did still works: the full meal-composer.test.tsx (65 tests) and planner-shift.test.tsx (36 tests) pass. They cover event chains, ready-by, head counts and scaling, the 50,000 ceiling, Ekadashi filtering, grain confirm and "Leave it out", last menu, the festival crowd, delivery pins, edit mode, the shift layer, the times-changed warning, and the leave guard. "Series/repeat" has no code in the composer; it is the edit page's SeriesLine, which is unchanged.

**Classes carried over from the mock** (`dev-kitchen-meal` BuildMeal/KitchenBand/AddKitchen/Counter/Readout/Step), class for class:
- The step card `card grid gap-3 p-5` and the band wrapper `-mx-5 grid gap-3`.
- The band section `border-y border-hairline bg-sunken px-5 pb-5 pt-3`, the heading row, the × and the dish-row ×.
- The inline confirm box with Button danger and ghost.
- The dish row `flex flex-wrap items-center gap-x-3 gap-y-2 border-t …`, the name/category span, the amount box `min-h-touch w-20 rounded-control border bg-raised px-2 text-sm tabular-nums`, and the unit `w-6 text-xs text-ink-muted`.
- The search box and its results list.
- The band's FieldRow `mt-5 [grid-template-columns:repeat(3,minmax(12rem,1fr))] ${NARROW_TWO_UP}`, Counter and Readout with `onBand` (raised), and the Ask for volunteers Button `h-full w-full justify-center`.
- The names line `mt-2 text-sm text-ink-secondary`.
- AddKitchen's LINK_BUTTON, the combobox and the listbox.

**The design-system guard, fixed after the merged-tree run (2026-09-19).** The work manager reported that `__tests__/design-system.test.ts` › "indents every field label to the inset, and sets it in the one label style" failed on KitchenSections.tsx:228 ("Add a dish to …") and :345 ("Add another kitchen"). The guard rejects any `<label>` whose words sit as a bare text node, because a bare text node cannot carry padding. The mock wrote both labels that way, and the design system wins, so both texts are now in their own `<span>`. The guard was not edited. `FIELD_LABEL` (`pl-field-inset …`) was already on both labels, so what renders is unchanged.

`tools/work-lock.sh run verify "frontend/node_modules/.bin/vitest run --root frontend __tests__/design-system.test.ts __tests__/meal-composer.test.tsx __tests__/meal-composer-kitchens.test.tsx __tests__/planner-shift.test.tsx"`:
```
 ✓ __tests__/design-system.test.ts  (23 tests) 244ms
 ✓ __tests__/meal-composer-kitchens.test.tsx  (26 tests) 562ms
 ✓ __tests__/planner-shift.test.tsx  (36 tests) 887ms
 ✓ __tests__/meal-composer.test.tsx  (65 tests) 4640ms
 Test Files  4 passed (4)
      Tests  150 passed (150)
```

**Not done / needs Rajeev**
1. Where I went beyond the mock, and why. I made these without stopping to ask first, so they need your review:
   - The amount/unit pair has two extra classes, `flex-wrap [&>[data-form-error-slot]]:order-last [&>[data-form-error-slot]]:basis-full`. The form puts the 50,000-ceiling error message right after the box, and without these it would land between the box and its unit.
   - Enter in either search box is always blocked, not only when something matches. The boxes are inside the meal's form, so Enter would otherwise save the meal.
   - Typing in a search box does not mark the form as changed.
   - Things the mock doesn't have: the amount box keeps `max={50000}`, and the ceiling also counts toward the warning border.
   - The dish row no longer shows "· set by hand". The mock's unit cell is `w-6` and the note doesn't fit. The behaviour behind it is unchanged: an amount set by hand is still never overwritten.
   - If the kitchens fail to load on the compose page, it shows an ErrorNotice.
2. Decisions the mock does not settle:
   - The volunteer band says "N volunteers" even when N is 0, because that is how the composer has always worded it. The other bands leave volunteers out.
   - Ask for volunteers appears in every band that is short. It opens the meal's one shift layer with Volunteers requested set to the sum of all bands' shortfalls.
   - Once a shift exists, View volunteer shift and the shift line appear only in the volunteer band.
   - The meal kind's suggested crew fills the first band only.
   - A People needed of 0 is sent as null, because `MealKitchenDraft` says never 0.
   - Last time's menu goes into the first band.
   - A band whose kitchen is in the saved meal's crew row reads its figures from that row. That row counts volunteers signed up to this meal's own shift, which `mealCrewAt` cannot. Every other band is asked with `mealCrewAt(date, readyBy, token, kitchenId, countVolunteers)`.
3. Fixed after the grant below: `planner-day-routes.test.tsx` → "opens that meal and nothing else …" used to fail because it looked for the tick boxes.

**Contract widened (2026-09-19):** the work manager (agent a93967b8b0080da44) granted me `frontend/__tests__/planner-day-routes.test.tsx` after T-352 finished, because no live contract held it. Its words: *"T-351: you can have frontend/__tests__/planner-day-routes.test.tsx. T-352 has finished and no other live contract holds it."*

What changed in that file:
- The checkbox assertions are replaced. The test now checks that the Main kitchen band holds exactly two amount boxes, Bisi Bele Bath at 133 and Kesari Bath at 133, so it still proves "that meal and nothing else".
- `api.listKitchens` is mocked with the main kitchen.
- `within` is added to the import.
- `kitchens` was already on the `lunch()` fixture, and `kitchenId` on its dishes, from T-352, so I left them.

`tools/work-lock.sh run verify "frontend/node_modules/.bin/vitest run --root frontend __tests__/planner-day-routes.test.tsx"`:
```
 ✓ __tests__/planner-day-routes.test.tsx  (36 tests) 702ms
 Test Files  1 passed (1)
      Tests  36 passed (36)
```
4. No hand smoke test, because there is no live backend for Epic 12 yet (E12-2). I have not measured the layout at 1280/1024/390 against the mock; that is T-359 in E12-3.
