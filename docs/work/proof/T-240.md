# T-240 — secondary buttons raised (style E), the press on every `.btn`, mock deleted, DESIGN_SYSTEM v1.13

**Files changed:**
- `frontend/app/globals.css` — `.btn-secondary` and `.btn-quiet` now share style E (card fill, `ink` text, 1px `ink-muted` border, 2px `ink-secondary` bottom edge, `0 1px 3px` ink shadow at 0.14; hover `sunken` fill and a larger shadow). New unlayered block at the end of the file puts the press on `.btn`: transition on transform/colours/shadow at `theme(transitionDuration.press)` with `theme(transitionTimingFunction.out)`, `.btn:active:not(:disabled):not([aria-disabled="true"])` gets `translateY(1px) scale(theme(scale.press))`, and no transform under `prefers-reduced-motion: reduce`. It sits after `@tailwind utilities` on purpose: most hand-made buttons carry `transition-colors duration-state`, which would otherwise replace the transition.
- `frontend/components/ds/Button.tsx` — removed the press utilities (transition, `active:` scale/translate, disabled overrides). Kept `disabled:cursor-not-allowed`.
- `frontend/app/dev-buttons/` — deleted (the T-238 mock). Also deleted `frontend/.next/types/app/dev-buttons/` (generated).
- `docs/DESIGN_SYSTEM.md` — v1.13: status line; §4 press paragraph now says it lives on `.btn`; new §4 subsection "Secondary buttons are raised"; §6 exception for the recipe page's download icon (Q8).
- `docs/versions/DESIGN_SYSTEM_v1.13.md` — new snapshot, copy of the above.
- `docs/CHANGELOG.md` — "DESIGN_SYSTEM v1.13 — 2026-09-19" entry inserted after v1.12, before "Versioning convention".
- `docs/work/proof/T-240.md` — this file.

All are the files the task named. The CHANGELOG is normally off-limits to builders; the task brief explicitly asked for the entry, so I added one entry and touched nothing else in it.

**Commands run and what came back:**

```
tools/work-lock.sh run verify 'cd frontend && npx tsc --noEmit; echo TSC_EXIT=$?; npx vitest run 2>&1 | tail -25; npx eslint components/ds/Button.tsx; echo ESLINT_EXIT=$?'
TSC_EXIT=0
 ...
 Test Files  147 passed (147)
      Tests  2010 passed (2010)
   Start at  18:52:08
   Duration  18.41s (transform 4.74s, setup 6.12s, collect 19.88s, tests 84.01s, environment 31.57s, prepare 5.37s)

ESLINT_EXIT=0
work-lock: 'verify' released; command exited 0
```
(globals.css is not in eslint's scope; the only changed TS file is Button.tsx.)

**Browser checks** (local http://localhost:3000, signed in as the temple admin, Graphite pack, 1456px viewport; my own tab, closed at the end). The tab was `visibilityState: hidden` and CDP 9222 was not listening, so transitions do not tick there; I did not time anything.

Calendar day panel, computed styles:
```
Today                | btn-secondary | 36 | rgb(252,253,255) | rgb(35,37,40) | 1px rgb(107,109,113) | 2px rgb(79,81,84) | rgba(35,37,40,0.14) 0px 1px 3px 0px | 5px
Correct this date    | (warning)     | 36 | rgb(249,238,226) | rgb(128,93,48) | 1px ... | 1px ... | none | 5px
Plan this day's menu | btn-quiet     | 36 | rgb(252,253,255) | rgb(35,37,40) | 1px rgb(107,109,113) | 2px rgb(79,81,84) | rgba(35,37,40,0.14) 0px 1px 3px 0px | 5px
```
Screenshot looked at: "Plan this day's menu" now shows a border, darker bottom edge and shadow next to the unchanged amber button. Both 36px (the small size), same top.

Press, read from the live stylesheets (walked `document.styleSheets`) and computed style:
```
.btn:active:not(:disabled):not([aria-disabled="true"]) => transform:translateY(1px) scale(0.96)
@media (prefers-reduced-motion: reduce) .btn:active:not(:disabled):not([aria-disabled="true"]) => transform:none
computed transition: transform, background-color, border-color, color, box-shadow / 0.12s x5 / cubic-bezier(0.23, 1, 0.32, 1)
```
Real press, done with a trusted mouse down/up from the extension on "Plan this day's menu" (click default prevented, transition set to none on that element so the tab's frozen clock could not hide the end value), recorded in a pointerdown listener:
```
["down", :active=true, "matrix(0.96, 0, 0, 0.96, 0, 1)", "rgba(0, 0, 0, 0.12) 0px 1px 2px 0px inset"]
```
After release: `:active=false | transform none | height 36`.
Disabled exemption: the calendar's "Today" button was disabled (already on today). Held down, it read `:active=true` with `transform: none` throughout, and `matches('.btn:active:not(:disabled)...')` was false.
Hand-made button: on /shopping-list, "Generate purchase order" (`btn btn-primary ... transition-colors duration-state`) computes `transform, background-color, border-color, color, box-shadow / 0.12s`, so the base `.btn` transition beats the utility as intended.

Meal edit (Edit Dinner, 19 Sept), "Who will run it" row, screenshot looked at, DOM geometry:
```
People needed box  top=427 h=52 w=351
Rostered box       top=427 h=52 w=351
View volunteer shift (btn-quiet) top=427 h=52 w=351
```
This meal already has a shift, so the button says "View volunteer shift". It is the same `variant="secondary"` material as "Ask for volunteers". Form header: "Cancel" (btn-quiet) 44px and "Update this meal" (btn-primary) 44px, same top, style E border and shadow on Cancel.

Contrast in all 15 packs, on the real classes: a wrapper was given each pack's tokens (exported from `lib/theme-packs.ts` through `lib/theme.ts`), with a real `button.btn.btn-secondary` and a real `button.btn.btn-quiet` inside it. Colours come from `getComputedStyle`. The hover fill was read from the live `.btn-secondary:hover, .btn-quiet:hover` rule (`var(--sunken)`) and resolved in the same wrapper. WCAG ratio.
```
secondary terracotta text=15.15 hoverText=13.65 borderCard=5.12 borderGrey=4.61 bottomCard=7.85 bottomGrey=7.07 borderOnHover=4.61 h=44 bw=1px/2px
quiet     terracotta (identical)
WORST over 15 packs (both classes identical):
text 15 (indigo-ledger)   hoverText 11.85 (jamun)
border vs card 5.09 (graphite)   border vs grey sunken 4.61 (peacock)
bottom edge vs card 7.76 (fern-courtyard)   bottom edge vs grey 6.15 (jamun)
border on its own hover fill 4.61 (peacock)
```
Text is at least 4.5:1 and border at least 3:1 everywhere. Height 44px at the md size, same as primary.

Focus: unchanged global `:focus-visible` outline (2px `--focus-ring`). Not re-measured in the browser.

**Button-looking elements that use neither `Button` nor `btn`** (they get neither style E nor the press; all outside this contract, not changed):
- app/donations/page.tsx:338 (bordered button)
- app/library/page.tsx:123
- app/tenants/new/page.tsx:401 (Link)
- app/tenants/[id]/page.tsx:75 (Link), :264, :391, :512, :519 (hand-made danger)
- app/tenants/[id]/edit/page.tsx:270 (Link)
- app/notices/page.tsx:157 (hand-made danger-tint)
- app/shopping-list/page.tsx:663
- app/recipes/page.tsx:153 (Link)
- app/recipes/[id]/page.tsx:159 (Link), :166, :215 (hand-made danger), :309, :355 (the download icon)
- app/sign-in/page.tsx:132 (accent-tint)
- app/orders/[id]/page.tsx:773, :774, :1443 (hand-made danger)
- app/shifts/page.tsx:136, :143
- components/PurchaseOrderEditor.tsx:460, :515
- components/RecipeForm.tsx:350
- components/staff/Ban.tsx:170
- components/staff/PayPanel.tsx:265
- Icon-only, borderless: components/RecipePeek.tsx:105, components/ds/PeriodNav.tsx:142, app/communications/composer.tsx:387
Found with a regex scan of every .tsx for `<button|a|Link|label>` whose className has a border or tinted fill, padding or min-height, and rounding, but no `btn`. List rows, the staff-schedule grid cell, sidebar items and checkbox cards also matched and were left off because they are not buttons.

**Acceptance criteria:**
1. Style E on both `.btn-quiet` and `.btn-secondary`, tokens only, rounded-control, 44px, hover and focus visible, danger and warning unchanged: done. Computed styles above. Contrast measured in 15 packs.
2. Press moved to base `.btn` (0.96 plus 1px, 120ms, disabled exempt, reduced motion drops the movement): done. Rule, computed transition, real pressed transform, disabled exemption and hand-made override all shown above. Hand-made buttons without `btn` are listed.
3. `app/dev-buttons/` deleted along with its `.next` types. tsc is clean and the full vitest run is 147/147 files.
4. DESIGN_SYSTEM v1.13 (§4 style E, §4 press on `.btn`, §6 download icon), snapshot and CHANGELOG entry: done.

**Not done / needs Rajeev:**
- Style E is painted flat in every pack, as the mock showed. The glossy packs lose their gradient on the white button and the frosted packs lose its blur. The `btn-secondary-*` tokens are still in `lib/theme-packs.ts` but nothing reads them now.
- `.btn-quiet` now has dark ink text instead of accent text. Some screens used quiet (accent) against ghost (grey) to tell two buttons apart. Both now look the same. The settings edit-mode tests still pass because the class names are unchanged.
- I did not time the 120ms animation: the tab was hidden and CDP was not available. Only the rule and the end state were measured.
- The §6 wording for the download icon comes from the brief ("Rajeev's Q8 decision"). I had no quote of his.
- Checked only at a 1456px desktop width, in the Graphite pack live, plus the 15-pack wrapper measurement. Phone width not checked. Hover was checked from the rule, not by hovering on screen.
- To export the pack tokens, `npx tsx` was downloaded on demand (tsx@4.23.13, into the npx cache, not into package.json).
