# T-221 — every pill and over-rounded chip, control, hand-made button and value box takes the buttons' corner

Built the 13 "change" rows from T-220's comparison, approved by Rajeev 2026-09-17 ("Approved, make the
changes"). Every one now uses `rounded-control` (`--radius-control`, the corner `.btn` already uses).

## How it was done

Hand edits only, line by line. T-220's shortcut of remapping Tailwind's `sm`/`DEFAULT`/`lg` steps to the
theme corners was NOT used, because it would also have changed the notice/alert boxes and card-like
boxes, which Rajeev did not decide on. `tailwind.config.ts` is untouched.

136 class edits, applied by a script that rewrote one listed file:line at a time and refused any line
holding more than one radius class, plus `Badge.tsx` by hand:

- Status pill: `Badge` is `rounded-control` for every badge. Its `shape` prop now meant nothing, so it
  was removed, along with its 4 call sites (vendors/page.tsx, calendar/page.tsx, today/page.tsx x2).
- Hand-made status chips (`rounded-sm` / `rounded` / `rounded-md`): vendors, vendors/[id], invoices,
  invoices/[id], my-shifts, tenants, shopping-list, recipes, recipes/library/[id] (tags), recipes/[id],
  inventory, inventory/[id], money, ingredients, volunteers/[id], orders/po-status, shifts, wishlist,
  design-reference, planner/reuse (dish chips), settings:632 (event-name chips).
- Hand-made `rounded-full` pills: settings:753, :1313; DonatePage:468.
- Crew and shift pills: MealServices:1203, :1259.
- Workforce pills and fast/festival pills: planner/page.tsx:349, :357, :546, :554.
- Meal-kind chip: MealComposer:1184.
- Counter −/+ buttons: MealComposer:2022, :2038. (The Counter box and Readout box themselves had
  already been done by the main session and were not touched.)
- Segmented control: SegmentedControl track and segments; hand-made tab lists in register:288/303,
  sign-in:143/152, DonatePage:128/142.
- Period heading pill: PeriodNav:97. PeriodNav's own IconButton (:146) too, as a hand-made button.
- Hand-made buttons: donations, staff-schedule (Prev/Next), my-shifts, library, tenants/new,
  tenants/[id], tenants/[id]/edit, notices, shopping-list, profile, recipes, recipes/library/[id],
  recipes/[id], inventory (low filter), inventory/[id], sign-in (Google, back), my-donations,
  orders/[id], shifts, equipment (overdue filter), communications/composer (toolbar), PlatformNotices
  (dismiss), IngredientRequestForm, PurchaseOrderEditor, RecipePeek, RecipeForm, staff/Ban,
  staff/PayPanel.
- Value boxes: settings:503, :591, :782, :1133, :1220; InventoryItemForm:204, :212.

Comments that described the old round shape were corrected: PeriodNav ("pill" → "mark" in 4 comments),
Badge's header, and app/staff/page.tsx, whose comment said §4 reserved the pill for status chips.

## Files changed (all inside the task)

frontend: components/ds/Badge.tsx, components/ds/SegmentedControl.tsx, components/ds/PeriodNav.tsx,
components/planner/MealServices.tsx, components/planner/MealComposer.tsx, components/give/DonatePage.tsx,
components/PlatformNotices.tsx, components/InventoryItemForm.tsx, components/IngredientRequestForm.tsx,
components/PurchaseOrderEditor.tsx, components/RecipePeek.tsx, components/RecipeForm.tsx,
components/staff/Ban.tsx, components/staff/PayPanel.tsx, app/settings/page.tsx, app/vendors/page.tsx,
app/vendors/[id]/page.tsx, app/donations/page.tsx, app/invoices/page.tsx, app/invoices/[id]/page.tsx,
app/staff-schedule/page.tsx, app/planner/page.tsx, app/planner/reuse/page.tsx, app/my-shifts/page.tsx,
app/library/page.tsx, app/tenants/page.tsx, app/tenants/new/page.tsx, app/tenants/[id]/page.tsx,
app/tenants/[id]/edit/page.tsx, app/notices/page.tsx, app/register/page.tsx, app/shopping-list/page.tsx,
app/profile/page.tsx, app/recipes/page.tsx, app/recipes/library/[id]/page.tsx, app/recipes/[id]/page.tsx,
app/inventory/page.tsx, app/inventory/[id]/page.tsx, app/money/page.tsx, app/ingredients/page.tsx,
app/volunteers/[id]/page.tsx, app/sign-in/page.tsx, app/my-donations/page.tsx, app/orders/po-status.tsx,
app/orders/[id]/page.tsx, app/shifts/page.tsx, app/equipment/page.tsx, app/design-reference/page.tsx,
app/communications/composer.tsx, app/wishlist/page.tsx, app/calendar/page.tsx (one `shape="pill"`
removed), app/today/page.tsx (two `shape="pill"` removed), app/staff/page.tsx (comment only).

Deleted: frontend/app/dev-shapes/ (the T-220 mock), and the dev server's stale generated
frontend/.next/types/app/dev-shapes/, which otherwise made tsc fail on a missing module.

docs: docs/DESIGN_SYSTEM.md (status line, §4 radii line, now v1.9), docs/CHANGELOG.md (v1.9 entry in
the same place and format as v1.7 and v1.8), docs/versions/DESIGN_SYSTEM_v1.9.md (new snapshot, a copy
of the root file, as v1.7 and v1.8 were done). This proof.

Files already dirty from other tasks (calendar/page.tsx, planner/page.tsx, MealComposer.tsx,
MealServices.tsx, DayView.tsx and others) were edited by exact line only; nothing of theirs was
reverted. DayView.tsx was not touched (its only radius is the dashed add box, a card-like row).
Sidebar.tsx was not opened, per the builder contract, so its nav items and icon button keep their
12px corner.

No tests needed changing: no test in `__tests__` asserts a radius class.

## Commands run and what came back

First run failed on the dev server's stale generated types for the deleted mock:

```
.next/types/app/dev-shapes/page.ts(2,24): error TS2307: Cannot find module '../../../../app/dev-shapes/page.js' or its corresponding type declarations.
```

Removed `frontend/.next/types/app/dev-shapes` and re-ran:

```
$ tools/work-lock.sh run verify 'cd frontend && npx tsc --noEmit; echo TSC_EXIT=$?; npx vitest run ...; echo VITEST_EXIT=$?; ...; npx eslint app components; echo ESLINT_EXIT=$?'
TSC_EXIT=0
VITEST_EXIT=0
 ✓ __tests__/back-to-recipes.test.tsx  (3 tests) 34ms
 ✓ __tests__/routes.test.tsx  (3 tests) 1ms

 Test Files  141 passed (141)
      Tests  1939 passed (1939)
   Duration  17.03s

ESLINT_EXIT=0
work-lock: 'verify' released; command exited 0
```

Dev server after the change:

```
sign-in 200
planner 200
dev-shapes 404
```

`next build` was not run, because a dev server is running on :3000.

Remaining full rounding, checked by grep: 14 `rounded-full`, all circles (settings:723, calendar:214,
:273, :288, reuse:526, Sidebar:315, :362, InfoHint:39, MealComposer:1844) or meter bars
(shifts:162, DonatePage:372, :375, :481, :485).

## Acceptance criteria

- 13 "change" rows built: see the list above. Verified by grep; the only `rounded-sm` left are native
  checkboxes, InlineNotice, the tenants/[id] result notice, the calendar year view's day cells, the
  reuse screen's faux checkbox, and an underlined text link in MealServices:536.
- "Your call" rows left as they are: notice and alert boxes, and card-like boxes (settings alerts,
  sign-in notices, ErrorNotice, InlineNotice, PlatformNotices cards, week cells, dashed add rows,
  profile and PayPanel boxes, etc.).
- "Keep" rows kept: circles, meter bars, checkboxes, Button, Tooltip, inputs.
- Design system amended to v1.9 with changelog entry and snapshot.
- dev-shapes deleted; tsc, full vitest and eslint green.

## Not done / needs Rajeev

- Notice/alert boxes and card-like boxes: deliberately unchanged. He has not decided them.
- Sidebar.tsx nav items and its icon button still have 12px corners: the builder contract forbids
  opening that file.
- The staff schedule's day cells (staff-schedule:328 and the clickable :442) were treated as grid
  cells, card-like, and left alone.
- Not checked by eye in a browser. Only HTTP 200s on the dev server; most changed screens need a
  signed-in session.
