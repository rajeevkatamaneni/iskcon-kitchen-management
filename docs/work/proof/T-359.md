# T-359 — the series-of-one defect, the leave line's kitchen name, and Epic 12 driven and measured

Written as the work went, not at the end.

## 1. What the dead run left, and what I did with each piece

The previous run of T-359 died from an infrastructure error, not a code error. Its work was still
in the worktree, and its two servers and its database were still up. Taking stock first:

### Still running from the dead run (contrary to the brief, which said nothing was listening)

| Thing | State found | What I did |
|---|---|---|
| API on :8180, pid 91496, cwd `…/agent-a93967b8b0080da44/backend` | `{"status":"UP"}` | Kept it, and did **not** restart it: my one backend edit is a javadoc comment, which changes no behaviour. The running process was started 15:46 and so predates that edit. The backend tests below were compiled and run fresh from the edited source. |
| Web on :3100, pid 73324, `next-server v14.2.15`, cwd `…/agent-a93967b8b0080da44/frontend` | HTTP 200 | Kept it. It is `next dev`, so it picks up edits. |
| Database `kms_e12` | exists, Flyway at v152, 1 tenant, 5 kitchens, seeded | Kept it — rebuilding it would have thrown away the seed and the Flyway evidence. |

`:3000`, `:8080`, `kms` and `kms_verify` were left alone throughout; see §9.

### The files the brief named, hunk by hunk

**`backend/.../meal/MealCrewView.java` — kept, with one repair.** The record already carried
`mealKitchenCount` and `kitchens`. The dead run had left a **duplicated `@param mealId` javadoc
block**: the Epic 12 `<p>` paragraph had been inserted *after* the first `@param mealId` and the
original `@param mealId` re-pasted below it, so the tag appeared twice and the prose sat inside a
`@param`. Repaired by moving the paragraph above the tag block and deleting the duplicate. No code
change.

**`backend/.../meal/MealCrewService.java` — kept, complete.** `crewFor(from, to, viewerUserId)`
builds `KitchenCrewView`s per section and passes `kitchens.size()` as the meal's kitchen count
(right: a range readout carries every section). `crewIfAway` passes `onMeal.size()` — the whole
meal's count — while putting only the affected section in the list, which is exactly the
distinction `mealKitchenCount` exists for. Nothing half-done.

**`backend/.../meal/MealCrewController.java` — kept, complete.** `/range` and `/suggested` take
`@AuthenticationPrincipal`, `/at` takes `kitchenId` + `countVolunteers`, `CrewAt` carries
`staffNames`.

**`backend/src/test/.../meal/MealCrewIT.java` — kept.** Re-run below.

**`frontend/app/leave/page.tsx` — kept, complete.** `describe()` names the kitchen only when
`meal.mealKitchenCount > 1`.

**`frontend/__tests__/leave.test.tsx` — kept, complete.** Both halves covered: the one-kitchen case
asserts the name is *absent*, the two-kitchen case asserts `Lunch (Sweets kitchen) … 0 of 2`.

**`frontend/components/planner/MealServices.tsx` — kept, complete on both halves of the defect.**
`SeriesLine` returns `null` when `series.count <= 1`; `askToCancel` only reads the later occurrences
when `series.position < series.count`, so a survivor never gets the "just this one / this and all
later ones" question and `api.laterInSeries` is never called.

**`frontend/__tests__/repeat-series.test.tsx` — kept, complete.** Both halves tested.

So of the seven files the brief named, six were finished and right; one needed a javadoc repair.
The remaining work was §3 onwards: driving it and measuring it.

## 2. The Flyway log from `kms_e12`

The copy database was created by the dead run and the migrations applied to it at 15:14:58 today.
Captured in `scratchpad/flyway-kms_e12.txt`:

```
15:14:57.956 INFO  [req=] org.flywaydb.core.FlywayExecutor - Database: jdbc:postgresql://localhost:5432/kms_e12 (PostgreSQL 16.15)
15:14:58.041 INFO  [req=] o.f.core.internal.command.DbValidate - Successfully validated 146 migrations (execution time 00:00.073s)
15:14:58.066 INFO  [req=] o.f.core.internal.command.DbMigrate - Current version of schema "public": 149
15:14:58.075 INFO  [req=] o.f.core.internal.command.DbMigrate - Migrating schema "public" to version "150 - which kitchen is cooking"
15:14:58.095 INFO  [req=] o.f.c.i.s.DefaultSqlScriptExecutor - +-------------------+
| enable_tenant_rls |
+-------------------+
|                   |
+-------------------+

15:14:58.113 INFO  [req=] o.f.c.i.s.DefaultSqlScriptExecutor - DB: V150: temple iskcon-south-bengaluru — kitchen "Main Kitchen" (existing): 76 meals, 174 dishes, 11 staff to check
15:14:58.124 INFO  [req=] o.f.core.internal.command.DbMigrate - Migrating schema "public" to version "151 - crew is per kitchen"
15:14:58.133 INFO  [req=] o.f.core.internal.command.DbMigrate - Migrating schema "public" to version "152 - card version is per kitchen"
15:14:58.145 INFO  [req=] o.f.core.internal.command.DbMigrate - Successfully applied 3 migrations to schema "public", now at version v152 (execution time 00:00.050s)
```

Real data, not a fixture: 76 meals and 174 dishes, all of them adopted by the existing Main Kitchen,
and 11 staff records flagged for the Temple Admin to check. Confirmed in the table afterwards:

```
 version |          description          | success |        installed_on
---------+-------------------------------+---------+----------------------------
 152     | card version is per kitchen   | t       | 2026-09-19 15:14:58.128624
 151     | crew is per kitchen           | t       | 2026-09-19 15:14:58.120294
 150     | which kitchen is cooking      | t       | 2026-09-19 15:14:58.063138
```

## 3. The seed the roles were driven against

```
 plan_date  | mealkind |    kitchen     | crew_required |                 dishes
------------+----------+----------------+---------------+-----------------------------------------
 2026-09-24 | Lunch    | Govindas Bliss |             2 | Kesari Bath
 2026-09-24 | Lunch    | Main Kitchen   |             6 | Arbi ki Sabzi (Haryana), Lauki Chana Dal (Bihar), Basmati Ghee Rice
```

Kitchens (`uses_meal_planner` is the flag that decides who gets the planner):

```
      name        | is_main | uses_meal_planner
------------------+---------+-------------------
 Main Kitchen     | t       | t
 Govindas Bliss   | f       | t
 Health Kitchen   | f       | t
 Deity Kitchen    | f       | f      <- the kitchen that does not plan meals here
 Individual Person| f       | f
```

Staff and accounts:

```
    kitchen     | staff
----------------+-------------------------------------------------------------------------
 Main Kitchen   | Karuna Murti Das, Lalita Devi Dasi, Nitai Chandra Das, Yamuna Devi Dasi
 Govindas Bliss | Madhava Das
 Deity Kitchen  | Gopal Das
```

| Account | Role | Kitchen |
|---|---|---|
| `ikms.temple-admin.1` | TEMPLE_ADMIN | Main Kitchen |
| `ikms.kitchen-staff.2` | KITCHEN_STAFF | Main Kitchen (plans meals) |
| `ikms.kitchen-staff.5` | KITCHEN_MANAGER | Govindas Bliss (plans meals) |
| `ikms.kitchen-staff.1` | KITCHEN_STAFF | Deity Kitchen (**does not** plan meals) |
| `ikms.volunteer.1` | VOLUNTEER | — |

## 4. Priority 1 — the series-of-one defect, driven in the live app

The database had no series-of-one, so the dead run had already made one: event **"T-359 Series
Check"**, four weekly occurrences from 1 Oct, the last three cancelled, one standing. The series row's
`until_date` is 2026-10-01 — the survivor's own date, which is the staging symptom exactly.

```
                  id                  | plan_date  |     event_name     | all_cancelled
--------------------------------------+------------+--------------------+---------------
 d57a876e-620b-4784-8c1e-9578ea5cbbcb | 2026-10-01 | T-359 Series Check | f
 4216387d-a9a5-4bf2-9210-d264f0b672d4 | 2026-10-08 | T-359 Series Check | t
 56c448f1-e21a-4c33-a1bc-eb27d31c3b80 | 2026-10-15 | T-359 Series Check | t
 8394c792-3087-41f9-811f-f66a82a9ee77 | 2026-10-22 | T-359 Series Check | t
```

**Half one — the line is gone.** `/planner?date=2026-10-01`, read out of the DOM:

```
{"found":true,"repeats":false,
 "block":"T-359 Series Check\nEvent\nReady by 18:00\nNot yet recorded\n40 adults expected\n·\n40 servings
          \n·\nMain Kitchen\nRecord actuals\nEdit\nCancel this meal\nRepeat this event\nMain Kitchen\n…"}
```

`repeats:false` is a regex for `/Repeats every/` over the whole page. No *"Repeats every week until 1
Oct 2026 · event 1 of 1"*.

**The meal's own page too**, `/planner/meal/d57a876e-…`, the other place `SeriesLine` is used:
`{"repeats":false,"thisDateOnly":false}` — neither the sentence nor the *"Changes here apply to this
date only"* note it carries, which is right: one occurrence is not a series, so there is no other
date for changes to be distinguished from.

Both call sites go through the `SeriesLine` component; the string form `seriesLine()` is now only
referenced by its own unit test. Grepped:

```
app/planner/meal/[id]/page.tsx:166:  {meal.series && <SeriesLine series={meal.series} note="Changes here apply to this date only." />}
components/planner/MealServices.tsx:510:  {series && <SeriesLine series={series} />}
```

**Half two — cancelling asks nothing extra.** `api.laterInSeries` was wrapped with a fetch counter,
then Cancel was pressed:

```
{"laterCalled":false,
 "dlgs":[["DIV","alertdialog",null,"Cancel T-359 Series Check?\n\nIts preparations come off the plan.\n\nKeep it\nCancel this meal"]]}
```

No radio buttons, no *"This event repeats."* sentence, and the server was never asked for the later
occurrences. Screenshot: `T-359-series-of-one-plain-cancel.jpg`.

**Week view.** Checked as well, since a week tile could carry its own repeat marker: grepping the
whole frontend for `Repeats` and `ti-repeat` returns five hits, all inside `MealServices.tsx`'s
`seriesLine`/`SeriesLine`. The week tile has no series marker at all, so there is nothing there to
fix.

## 5. Priority 2 — the leave line names the kitchen only when there is more than one

The frontend and its test were finished by the dead run; the backend side was too. Read back to
confirm rather than assumed:

- `MealCrewService.crewFor` passes `kitchens.size()` — every section of the meal is in that readout,
  so the two counts coincide.
- `MealCrewService.crewIfAway` passes `onMeal.size()` (the whole meal) while putting only the one
  affected section in `kitchens`. That is the case `mealKitchenCount` exists for, and it is the case
  the leave line reads.
- `app/leave/page.tsx`'s `describe()`: `const kitchen = meal.mealKitchenCount > 1 ? meal.kitchens[0]?.kitchenName : undefined;`

`frontend/lib/api.ts` was **not** edited — `mealKitchenCount` was already declared there.

## 6. Priority 3 — the roles, and what each one saw

Tokens were minted out of band for each test account (Firebase custom token via the admin SDK service
account — the method in the `testing-uat-api-as-a-user` note). No account password was used anywhere.

### At the API, `/api/v1/meals?from=2026-09-24&to=2026-09-24`

```
================ Temple Admin (Karuna Murti Das, Main Kitchen, canPlanMeals: True) ================
   HTTP 200 … "kitchens":[{"kitchenId":"7b7a1f93-…(Main Kitchen)"…
================ Kitchen Staff, planner kitchen (Yamuna Devi Dasi, Main Kitchen, canPlanMeals: True) ================
   HTTP 200 … "kitchens":[{"kitchenId":"7b7a1f93-…(Main Kitchen)"…
================ Kitchen Manager, second planner kitchen (Madhava Das, Govindas Bliss, canPlanMeals: True) ================
   HTTP 200 … "kitchens":[{"kitchenId":"f159107d-…(Govindas Bliss)"…   <- their own kitchen first
================ Kitchen Staff, NON-planner kitchen (Gopal Das, Deity Kitchen, canPlanMeals: False) ================
   HTTP 403
{"code":"KMS-400183","message":"Your kitchen doesn't plan its meals here, so the meal planner isn't open to you.","action":"If you have moved kitchens, ask a Temple Admin to change your kitchen on the Staff page.","fieldErrors":[]}
================ Volunteer (Volunteer One, no kitchen, canPlanMeals: False) ================
   HTTP 403
{"code":"KMS-400021","message":"You don't have permission to do that.","action":"If you think you should, ask your temple administrator.","fieldErrors":[]}
```

`whoami` carries `kitchenId`, `kitchenName` and `canPlanMeals` for every one of them.

### In the browser

**Temple Admin** (Main Kitchen). `/planner?date=2026-09-24` — screenshot
`T-359-1280-day-two-kitchens.jpg`. Sections in the order **Main Kitchen, then Govindas Bliss**; the
fact line reads *"600 adults expected · 600 servings · Main Kitchen and Govindas Bliss"*, same order.

**Kitchen Manager in the second planner kitchen** (Madhava Das, Govindas Bliss). Same meal, same
minute — screenshot `T-359-own-kitchen-first-sweets-manager.jpg`:

```
Lunch … 600 servings · Govindas Bliss and Main Kitchen
Govindas Bliss  1 preparation   Kesari Bath … 120 L   People needed 2 · 1 of 2 rostered   Madhava Das
Main Kitchen    3 preparations  Basmati Ghee Rice … / Arbi ki Sabzi … / Lauki Chana Dal …
                                People needed 6 · 2 of 6 rostered   Nitai Chandra Das, Yamuna Devi Dasi
```

Their own kitchen first, and the fact line's order follows the sections.

**Kitchen Staff in a kitchen that does not plan meals here** (Gopal Das, Deity Kitchen). Menu read out
of the DOM — **no "Meal planner" item**:

```
Today / Vaishnava calendar / Cost per serving / ORDERING … / INVENTORY & RECIPES … /
KITCHENS: Ingredient requests, Issued to kitchens / PEOPLE: My schedule, Volunteer shifts / GIVING & OUTREACH: Donations
```

`/planner` refuses: *"Not your page — You don't have access to this part of the app. Ask your temple
administrator."* See §8 for two problems found here.

**Volunteer.** Refused at the API with KMS-400021. Not driven in the browser — see §10.

### Composing a two-kitchen meal and saving it

Driven through the UI at 1280 as the Temple Admin, on 25 September (a day with nothing planned):

1. The composer opened on **one section, the viewer's own kitchen**, with *"Rostered in Main Kitchen ·
   3 staff · 0 volunteers"* and the three names under it — the per-kitchen `/meal-crew/at` call.
2. Added *Basmati Ghee Rice* to Main Kitchen.
3. **+ Add another kitchen** offered exactly `Govindas Bliss (1 staff)` and `Health Kitchen (0 staff)`
   — screenshot `T-359-compose-add-another-kitchen.jpg`. Deity Kitchen and Individual Person are
   absent (they do not plan meals here) and Main Kitchen is absent (already on the meal). Correct.
4. Added *Kesari Bath* to Govindas Bliss, 120 adults, 40 L / 12 L, People needed 5 and 2.
5. Saved. Landed on the day with *"The meal was planned."* and:

```
Breakfast  Ready by 07:30  Not yet recorded  120 adults expected · 120 servings · Main Kitchen and Govindas Bliss
Main Kitchen    1 preparation  Basmati Ghee Rice  Short · order by 23 Sept  40 L   People needed 5 · 3 of 5 rostered
                                                   Lalita Devi Dasi, Nitai Chandra Das, Yamuna Devi Dasi
Govindas Bliss  1 preparation  Kesari Bath        Ingredients ready        12 L   People needed 2 · 0 of 2 rostered
```

### Both job cards

`/api/v1/job-cards/print` as the Temple Admin. With no kitchen named on a two-kitchen meal:

```
   HTTP 400
{"code":"KMS-400186","message":"More than one kitchen is cooking this meal, so say whose job card to print.","action":"Use Download job card in that kitchen's section of the meal.","fieldErrors":[]}
```

Main Kitchen's card (`scratchpad/t359-card-main.html`), text stripped of markup:

```
LC-2026-0013 · Main Kitchen   ISKCON South Bengaluru   Lunch · Main Kitchen   Thursday 24 September 2026
Ready by 11:30   Head count 600   600 adults
Food items to prepare: Basmati Ghee Rice 210 L / Arbi ki Sabzi (Haryana) 110 L / Lauki Chana Dal (Bihar) 155 L
People working on this job card — People needed · 6 people
  Staff · 2   Nitai Chandra Das +919845020003   Yamuna Devi Dasi +919845020002
  Volunteers · 0  Nobody is rostered
   dishes named anywhere in the card: {'Lauki Chana Dal', 'Arbi ki Sabzi', 'Basmati Ghee Rice'}
```

Govindas Bliss's card (`scratchpad/t359-card-sweets.html`):

```
LC-2026-0013 · Govindas Bliss   ISKCON South Bengaluru   Lunch · Govindas Bliss   Thursday 24 September 2026
Ready by 11:30   Head count 600   600 adults
Food items to prepare: Kesari Bath 120 L
People working on this job card — People needed · 2 people
  Staff · 1   Madhava Das +919845020005
  Volunteers · 0  Nobody is rostered
   dishes named anywhere in the card: {'Kesari Bath'}
```

Each names its kitchen twice (card-number line and heading), carries only its own dishes and only its
own kitchen's staff, and both carry the meal's one card number `LC-2026-0013`. The dish-name check is
a search of the **whole** card including the recipe appendix, so no other kitchen's dish leaks in.

### The week view

`/planner?view=week&date=2026-09-24`, screenshot `T-359-1280-week-view.jpg`. Thursday 24's tile:

```
THU | 24 | 4 staff in | 0 volunteers signed up | Srila Bhaktivinoda Thakura -- Appearance
11:30 Lunch | 4 preparations · 600 servings
Main Kitchen | Basmati Ghee Rice | Arbi ki Sabzi (Haryana) | Lauki Chana Dal (Bihar)
Govindas Bliss | Kesari Bath
```

The kitchen appears as a grouping heading only where there is more than one: Tuesday 22's one-kitchen
dinner in the same screenshot lists its five dishes flat, with no kitchen name.

### Today

`/today`. Each meal reads *"184 servings · Main Kitchen"* — the kitchen is named even at one kitchen
here, which is the settled Epic 12 behaviour (the meal card's fact line does the same, deliberately:
*"Said even for one kitchen, as every meal shows its sections"*). Only the **leave line** suppresses
it at one kitchen, and only the **week tile** omits it, where an 89px-wide tile has nothing to group.

### The staff form's required Kitchen

`/staff/new`, read off the DOM:

```
{"name":"kitchenId","req":true,"label":"Kitchen","opts":["Choose a kitchen","Main Kitchen","Deity Kitchen","Govindas Bliss","Health Kitchen","Individual Person"],"value":""}
```

`required: true`, empty initial value, placeholder option *"Choose a kitchen"*. Every active kitchen
is offered, including the two that do not plan meals here — right, because a person works in a kitchen
whether or not that kitchen plans through this app.

### The Temple Admin's "Check these kitchen assignments"

The dead run had already confirmed all six active staff, so the panel was gone. Two were flagged again
(`update staff_profiles set kitchen_needs_check=true where full_name in (…)`) to see it. Screenshot
`T-359-kitchen-assignment-checks.jpg`:

```
Check these kitchen assignments
When staff were given kitchens, everyone was put in the main kitchen. Move anyone who works in another kitchen.
Lalita Devi Dasi   Cook           [Main Kitchen ▾]
Nitai Chandra Das  Store Manager  [Main Kitchen ▾]
[These are right]
```

The flags were set back to false afterwards, so the copy database is as the dead run left it.

## 7. Priority 4 — measured against the mock, element by element

Neither the window nor the screen can be made 1280 wide (the display is 1470 CSS px and the window is
fullscreen; `resize_window` reported success while `innerWidth` stayed 1470 — measured, not assumed).
So each width was measured in a same-origin iframe of exactly that width: an iframe has its own
viewport, so the page inside it lays out and answers media queries as it would in a window of that
width. `contentWindow.innerWidth` was read back as 1280 / 1024 / 390 each time to confirm it.

The mock (`localhost:3000/dev-kitchen-meal?option=one`) was measured the same way, in its own tab.
All numbers are CSS pixels from `getBoundingClientRect()` and `getComputedStyle()`.

### 1280 — the app against the mock

| Element | Mock | App | Same? |
|---|---|---|---|
| `<main>` beside the 280px sidebar | x 280, w 1000 | x 280, w 1000 | yes |
| content column inside it (`px-8`) | 936 | 936 | yes |
| kitchen band | x 313, w 934, pad 24/24/12/16, bg `rgb(241,242,243)`, borders 1/1 | identical | yes |
| band classes | `border-y border-hairline bg-sunken px-6 pb-4 pt-3 last:-mb-6 last:border-b-0` | identical string | yes |
| band edges vs card interior | `-mx-6` reaches both card edges (313 → 1247) | identical | yes |
| heading `<h3>` | x 337, w 886, h 35, padding-bottom 8, border-bottom 1, column-gap 12 | identical | yes |
| heading kitchen name | 16px / 600, w 88.4 ("Main kitchen") | 16px / 600, w 89.4 ("Main Kitchen") | yes — the 1px is the real name's capital K |
| heading "3 preparations" | x 437.4, w 86.2, 14px | x 438.4, w 86.2, 14px | yes |
| dish row | x 337, w 886, h 50, py 12/12, column-gap 12, `flex flex-wrap items-center gap-3 border-t border-hairline py-3 first:border-t-0` | identical | yes |
| dish quantity | right-aligned, 500 weight, 16px, right edge 1223 | identical | yes |
| People needed / rostered pebble | x 452.5, w 115, h 24, px 10, py 4, radius 5, bg `rgb(249,238,226)`, colour `rgb(128,93,48)`, 12px / 600, gap 6 | identical | yes |
| crew line | x 337, w 886, mt 8, pt 16, border-top 1, gap-x 24, gap-y 12, `items-end` | identical | yes |
| job-card controls block | x 946.6, w 276.4, h 44, gap 8 | identical | yes |
| "Include the recipes" label | x 946.6, w 133.6, h 44, min-height 44 | identical | yes |
| Download job card button | x 1088.2, w 134.8, h 36, min-height 36, px 12, 14px, radius 5 | identical | yes |
| week tile | `grid gap-3 md:grid-cols-4 xl:grid-cols-7`, `rounded-2xl border border-hairline p-4 …` | identical class string; 7 × 123.43px, gap 12, x 312 → 1248 | yes |

Every class string on every one of these elements is byte-identical to the mock's. **Nothing was off
at 1280, so nothing was changed.**

### 1024

| Element | Mock | App |
|---|---|---|
| `<main>` | x 280, w 744 | x 280, w 744 |
| band | x 313, w 678, pad 24/24/12/16, borders 1/1 | identical |
| heading | x 337, w 630, pb 8, gap 12 | identical |
| dish row | x 337, w 630, py 12/12, gap 12; quantity right edge 967 | identical |
| pebble | x 452.5, w 115, h 24, radius 5, bg `rgb(249,238,226)` | identical |
| crew line | x 337, w 630, mt 8, pt 16, gap-x 24, gap-y 12 | identical |
| controls / button | controls x 690.6 w 276.4 h 44; button x 832.2 w 134.8 h 36 | identical |
| week tile | — | `md:grid-cols-4`, 4 × 161px, gap 12 |
| horizontal scroll | — | `scrollWidth 1024 === clientWidth 1024`, none |

Crew figures and job-card controls **stay side by side** at 1024: crew line top 763 height 65,
controls top 784 height 44, both bottoms at 828 (`items-end` bottom-aligns them). No new row is forced
on content that fits.

### 390

| Element | Mock | App |
|---|---|---|
| `<main>` | x 0, w 390 | x 0, w 390 |
| band | x 17, w 356, pad 24/24/12/16 | identical |
| heading | x 41, w 308, pb 8, gap 12 | identical |
| dish row | x 41, w 308, py 12/12, gap 12 | identical x/w; h 74 not 50, because the real dish names plus a "Short · order by 22 Sept" pill wrap where the mock's short names do not |
| dish quantity right edge | 349 | 349 |
| pebble | x 156.5, w 115, h 24 | identical |
| crew line | x 41, w 308, mt 8, pt 16 | identical x/w; h 121 not 141, because this meal has two rostered names and the mock's has three |
| controls / button | controls x 41 w 276.4 h 44; button x 182.6 w 134.8 h 36 | identical |
| side gutter | — | 17px to the band (page `p-4` = 16 + the card's 1px border); 41px to the text, symmetric (41 left, 390−349 right) |
| week tile | — | one column, 358px wide, 16px gutters |
| horizontal scroll | — | `scrollWidth 390 === clientWidth 390` on both the day and the week view, none |

Screenshot `T-359-390-day-two-kitchens.jpg`. At 390 the label and the button still sit side by side on
one line (41 → 317.4 of 390 available), which is what the layout rule asks for.

**Nothing measured off the mock at any of the three widths, and nothing in these surfaces was changed
for layout.** The only differences are where real data is longer or shorter than the mock's, and those
are differences in height, never in position or padding.

## 8. Found while driving — written up, not fixed

### A. Today hands a non-planning kitchen's staff seven links into a planner they cannot open

Signed in as Gopal Das (Kitchen Staff, Deity Kitchen, `canPlanMeals: false`), the menu correctly has
no **Meal planner**. `/today` nonetheless offers these, every one landing on *"Not your page"*:

```
[["Open planner","/planner"],
 ["Record them","/planner/catch-up"],
 ["Servings today 405 …","/planner"],
 ["Cost of materials ₹7,092 …","/planner"],
 ["07:30 Breakfast 97 servings · Ma…","/planner?date=2026-09-20"],
 ["12:00 Lunch 184 servings · Main …","/planner?date=2026-09-20"],
 ["19:30 Dinner 124 servings · Main…","/planner?date=2026-09-20"]]
```

`frontend/app/today/page.tsx` is inside my contract, so I could have changed it. I did not, for two
reasons. First, Epic 12's only edit to that file is the three lines adding kitchen names to a meal row
— the planner links are all older than this epic; what Epic 12 created is the first user who is signed
in and barred from the planner, which is what exposed them. Second, it is seven call sites plus tests,
and half the remedy properly lives in the refusal below, which is no longer mine; a half-gated Today
would be worse than an ungated one. `whoami.canPlanMeals` is already on the client, so the fix is
small once somebody owns both halves.

### B. The refusal is the generic one, not the kitchen one

`/planner` as that same person says *"Not your page — You don't have access to this part of the app.
Ask your temple administrator."* The API's own refusal for exactly this case is better and already
written: KMS-400183, *"Your kitchen doesn't plan its meals here, so the meal planner isn't open to
you"*, with *"If you have moved kitchens, ask a Temple Admin to change your kitchen on the Staff
page."* As it stands the person is told to ask the administrator with no idea what to ask for. This
lives in `frontend/components/RequireRole.tsx`, which was removed from my contract mid-run (the
menu-layout task took it), so it is reported rather than touched.

### C. The staff register's Name column is cramped, and Epic 12 made it slightly worse

Measured at 1280, six columns in 934px:

```
NAME 93 | CONTACT 180 | JOB 170 | KITCHEN 107 | ACCESS 124 | ACTIONS 260
```

"Gopal Das" wraps to two lines; "Karuna Murti Das" and "Nitai Chandra Das" to three (rows 106.5px
against 92.5px). Causation was measured, not guessed: hiding the Kitchen column in the live DOM takes
NAME from 93px to 105px and drops the three-line rows from two to one. So the register was already
wrapping names before Epic 12; the new column cost 12px and one more wrapped row. This is not dead
space — every column is carrying content — so it is a genuine width shortage in a shared ruled table,
and re-proportioning that table is bigger than the defect.

### D. Two different meanings wear the same shape in the composer

**+ Add another kitchen** lists `Govindas Bliss — 1 staff`, meaning *this kitchen has one staff
member*. The section it then adds says `Rostered in Govindas Bliss — 0 staff`, meaning *nobody from it
is rostered at this meal's time*. Both are right; both read as "N staff". Wording rather than
behaviour, so it is left for Rajeev.

## 9. Files changed, and the commands with their output

### Files changed

One file, and it is inside the contract:

```
backend/src/main/java/org/iskcon/kms/meal/MealCrewView.java   (javadoc only — the duplicated @param mealId block)
docs/work/proof/T-359.md                                      (this file)
```

Everything else in the worktree's diff is the dead run's and the rest of Epic 12's work, left as
found. In particular I did **not** touch `frontend/lib/api.ts`, `ErrorCode.java`,
`RolePermissions.java`, the migrations, `docs/CHANGELOG.md` or `docs/WORK_QUEUE.md`, and I did **not**
touch `frontend/lib/nav.ts`, `frontend/components/Sidebar.tsx`,
`frontend/components/RequireRole.tsx` or `frontend/__tests__/nav.test.ts`, which were taken out of my
contract mid-run by the menu-layout task. Nothing in the main checkout was written.

### Frontend tests — every file I re-ran, through the lock

`tools/work-lock.sh run verify …/t359-fe.sh`, which runs
`npx vitest run` over the eighteen files these surfaces own:

```
 ✓ __tests__/meal-correction.test.tsx  (11 tests) 455ms
 ✓ __tests__/meal-recording.test.tsx  (13 tests) 718ms
 ✓ __tests__/today.test.tsx  (39 tests) 1004ms
 ✓ __tests__/meal-composer-kitchens.test.tsx  (26 tests) 996ms
 ✓ __tests__/planner.test.tsx  (38 tests) 1144ms
 ✓ __tests__/my-schedule.test.tsx  (17 tests) 240ms
 ✓ __tests__/planner-day-routes.test.tsx  (36 tests) 1400ms
 ✓ __tests__/staff-kitchen.test.tsx  (19 tests) 449ms
 ✓ __tests__/planner-shift.test.tsx  (36 tests) 1563ms
 ✓ __tests__/nav.test.ts  (18 tests) 11ms
 ✓ __tests__/meal-kitchen-sections.test.tsx  (10 tests) 330ms
 ✓ __tests__/leave.test.tsx  (19 tests) 374ms
 ✓ __tests__/repeat-series.test.tsx  (26 tests) 2032ms
 ✓ __tests__/staff-ban.test.tsx  (15 tests) 329ms
 ✓ __tests__/staff.test.tsx  (13 tests) 177ms
 ✓ __tests__/crew-pebble.test.tsx  (7 tests) 128ms
 ✓ __tests__/staff-hire.test.tsx  (8 tests) 153ms
 ✓ __tests__/meal-composer.test.tsx  (65 tests) 4948ms

 Test Files  18 passed (18)
      Tests  416 passed (416)
   Start at  16:27:54
   Duration  6.01s (transform 1.20s, setup 1.09s, collect 4.12s, tests 16.45s, environment 5.06s, prepare 1.00s)

work-lock: 'verify' released; command exited 0
```

`repeat-series.test.tsx` (26) and `leave.test.tsx` (19) are the two files that carry the new
assertions for priorities 1 and 2.

### tsc and eslint, through the lock

```
=== tsc ===
tsc exit 0
=== eslint ===
eslint exit 0
work-lock: 'verify' released; command exited 0
```

Both silent, both zero. eslint was run as `npx eslint . --max-warnings=0`.

### Backend — `meal.MealCrew*` plus `KitchenCrewIT`, through the lock, on Testcontainers PostgreSQL

```
KitchenCrewIT > each kitchen is held against its own staff, the volunteers fall to the main kitchen, and the meal is the kitchens added up PASSED
KitchenCrewIT > with no main kitchen on the meal, the volunteers fall to the first kitchen the person sees PASSED
KitchenCrewIT > a Sweets cook sees the Sweets kitchen first, on the crew figures and on Today, and the volunteers stay with Main PASSED
KitchenCrewIT > a meal is short when one kitchen is, even where the sums would say it is covered PASSED
KitchenCrewIT > a day off costs only the kitchen the person works in PASSED
KitchenCrewIT > asked for one kitchen, the count before a save is that kitchen's staff, and its volunteers only when the caller says so PASSED
KitchenCrewIT > another temple's staff are never counted, even with a cook in all day PASSED
MealCrewDefaultTest > one meal gives itself PASSED
MealCrewDefaultTest > two meals give their mean, rounded up — being short is worse than being over PASSED
MealCrewDefaultTest > no meals give nothing at all, and the field opens empty PASSED
MealCrewDefaultTest > three or more meals give the middle value, not the last and not the mean PASSED
MealCrewIT > before the first save a volunteer counts through a shift not for a meal that covers the ready-by, never through another meal's shift PASSED
MealCrewIT > a person counts for a meal only if their working window covers its ready-by time PASSED
MealCrewIT > asking for a meal's count with no ready-by is a field to fill in, not our fault PASSED
MealCrewIT > the default is the median of the last three ordinary meals of that kind PASSED
MealCrewIT > a crew short of hands is read as short and nothing more — a meal is planned weeks before anybody is rostered PASSED
MealCrewIT > a volunteer, who cannot plan meals, is refused the count for a meal not saved yet PASSED
MealCrewIT > a volunteer shift falls to the meal it covers, without anybody linking it to one PASSED
MealCrewIT > a meal nobody has crewed is not short of anything — null is not zero PASSED
MealCrewIT > a festival lunch never sets the default for an ordinary one PASSED
MealCrewIT > Today reads the meals apart, not one figure for the whole day PASSED
MealCrewIT > an approver is told what a day off costs each meal, and is not stopped PASSED
MealCrewIT > a meal not saved yet is counted at its date and ready-by, reads the same once saved, and asking writes nothing PASSED

────────────────────────────────────────────────────────
  Test summary
────────────────────────────────────────────────────────
  Total:    23
  Passed:   23
  Failed:   0
  Skipped:  0
  Result:   SUCCESS
────────────────────────────────────────────────────────

BUILD SUCCESSFUL in 16s
5 actionable tasks: 3 executed, 2 up-to-date
work-lock: 'verify' released; command exited 0
```

### Screenshots, in the scratchpad

```
T-359-1280-day-two-kitchens.jpg          the two-kitchen Lunch at 1280, Temple Admin, Main Kitchen first
T-359-390-day-two-kitchens.jpg           the same meal at 390
T-359-1280-week-view.jpg                 the week, Thursday 24 grouped by kitchen and Tuesday 22 not
T-359-series-of-one-plain-cancel.jpg     the survivor's plain cancel — no scope question
T-359-own-kitchen-first-sweets-manager.jpg   the same Lunch as the Govindas Bliss manager, their kitchen first
T-359-compose-add-another-kitchen.jpg    + Add another kitchen offering only the planning kitchens
T-359-kitchen-assignment-checks.jpg      the Temple Admin's "Check these kitchen assignments"
```

Also in the scratchpad: `flyway-kms_e12.txt`, `t359-card-main.html`, `t359-card-sweets.html`,
`t359-vitest.log`, `t359-backend.log`, and the scripts `t359-mint.sh`, `t359-roles.sh`,
`t359-cards.sh`.

## 10. Not done, and what needs Rajeev

- **The Volunteer was not driven in the browser**, only at the API (HTTP 403, KMS-400021). Time.
- **The Kitchen Staff member in a planner kitchen** (`ikms.kitchen-staff.2`, Main Kitchen) was driven
  at the API only. The Kitchen *Manager* in the second planner kitchen was driven in the browser, and
  that is the case that actually tests "your own kitchen first", so the gap is narrow.
- **Three items are written up in §8 rather than fixed** — Today's seven dead links for a non-planning
  kitchen's staff (A), the generic instead of the kitchen-specific refusal (B, now outside my
  contract), and the staff register's cramped Name column (C). A and B together are one small change
  that needs one owner across `app/today/page.tsx` and `components/RequireRole.tsx`.
- **The nav/menu was not measured**, by instruction: `nav.ts`, `Sidebar.tsx`, `RequireRole.tsx` and
  `nav.test.ts` were removed from my contract mid-run, and the menu was visibly changing under me
  (compare the sidebar in the earlier and later screenshots). What I did check there — that a
  non-planning kitchen's staff get no **Meal planner** item — was reported to the coordinator, not
  fixed.
- **The browser's `localhost:3100` session was swapped** to two test accounts to drive the roles and
  then put back to `ikms.temple-admin.1`, verified by reading `whoami` back: `TEMPLE_ADMIN / Karuna
  Murti Das / Main Kitchen`. No password was used at any point; the tokens were minted out of band
  from the admin SDK service account. Rajeev's session on `:3100` is signed in and working; the
  minted token refreshes normally.
- **Seed changes made and undone**: two staff were re-flagged `kitchen_needs_check` to see the Temple
  Admin's panel, then set back to false. A two-kitchen Breakfast was composed on 25 September and
  left in place as evidence. All of it is in `kms_e12`, which is dropped at the end.
- **The measurement method is worth knowing about**: the display is 1470 CSS px and the window is
  fullscreen, so 1280/1024/390 were measured in same-origin iframes of exactly those widths rather
  than by resizing. `resize_window` reports success and changes nothing — I checked `innerWidth`
  after it and it was still 1470. If Rajeev wants these widths measured in a real window, the window
  has to come out of fullscreen first.

## 11. The shared stack was not touched — and my stack is still up, deliberately

### `:3000`, `:8080`, `kms`, `kms_verify` — all as they were

```
=== listeners ===
java 52556 *:8080          <- not mine, not touched
node 56394 *:3000          <- not mine, not touched
node 73324 *:3100          <- mine (the dead run's, kept)
java 91496 *:8180          <- mine (the dead run's, kept)

3000 -> HTTP 200
{"status":"UP"} <- 8080 health
```

The migrations only ever ran against the copy. Proof, from the three databases' own Flyway tables:

```
kms         → 144 | procurement data model | 2026-09-19 01:50:43   (V150/151/152 never applied)
kms_verify  → 149 | meal series            | 2026-09-19 11:48:52   (V150/151/152 never applied)
kms_e12     → 152 | card version is per kitchen | 2026-09-19 15:14:58
```

`kms_verify` was read only (as permitted) and its last migration predates this run by three hours.
Nothing was written to either.

### `kms_e12` and my two servers were NOT torn down

The brief says to stop :8180 and :3100 and `DROP DATABASE kms_e12` at the end. I have not, on purpose:
at 17:07 the coordinating session said Rajeev wanted to look at this stack and asked me not to restart
either server without telling it first. Tearing the stack down would have taken the thing he was
looking at out from under him, and the teardown is three commands anybody can run in a minute.

So the stack is left up and coherent, and the teardown is handed over:

```
kill 91496 73324
docker exec kms-postgres psql -U kms -d postgres -c 'DROP DATABASE kms_e12'
```

`kms_e12` holds nothing that is needed: it is a copy of `kms_verify` plus the three Epic 12
migrations, and everything it proved is quoted in this file.

Three things worth seeing before it goes:

```
http://localhost:3100/planner?date=2026-09-24   the two-kitchen Lunch (Main Kitchen + Govindas Bliss)
http://localhost:3100/planner?date=2026-09-25   the two-kitchen Breakfast composed through the UI in §6
http://localhost:3100/planner?date=2026-10-01   the series-of-one, "T-359 Series Check"
```

signed in as `ikms.temple-admin.1@trading4good.org`, which is the session the browser is on.


