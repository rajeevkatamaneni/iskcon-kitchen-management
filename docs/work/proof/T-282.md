# T-282 — tests now say Kitchen Staff may receive deliveries (Rajeev's answer to Q-1, 2026-09-19)

**Files changed (all inside the contract):**

- `backend/src/test/java/org/iskcon/kms/auth/RolePermissionsTest.java`: `denied(KITCHEN_STAFF, RECEIVE_DELIVERIES)` is now `allowed(...)`, and the comment records his answer and the date. The Volunteer and Super Admin refusals are unchanged.
- `backend/src/test/java/org/iskcon/kms/receiving/DeliveriesIT.java`: the test that expected Kitchen Staff to get 403 is replaced by `kitchenStaffReceiveDeliveries`. As Kitchen Staff it does GET /api/v1/deliveries (200), GET /deliveries/received (200), POST /api/v1/deliveries (201) and POST /api/v1/purchase-orders/{id}/receipts (201, `receivedByName` = "Staff A"). It then checks that stock moved, both orders are RECEIVED and the screen names the cook. There is a new test, `aVolunteerIsRefused`: a Volunteer user gets 403 on GET and POST /deliveries, on older deliveries, and on GET and POST receipts, and nothing is written. The class comment is updated.
- `backend/src/test/java/org/iskcon/kms/receiving/ReceivingIT.java`: comment only.
- `backend/src/test/java/org/iskcon/kms/receiving/ReturnToVendorIT.java`: comment only.
- `backend/src/test/java/org/iskcon/kms/purchaseorder/DescribedPurchaseLineIT.java`: comment only.
- `frontend/__tests__/nav.test.ts`: Deliveries now sits straight after Purchase orders for Temple Admin, Kitchen Manager and Kitchen Staff. It is still absent for Volunteer and Super Admin.
- `docs/work/proof/T-282.md`: this file.

`RolePermissions.java` and `nav.ts` were only changed temporarily for the control run and then restored byte for byte. The checksums are below.

**Commands run and what came back:**

Everything below ran under `tools/work-lock.sh run verify` with JAVA_HOME set to JDK 21. The script is `scratchpad/t282b.sh`.

Green run: my classes, AccessControlEnforcementIT, and the repo-wide guards from lesson 3a of README.md.
```
./gradlew test --tests '*RolePermissionsTest' --tests '*DeliveriesIT' --tests '*ReceivingIT' --tests '*ReturnToVendorIT' --tests '*DescribedPurchaseLineIT' --tests '*AccessControlEnforcementIT' --tests '*BaseQuantityIT' --tests '*UnitLabelAgreementTest' --tests '*TempleClockTest' --tests '*CommunicationSendGuardSourceTest' --tests '*ErrorCodeTest' --tests '*FieldErrorMessageTest' --tests '*NextStepPermissionTest' --tests '*RowLevelSecurityIT' --tests '*TenantLoopMigrationIT'
exit 0
BUILD SUCCESSFUL in 27s
TenantLoopMigrationIT: tests=1 failures=0 errors=0 skipped=0
AccessControlEnforcementIT: tests=10 failures=0 errors=0 skipped=0
RolePermissionsTest: tests=90 failures=0 errors=0 skipped=0
CommunicationSendGuardSourceTest: tests=2 failures=0 errors=0 skipped=0
ErrorCodeTest: tests=887 failures=0 errors=0 skipped=0
FieldErrorMessageTest: tests=6 failures=0 errors=0 skipped=0
NextStepPermissionTest: tests=6 failures=0 errors=0 skipped=0
UnitLabelAgreementTest: tests=2 failures=0 errors=0 skipped=0
BaseQuantityIT: tests=7 failures=0 errors=0 skipped=0
DescribedPurchaseLineIT: tests=13 failures=0 errors=0 skipped=0
DeliveriesIT: tests=12 failures=0 errors=0 skipped=0
ReceivingIT: tests=10 failures=0 errors=0 skipped=0
ReturnToVendorIT: tests=8 failures=0 errors=0 skipped=0
ProcurementRowLevelSecurityIT: tests=6 failures=0 errors=0 skipped=0
RowLevelSecurityIT: tests=30 failures=0 errors=0 skipped=0
TempleClockTest: tests=1 failures=0 errors=0 skipped=0
```
The `*RowLevelSecurityIT` pattern also picked up ProcurementRowLevelSecurityIT.

Control: I deleted the `RECEIVE_DELIVERIES,` line from the KITCHEN_STAFF block of RolePermissions.java. A shell `trap` put the file back.
```
./gradlew test --tests '*RolePermissionsTest' --tests '*DeliveriesIT'
exit 1
102 tests completed, 3 failed
RolePermissionsTest: tests=90 failures=1
   FAILED: KITCHEN_STAFF may RECEIVE_DELIVERIES
DeliveriesIT: tests=12 failures=2
   FAILED: Kitchen Staff (Q-1, answered 2026-09-19) read /deliveries and record a delivery on both routes
   FAILED: a Volunteer gets 403 on GET and POST /deliveries, older deliveries, and GET and POST receipts
=== RESTORED
acc04a965b1fa1477065488ed3a5e18587f68f3c5e2a0f8506c9f9765fa1f2c7  scratchpad/RolePermissions.java.orig2
acc04a965b1fa1477065488ed3a5e18587f68f3c5e2a0f8506c9f9765fa1f2c7  backend/src/main/java/org/iskcon/kms/auth/RolePermissions.java
byte-identical
```
The Volunteer test failing here is expected. Its last lines check that Kitchen Staff can open the screen.

Frontend, from the first run under the lock (`scratchpad/t282.sh`).
```
npx vitest run __tests__/nav.test.ts
 Test Files  1 passed (1)
      Tests  16 passed (16)
```
Control: I removed `KITCHEN` from the Deliveries item's roles in nav.ts, then restored it.
```
   × navForRole > puts Deliveries straight after Purchase orders, for the admin, the manager and kitchen staff
      Tests  1 failed | 15 passed (16)
d61c4a0067883df0c5cc40d7221faa9a84994ca6fba2606a068cfa5a5d291f6e  scratchpad/nav.ts.orig
d61c4a0067883df0c5cc40d7221faa9a84994ca6fba2606a068cfa5a5d291f6e  frontend/lib/nav.ts
byte-identical
```
The same run restored RolePermissions.java and its checksum matched. It could not compile the backend at that point because of the broken `RecipeImportService.java` described below.

`npx tsc --noEmit` exits 2 for the tree as a whole. None of its errors are in my files:
```
npx tsc --noEmit 2>&1 | grep -c "__tests__/nav.test.ts"
0
```
The errors come from two places that belong to other tasks. One is stale `.next/types/app/dev-{deliveries,invoices,po}` entries. The other is `__tests__/deliveries.test.tsx` (lines 109–111, 137, 289), where `returns` is missing from `DeliveryReceiptLineView`. That file belongs to T-285/T-289.

The first two backend runs failed to compile. The cause was T-287 editing `library/RecipeImportService.java` at the same time (`cannot find symbol LinePlan`, then an inference error at line 91). I waited until that file had not changed for 3 minutes and ran again. That third run is the green one above.

**Acceptance criteria:**
- The contract tests are green: see the green run and the vitest summary above.
- A Kitchen Staff user gets 200 on GET /api/v1/deliveries and records a delivery on POST /api/v1/deliveries and on the PO receipts endpoint: `DeliveriesIT.kitchenStaffReceiveDeliveries` is green.
- The nav shows Deliveries to KITCHEN_STAFF: `nav.test.ts` is green.
- Volunteer and Super Admin are still refused: `RolePermissionsTest` rows for both, plus the Volunteer HTTP 403s in `DeliveriesIT.aVolunteerIsRefused`.
- The control went red and the files were restored byte-identical: checksums above.
- AccessControlEnforcementIT and the repo-wide guards from lesson 3a are green: see the green run.
- The stale Q-1 comments are updated with his decision and the date. `grep -rn "Q-1" backend/src/test frontend/__tests__` shows no open-question wording left in contract files.

**Not done / needs Rajeev:**
- Two tests outside my contract still assert the refusal. I did not edit either:
  - `frontend/__tests__/po-merged-table.test.tsx:225` ("does not show the link to Kitchen Staff, who cannot record a delivery while Q-1 is open")
  - `frontend/__tests__/deliveries.test.tsx:174` ("refuses Kitchen Staff, who are not given RECEIVE_DELIVERIES yet (Q-1)…")

  Both belong to T-285/T-289.
- `docs/work/PROCUREMENT-REQUIREMENTS.md` still lists Q-1 as open (lines 87, 258, 462), but RolePermissions.java now cites §12 of that file for the answer. It is outside my contract, so the conductor should record the answer there.
- The live API and screen check (Kitchen Staff sees Deliveries and records a delivery) waits on a restart and on T-285's page guard.
