# T-286 — a detail page for every ingredient: facts, pack sizes, market rate, vendors

**Files changed** (all inside the contract):
- `frontend/app/ingredients/[id]/page.tsx` (new)
- `frontend/components/ingredient/access.tsx` (new): permission to role map, one place
- `frontend/components/ingredient/supply.tsx` (new): the vendor page's supply helpers, copied (see below)
- `frontend/components/ingredient/IngredientFacts.tsx`, `PackSizes.tsx`, `MarketRate.tsx`, `IngredientVendors.tsx` (new)
- `frontend/app/ingredients/page.tsx`: the name is now a `Link` to `/ingredients/{id}` (+1 import, +6 lines; nothing else touched)
- `frontend/__tests__/ingredient-detail.test.tsx` (new, 28 tests), `frontend/__tests__/ingredients.test.tsx` (+1 test for the link)
- `docs/work/proof/T-286.md`

Not touched: backend, `lib/api.ts`, `ds/table.ts`, the vendor page. I read `api.ts` signatures by grep only.

**Commands run and what came back**

```
tools/work-lock.sh run verify 'cd frontend && npx tsc --noEmit ... | grep -v dev-*/deliveries.test ; npx eslint <touched files>; npx vitest run __tests__/ingredient-detail.test.tsx __tests__/ingredients.test.tsx __tests__/preferred-replaces.test.tsx'

components/ds/table.ts(484,174): error TS2783: 'min' is specified more than once, so this usage will be overwritten.
components/ds/table.ts(484,179): error TS2783: 'flexible' is specified more than once, so this usage will be overwritten.
TSC_EXIT=2
/Users/Rajeev/Workspace/kitchen-management-system/frontend/app/ingredients/page.tsx
  196:5  warning  Unused eslint-disable directive (no problems were reported from 'react-hooks/exhaustive-deps')
✖ 1 problem (0 errors, 1 warning)
ESLINT_EXIT=0
 Test Files  3 passed (3)
      Tests  68 passed (68)
```
tsc: no error in any T-286 file. The two remaining are in `ds/table.ts`, which another builder is editing right now (an earlier run, before that edit, showed only the known `.next/types/app/dev-*` and `__tests__/deliveries.test.tsx` errors). The eslint warning is on line 196 of `/ingredients`, an `eslint-disable` that is not in HEAD and is not mine (the uncommitted `?edit=` work); my edit there is the Link only.

Negative control (same locked run as the first green pass):
```
=== NEGATIVE CONTROL
ce26fcbf6b2b6b5cf791812ce69d2cc3a447fa5c  components/ingredient/supply.tsx
65:    pricePerPack: null,          <- supplyInput made to drop the per-pack price
   × vendors (R-ING-2, R-VEN-2, R-VEN-3) > links a vendor with the same body the vendor page's Other ingredients sends, to the same endpoint
      Tests  1 failed | 27 passed (28)
ce26fcbf6b2b6b5cf791812ce69d2cc3a447fa5c  components/ingredient/supply.tsx   <- restored, byte-identical
      Tests  28 passed (28)
```

**Acceptance criteria**
- Chips render with the server's labels ("250 gm · 500 gm · 1 Kg · Bag = 25 Kg"): test "renders the server's chip labels". Live: added "Bag = 25 Kg" and "500 gm" on Amla; chips read `["500 gm","Bag = 25 Kg"]` from the DOM.
- Add/remove and server errors shown: tests for add body, blank size, duplicate (KMS-400157), remove, in-use refusal (KMS-400159). Live: adding 25 Kg a second time showed "This ingredient already has a pack of that size." with KMS-400157.
- Hint text exact: test compares the constant and the tooltip text to "The pack sizes vendors sell this in. The shopping list suggests whole packs."
- Market rate shown and edit gated: tests for "Market rate · ₹60 / Kg · 12 Sept", per-gram stored rate shown per Kg and sent back per gram (typed 64 → sent 0.064), nought refused, server refusal shown, no button when `canEdit` false. Live: set ₹60 on Amla, line reads "Market rate · ₹60 / Kg · 19 Sept", "Typed in by hand".
- Vendor link posts the vendor page's body: test asserts `addVendorSupplies("v-b", [{ingredientId, lastPrice:null, leadTimeDays:0, preferred:false, packSizeId:"bag", pricePerPack:1450}])` with exactly those six keys; loose price sends `lastPrice` with both pack keys null. Edit row sends `setVendorSupply` with the six keys, as the vendor page's Edit. Live: linked VERIFY-C Traders, Bag = 25 Kg, ₹1,500, 2 days; the row reads "₹1,500 / bag · ₹60 / Kg", "2 days".
- "Preferred (replaces A)": tests in the link form and the edit row, neutral colour, nothing saved. Live: ticked it, read "Preferred (replaces Kalasipalya Vegetable Mandi)", unticked before saving.
- Arrows: red up, green down, nothing without a previous price (tests use `PriceTrend` as the vendor page does).
- Measured at 1280 (innerWidth 1280): document scrollWidth 1280, main 1000/1000, nothing past main's right edge, no ellipsis, no word split across lines, every select wider than its longest option (169/443, 67/112, 19/63). Preferred ticked: first line of the link form unmoved (same left/top/width for all four boxes), tick+button 389px on the second line.
- Measured at 390 (a 390px iframe of the same page, since the window would not go below 686 outer): document scrollWidth 390, main 390/390, nothing outside 0..390, no ellipsis; the only "split word" is "Vendor" inside the table's 1px visually-hidden thead in card layout (the shared table CSS, not visible).

**Sent back once (design-system.test.ts, 2 checks)** — fixed without touching the test:
- T-167 "every form is the shared Form": the three plain `<form>`s (IngredientVendors, MarketRate, PackSizes) are now `Form` from `components/ds/Form`.
- Item 23 "no hand-typed spacer": the `&nbsp;` label spacers in the link form are gone; the Preferred tick and Link vendor button now sit on their own line under the four boxes (at 1280 they were already wrapping to a second line).

```
tools/work-lock.sh run verify 'cd frontend && npx vitest run __tests__/design-system.test.ts __tests__/ingredient-detail.test.tsx __tests__/ingredients.test.tsx __tests__/preferred-replaces.test.tsx ...; npx tsc --noEmit | grep -E "ingredient/|ingredients/"; npx eslint <my files>'
 ✓ __tests__/preferred-replaces.test.tsx  (7 tests) 356ms
 ✓ __tests__/ingredients.test.tsx  (33 tests) 404ms
 ✓ __tests__/design-system.test.ts  (23 tests) 248ms
 ✓ __tests__/ingredient-detail.test.tsx  (28 tests) 606ms
 Test Files  4 passed (4)
      Tests  91 passed (91)
TSC_MINE_DONE          <- no tsc error in any T-286 file
ESLINT_EXIT=0
```
Before the fix, the same design-system run: `Tests  2 failed | 21 passed (23)`, naming IngredientVendors.tsx (spacer) and IngredientVendors.tsx:247, MarketRate.tsx:116, PackSizes.tsx:130 (plain form).
Re-measured live after the fix (the window would not resize this time, so at innerWidth 1470): document scrollWidth 1470, main 1190/1190; the four boxes on one line (top 647), tick and button on the next (top 731); `form.noValidate` true.

**Decisions made with the clarifier (conductor's rulings, 2026-09-19)**: the dropdown lists active vendors who don't already supply the ingredient; existing links are edited in the table (so the table has Edit); "Sells it as" with the price per pack is included, same labels as the vendor page.

**Facts no other screen showed (grep of app/ and components/, 2026-09-19)**: `aliases` for food (only inside `/ingredients`' edit box; `/supplies` does print them), `createdAt` (nowhere), `marketRate`/`marketRateOn`/`marketRateSource` (nowhere), `packSizes` as chips (only as select options on the vendor page and invoice). Shown here as Other names (merged names are stored as aliases, so they appear there too; they are not distinguishable from typed aliases in `IngredientView`), Added on, the market rate line and its source. `supply` is not printed (Rajeev's 2026-09-10 ruling); it only picks the back link.

**Price history**: R-VEN-3 asks for arrows only, no list, so nothing was added. The chart's place is the "Price" card under the market rate (comment in the page and the card).

**Local data created** (via the app as Temple Admin, on Amla (gooseberry) `3bf79531-…`): pack sizes "Bag = 25 Kg" and "500 gm"; market rate ₹60/Kg (MANUAL); a supply VERIFY-C Traders → Amla, Bag = 25 Kg, ₹1,500/bag, 2 days, not preferred. One add failed with KMS-0000 because the API was restarting (DevTools reload from another builder's Java save at 08:22:34); retried and it saved.

**Screenshots for Rajeev** (no mock exists; all retaken after the last layout change, older ones deleted), absolute folder `/private/tmp/claude-501/-Users-Rajeev-Workspace-kitchen-management-system/79bf5e51-4f9e-420c-b419-747d070a92df/scratchpad/T-286-shots/`: `1280-1-top.jpg`, `1280-2-vendors-preferred-ticked.jpg`, `390-1-top.png`, `390-2-price-vendors.png`, `390-3-link-form-ticked.png`.

**Not done / needs Rajeev**
- The frontend knows roles, not permissions, so `access.tsx` maps each permission to the roles that hold it. Today all three kitchen roles hold MANAGE_RECIPES, MANAGE_INVENTORY and MANAGE_VENDORS (from `RolePermissionsTest` and the guards on `/ingredients`, `/inventory`, `/vendors`; I did not open `RolePermissions.java`), so every control shows for all three. If a grant moves, that file must move with it.
- `supply.tsx` copies the vendor page's helpers because a `page.tsx` can't export them and the vendor page was out of contract. Follow-up: make the vendor page import from `components/ingredient/supply.tsx`.
- On a phone card, a non-preferred row's Preferred cell prints a lone "—" with no label. The vendor page does the same; I kept them matching rather than fix one.
- Hand smoke-test done as Temple Admin only; not driven as Kitchen Manager or Kitchen Staff.

## Re-measure after the last layout change (2026-09-19, final)

How: Chrome's port 9222 was not listening (`curl 127.0.0.1:9222/json/version` → exit 7), so no CDP `Emulation.setDeviceMetricsOverride`. 1280 was the tab's own viewport (`innerWidth` 1280). 390 was measured in a 390px-wide iframe of the same page (`contentWindow.innerWidth` 390), which is a real layout viewport, so media queries and the table's card layout apply. Script checked: document and `main` scrollWidth vs clientWidth; any element past 0..innerWidth; any element whose content is wider than it with overflow not visible; any `text-overflow: ellipsis`; any word whose glyphs land on more than one line; each select's longest option vs its content width. Visually-hidden text (`sr-only`, the card layout's 1px thead) excluded.

1280, as loaded: `{"iw":1280,"docSW":1280,"docCW":1280,"main":[1000,1000],"clipped":[],"broken":[],"overflow":[],"selects":["19/39","169/419","67/88"]}`. Link form: four boxes on one line at top 1125, widths 445+114+144+136 with three 16px gaps = 887 in an 886px row (no spare width); tick and button on the next line (top 1210).
1280, Preferred ticked + market-rate editor open + a vendor row in edit: same result, `clipped [] broken [] overflow []`; boxes unmoved; tick+button 252+113 on line two.
390, as loaded: `{"iw":390,"docSW":390,"docCW":390,"main":[390,390],"clipped":[],"broken":[],"overflow":[],"selects":["19/39","169/282","67/88"]}`.
390, Preferred ticked: identical, button drops under the two-line "Preferred (replaces Kalasipalya Vegetable Mandi)".
390, market-rate editor and a vendor edit row open: `clipped [] broken [] overflow []`, selects all wider than their longest option.

Edge cases, decided by the rule's logic (Rajeev's Q-18 answer: readable, nothing hidden, no dead space) and not escalated:
- 1280 vendor edit row: the Vendor name column wraps to two or three lines while the boxes take their width. Kept: the name is the one column allowed to wrap, nothing is cut, and it matches the vendor page's edit row (same table classes).
- 390 link form: Lead time (days) wraps alone onto its own line with space beside it. Kept: it genuinely doesn't fit beside Sells it as + List price (114+144+136+32 = 426 > 308), and stretching a number box to fill a line would make a 3-digit field 308px wide.
- 390 vendor edit card: the Preferred tick lines up with the Lead time label, not its box. Kept: this is the shared entry-row card CSS the vendor page uses; changing it is outside this task.
- 390 read-only card: a non-preferred row prints a lone "—" for Preferred with no label (as the vendor page does). Kept for consistency across views; noted above.
