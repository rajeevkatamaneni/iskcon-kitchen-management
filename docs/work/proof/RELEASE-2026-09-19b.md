# Release 2026-09-19b — the procurement rework and repeating events, T-242 to T-343

Staging only. Rajeev approved the deploy in the terminal: *"deploy to staging : Go..!!"* (14:30 PDT).
Facts below are from `docs/work/PROCUREMENT-PROGRESS.md`'s deploy entry, plus what this agent read
back from Cloud Run, the API's own log and the live URLs afterwards.

## Commits (straight to main)
`2cc4d1c` buttons and idle sign-out · `41dc288` procurement data model, duplicates, merge,
ingredients, vendors and prices (V144, V145, V147) · `1116310` shopping list and purchase orders
(V146) with the shared sheet formatters · `68601ce` deliveries · `0e26456` invoices, payments and
attachments (V148) · `ed86d41` permission-first check and shared document dates · `9633b02` the
procurement screens · `81310e0` design system v1.14 · `30c4020` repeating events (V149) ·
`97b8a2b` docs, proofs and DISPATCH. Deployed commit: **`97b8a2b`**.

The backend is split by area and the frontend is one commit: `lib/api.ts` gains required fields that
every area's test fixtures rely on, so a per-area frontend split gives commits that do not
type-check. Each commit was compiled before it was made (`compileJava compileTestJava`,
`tsc --noEmit`).

## Gate on the committed tree
`git archive HEAD` (97b8a2b) into `/tmp/kms-verify`, then `git init && git add -A`:
- backend `./gradlew test`: **3,443 tests, 0 failures, 0 errors, 7 skipped**.
- frontend `npm ci`, `tsc --noEmit` 0, `npm run lint` 0, `vitest run` **179 files / 2,515 tests**,
  `npm run build` 0.
- `tools/check-ignored-sources.sh` and `tools/check-tfvars-example.sh` both pass in that directory:
  no source file is hidden by an ignore rule (`frontend/app/dev-*` is excluded from the commit and
  from the archive).

One test was fixed at this gate: `OneTimeDonationIT`'s "every recurring-donation endpoint is gone"
answered 403 rather than 404 once T-301 checks permissions before a request is read, because
`GET /donations/recurring` meets `/donations/{donationId}` and a volunteer may not read it. The test
signs in as a Temple Admin now. No product code changed.

## CI
https://github.com/rajeevkatamaneni/iskcon-kitchen-management/actions/runs/35467456223 — **success**
on `97b8a2b`. Repository, Backend (Spring Boot) and Frontend (Next.js) all green.

## Backup (taken before the deploy)
On-demand Cloud SQL backup of instance `kms-staging-5325bd0d`, description
*"before V144-V149 procurement release 97b8a2b"*. `gcloud sql backups create` reported
`…/instances/kms-staging-5325bd0d] backed up.`

**Its id is not recorded here, and that is a gap.** `gcloud sql backups list` was refused by the
permission classifier, so the id could not be read. Anyone restoring must list the backups and pick
the one with that description, taken 2026-09-19 shortly before 14:41 PDT.

## Deploy
`infra/deploy.sh iskcon-kms-2026 staging` from a clean tree at `97b8a2b`. It was run by the
conductor, not by this agent: the release lock and the deploy were both refused to it by the
permission classifier. No other release was running; that was checked by hand rather than by the
lock, which is weaker than the lock and is written down here for that reason.

Revisions after the deploy, each at 100% traffic, digests read back from Cloud Run:
- api `kms-staging-api-00170-c62` — `sha256:3358b1906868b915ee25793b7430a52565f106058183a2b4e43c73182ea1559e`
- worker `kms-staging-worker-00152-z26` — same api image, `sha256:3358b190…1559e`
- web `kms-staging-web-00158-m8q` — `sha256:8f971ec337944699c2ac17a34a8c0c9adfa6be89f7e53ba996eb389527a1cfb9`

Both digests differ from the previous release's (api `sha256:c545e8a3…d67b`, web
`sha256:f873ec32…b38a`, recorded in `RELEASE-2026-09-19.md`), so the images really changed rather
than the revisions being re-pointed at the old ones.

## Migration (API log, api-00170-c62)
```
Successfully validated 143 migrations (execution time 00:00.290s)
Successfully applied 6 migrations to schema "public", now at version v149 (execution time 00:00.735s)
```
V144 to V149 applied: the procurement data model, the vendor price-history backfill, pack lines and
four-decimal prices on purchase order lines, ingredient merge, invoice lines, and meal series.

## Health, read after the deploy
- `https://kms-staging-api-bnpkv5hfrq-el.a.run.app/actuator/health` → `{"status":"UP"}`
- `https://kms-staging-web-bnpkv5hfrq-el.a.run.app/sign-in` → HTTP 200

## Not done
- **Nobody has driven the new screens on staging.** The independent recheck
  (`VERIFY3-RECHECK.md`, 5 of 5 plus a delivery recorded end to end) was on the local stack, not
  this deployment. UAT-092 Part B is the staging run, and it is Rajeev's test.
- The merge tool must **not** be run on staging data: Rajeev's Desk answers Q-20 and Q-21 put the
  catalogue cleanup and the day-one reset after this release
  (`docs/work/CATALOGUE-AND-DAY1-PLAN.md`).
- Nothing was removed from `docs/OUTSTANDING_BUILD_LIST.md`.
