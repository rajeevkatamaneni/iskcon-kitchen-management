# T-204 — the Give page's "Or another amount" box shows no spinner arrows

**Files changed** (all inside the contract):

- `frontend/components/give/DonatePage.tsx`: three classes added to the number input's className,
  `[appearance:textfield] [&::-webkit-inner-spin-button]:appearance-none [&::-webkit-outer-spin-button]:appearance-none`,
  and a comment saying why. `type="number" min="1" step="1" inputMode="numeric"` untouched.
- `frontend/__tests__/donate-checkout.test.tsx`: one new test, "hides the spinner arrows without
  giving up the number box (T-204)".
- `docs/work/proof/T-204.md`: this file.
- `frontend/__tests__/donate-signed-in.test.tsx`: run only, not edited.

Not opened: globals.css, lib/api.ts, ErrorCode.java, nav.ts, routes.ts.

design-system.test.ts checked by reading its rules. None of them look at arbitrary variants,
arbitrary properties or `appearance`. The only arbitrary-value rule is `tracking-[<number>]`. It
passed in both full runs (no FAIL line for it below).

**Commands run and what came back**

Everything ran through `tools/work-lock.sh run verify '<script>'`, backgrounded. Logs:
`scratchpad/t204-full.log` (negative control, Give tests, full verify) and
`scratchpad/t204-rerun.log` (the one rerun).

Negative control. The trap copied DonatePage.tsx aside, removed the three classes with sed,
restored the file on EXIT and checked it with cmp:

```
class lines remaining after trap edit: 0
Expected the element to have class:
  [&::-webkit-inner-spin-button]:appearance-none [&::-webkit-outer-spin-button]:appearance-none [appearance:textfield]
Received:
  min-h-touch w-full rounded-control border border-hairline px-3 pr-8 text-ink tabular-nums
 FAIL  __tests__/donate-checkout.test.tsx > the amount, under Form (T-166) > hides the spinner arrows without giving up the number box (T-204)
 Test Files  1 failed (1)
      Tests  1 failed | 9 passed (10)
negative-control vitest exit: 1
cmp: restored file identical to pre-trap copy
```

Give tests, restored file (`npx vitest run __tests__/donate-checkout.test.tsx __tests__/donate-signed-in.test.tsx`):

```
 ✓ __tests__/donate-checkout.test.tsx  (10 tests) 142ms
 ✓ __tests__/donate-signed-in.test.tsx  (11 tests) 148ms
 Test Files  2 passed (2)
      Tests  21 passed (21)
```

Full verify, first run (`npx tsc --noEmit && npm test && npm run lint && npx next build`, each
step run even if an earlier one failed, so every tail is real):

```
tsc exit: 0
 ❯ __tests__/communications.test.tsx:381:44
    381|     await waitFor(() => expect(createMock).toHaveBeenCalled());
 Test Files  1 failed | 138 passed (139)
      Tests  2 failed | 1879 passed (1881)
npm test exit: 1
lint exit: 0
next build exit: 0
compiled CSS containing the rule: .next/static/css/b4c7783412633930.css
.\[\&\:\:-webkit-inner-spin-button\]\:appearance-none::-webkit-inner-spin-button,.\[\&\:\:-webkit-outer-spin-button\]\:appearance-none::-webkit-outer-spin-button{-webkit-appearance:none;appearance:none}
.\[appearance\:textfield\]{-webkit-appearance:textfield;-moz-appearance:textfield;appearance:textfield}
```

`communications.test.tsx` and `app/communications/composer.tsx` were both dirty (T-202's
contract). I waited and reran once:

```
tsc exit: 0
npm test exit: 1
      Tests  2 failed | 1886 passed (1888)
 ✓ __tests__/donate-checkout.test.tsx  (10 tests) 209ms
 ✓ __tests__/donate-signed-in.test.tsx  (11 tests) 167ms
 FAIL  __tests__/planner-shift.test.tsx > a meal not saved yet is counted before its first save (T-215) > sends no request at Done apart from the read-only count
 FAIL  __tests__/settings-edit-mode.test.tsx > the WhatsApp App ID box > Save sends the App ID typed, and the saved answer is what the box shows, now and after a reload
 Test Files  2 failed | 137 passed (139)
lint exit: 0
next build exit: 0
  ▲ Next.js 14.2.15
   Creating an optimized production build ...
 ✓ Compiled successfully
   Linting and checking validity of types ...
 ✓ Generating static pages (74/74)
.\[appearance\:textfield\]{-webkit-appearance:textfield;-moz-appearance:textfield;appearance:textfield}
```

On the rerun communications passed. The two failures had moved to files in other contracts:
`planner-shift.test.tsx` (the meal composer, T-208/T-215) and `settings-edit-mode.test.tsx`
(settings, T-200). Neither imports or renders the Give page. I did not touch them.

`next build` compiled the CSS. Both rules are in the built stylesheet, and autoprefixer added the
`-moz-appearance:textfield` Firefox needs.

**Acceptance criteria**

- The box carries the classes that remove the spinners in WebKit and Firefox, asserted in a test:
  the new test uses `toHaveClass` on the box found by its label "Or another amount". The built CSS
  above shows the classes turn into real rules.
- type, min, step and inputMode unchanged, asserted: the same test checks `type=number`, `min=1`,
  `step=1`, `inputMode=numeric`. The diff touches only the className line and the comment.
- Existing Give tests pass: 21/21 across donate-checkout and donate-signed-in, both runs. The
  T-172 test that checks "Or another amount must be at least 1" is among them.
- Negative control: without the classes the new test fails (1 failed | 9 passed), and cmp confirms
  the file was restored byte for byte.

**Not done / needs Rajeev**

- Full `npm test` is not green, but not because of this change. 2 failures on each run, both times
  in other builders' files that were being edited (communications first, then planner-shift and
  settings-edit-mode). The release agent should rerun on the merged tree.
- No hand smoke-test from me. Whether the arrows are really gone in desktop Chrome and Firefox is
  for the main session's browser test.

## After-UAT lines

- Other `type="number"` boxes in the app still show spinner arrows. This change only covers the
  Give page box, as asked.
