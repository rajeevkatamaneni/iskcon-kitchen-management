# T-201 — "Why" becomes "Reason" on the close-order and change-condition forms

**Files changed:** all inside the contract.

- `frontend/app/orders/[id]/page.tsx` — line 1342 label: `closeNeedsASentence ? "Reason" : "Reason, if you want to say"`; line 1356 comment that quoted the old accessible name now quotes "Reason This goes on …".
- `frontend/app/equipment/[id]/page.tsx` — line 522 condition-form label "Why" → "Reason". The history table's "Why" column header (line 348) and "Why it is coming back" (reinstate form, line 696) are untouched, as instructed.
- `frontend/__tests__/closing-a-part-delivered-order.test.tsx` — label queries `/^why$/i` → `/^reason$/i`, `/^why, if you want to say$/i` → `/^reason, if you want to say$/i`, `"Why is required"` → `"Reason is required"`, test name and local variable renamed.
- `frontend/__tests__/blank-submit-slice-c.test.tsx` — only the two equipment condition tests (now lines 361–383): name, label query and `"Reason is required"`. The other diff hunks in this file (around lines 235 and 258) belong to T-203, not to me.
- `frontend/__tests__/equipment-edit.test.tsx` — lines 232 and 282, both in the "scrapping a machine" block, which drives the condition form.
- `frontend/__tests__/equipment-reinstate.test.tsx` — line 215, `askTheQuestion()`, which drives the condition form. The reinstate form's `/why it is coming back/i` queries are untouched.

**Commands run and what came back:**

One script, run through the lock: `tools/work-lock.sh run verify "<scratchpad>/t201-verify.sh" > <scratchpad>/t201-full.log`.
It (1) copied both pages aside, set a trap to copy them back, sed-restored the old labels, ran the four test files; (2) restored, `cmp`'d; (3) reran the four files; (4) `npx tsc --noEmit && npm test && npm run lint && npx next build` (each run in turn, exit code echoed).

Negative control, old labels in place:

```
1342:                        {closeNeedsASentence ? "Why" : "Why, if you want to say"}
          <span className="pl-field-inset font-medium text-ink">Why</span>
 FAIL  __tests__/blank-submit-slice-c.test.tsx > equipment (T-163) > changing condition: names a blank Reason, and records nothing
 FAIL  __tests__/blank-submit-slice-c.test.tsx > equipment (T-163) > changing condition to Scrapped with no reason: refuses before the scrap question is asked
 FAIL  __tests__/closing-a-part-delivered-order.test.tsx > closing a part-delivered order > sends the name the person chose, and the sentence with it
 FAIL  __tests__/closing-a-part-delivered-order.test.tsx > closing a part-delivered order > requires a sentence for the two outcomes that say something about the supplier
 FAIL  __tests__/closing-a-part-delivered-order.test.tsx > closing a part-delivered order > names the blank Reason when the outcome needs a sentence, and closes nothing (T-162)
 FAIL  __tests__/equipment-edit.test.tsx > scrapping a machine > asks before scrapping, and names the consequence
 FAIL  __tests__/equipment-edit.test.tsx > scrapping a machine > sends the change only once it has been confirmed, reason and all
 FAIL  __tests__/equipment-edit.test.tsx > scrapping a machine > sends nothing at all when the question is answered no
 FAIL  __tests__/equipment-edit.test.tsx > scrapping a machine > names the way back without offering it
 FAIL  __tests__/equipment-edit.test.tsx > scrapping a machine > still steers a merely broken machine to Needs repair
 FAIL  __tests__/equipment-edit.test.tsx > scrapping a machine > asks nothing before a condition that can be taken back
 FAIL  __tests__/equipment-reinstate.test.tsx > what the scrapping confirmation is now allowed to claim > no longer says scrapping cannot be undone, anywhere on the page
 FAIL  __tests__/equipment-reinstate.test.tsx > what the scrapping confirmation is now allowed to claim > still discourages a casual scrapping, by naming the friction rather than an impossibility
 FAIL  __tests__/equipment-reinstate.test.tsx > what the scrapping confirmation is now allowed to claim > keeps the two sentences that were true all along
 FAIL  __tests__/equipment-reinstate.test.tsx > what the scrapping confirmation is now allowed to claim > still asks before anything is sent, and still offers no undo of its own
 Test Files  4 failed (4)
      Tests  15 failed | 55 passed (70)
cmp: both pages restored byte-for-byte
1342:                        {closeNeedsASentence ? "Reason" : "Reason, if you want to say"}
          <span className="pl-field-inset font-medium text-ink">Reason</span>
```

New labels, same four files:

```
 Test Files  4 passed (4)
      Tests  70 passed (70)
```

Full checks:

```
=== FULL: tsc ===
tsc exit=0
=== FULL: npm test ===
 Test Files  139 passed (139)
      Tests  1880 passed (1880)
npm test exit=0
=== FULL: lint ===
> iskcon-kms-frontend@0.1.0 lint
> eslint . --max-warnings=0
lint exit=0
=== FULL: next build ===
○  (Static)   prerendered as static content
ƒ  (Dynamic)  server-rendered on demand
next build exit=0
=== DONE ===
work-lock: 'verify' released; command exited 0
```

**Acceptance criteria:**

- Close an order with an outcome other than "as computed" and a blank box shows "Reason is required" and closes nothing: `closing-a-part-delivered-order.test.tsx` › "names the blank Reason when the outcome needs a sentence, and closes nothing (T-162)" picks "the vendor let us down", presses Close order, finds the exact text "Reason is required", checks the box points at it through `aria-describedby`, and checks `closePurchaseOrder` was not called. Passes. The red colour comes from the shared `Form` component, which I did not change.
- Change a machine's condition with a blank box shows "Reason is required" and records nothing: `blank-submit-slice-c.test.tsx` › "changing condition: names a blank Reason, and records nothing" (and the Scrapped variant) use `expectRefused(... /^reason$/i, "Reason is required")` and check `changeEquipmentCondition` was not called. Pass.
- Negative control: with the old labels restored under a trap, 15 tests across the four files went red, including both acceptance tests above; `cmp` confirmed both pages went back byte-for-byte, and the rerun was 70/70 green.

**Not done / needs Rajeev:**

- Not hand-tested in a browser. The main session's Kitchen Manager browser test covers it.

## After-UAT lines

- "Why" is still used for the same idea on the equipment page: the reinstate form's "Why it is coming back", the scrap dialog's "they must record why", and the history table's "Why" column. If the rule is one word for one thing, those are the next ones to look at.
