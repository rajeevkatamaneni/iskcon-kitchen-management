# T-255: test fixtures state the new IngredientView fields explicitly

**Files changed** (all inside the contract):
- frontend/__tests__/closing-a-part-delivered-order.test.tsx
- frontend/__tests__/described-po-line.test.tsx
- frontend/__tests__/donations.test.tsx
- frontend/__tests__/duplicate-ingredient.test.tsx (untracked file, so it does not show in `git diff`)
- frontend/__tests__/goods-return.test.tsx
- frontend/__tests__/ingredient-request-new.test.tsx
- frontend/__tests__/ingredients.test.tsx
- frontend/__tests__/lead-time-one-promise.test.tsx
- frontend/__tests__/manual-purchase-order.test.tsx
- frontend/__tests__/order-detail.test.tsx
- frontend/__tests__/recipe-new.test.tsx
- frontend/__tests__/shopping-list-add.test.tsx
- docs/work/proof/T-255.md

I added `packSizes: [], marketRate: null, marketRateOn: null, marketRateSource: null` to every
IngredientView fixture in these files, and nothing else. No assertions and no product code changed.
Before the fix, tsc reported 14 errors in these files, all of them the missing IngredientView fields.
None of these files had a typed VendorSupplyView fixture that tsc flagged, so none needed the new vendor
fields.

**Commands run and what came back:**

Before the fix, the same grep matched 14 lines, all of them TS2739/TS2322 errors saying
"missing ... packSizes, marketRate, marketRateOn, marketRateSource".

After:
```
tools/work-lock.sh run verify 'cd frontend && npx tsc --noEmit 2>&1 | grep -E "^__tests__/(closing-a-part|...|shopping-list-add)" ; echo "tsc-grep-exit=$?"; npx vitest run <the 12 files> 2>&1 | tail -15'

tsc-grep-exit=1        (grep matched nothing, so no tsc errors in these files)
 Test Files  12 passed (12)
      Tests  219 passed (219)
work-lock: 'verify' released; command exited 0
```
(The jsdom "navigation not implemented" stack in the output is console noise from the link clicks, not a failure.)

**Acceptance criteria:**
- No tsc errors in the contract files: verified by the empty grep above.
- The 12 test files still pass: verified, 219 of 219 tests pass.
- No assertion or product code changed: the diff only adds fixture fields.

**Not done / needs Rajeev:** Nothing. Other test files still fail tsc, but other builders own them.
There is no user-facing surface here, so there was nothing to smoke-test by hand.
