# T-341 (was T-311) — the planner day page said 'planned was saved.'

> This task was first dispatched as T-311. Another work stream in this checkout used the same number, and
> its proof overwrote `docs/work/proof/T-311.md`, so this proof was lost. The work manager renumbered the
> task T-341. This file is rewritten from the builder's own record of the commands and their output; nothing
> was re-run. The scripts, logs and control artefacts in the scratchpad still carry the old number
> (`verify-T-311.sh`, `control-T-311.log`, `control-T-311.snapshot.tsx`, `scratchpad/T-311/`), because
> that is what they were called when they ran.

**Files changed** (all inside the contract as dispatched):

- `frontend/components/planner/savedNotice.ts` (new) — `savedNotice(flag)`: `planned` → "The meal was planned.", any other name → "<name> was saved.", absent or blank → null.
- `frontend/app/planner/[date]/page.tsx` — the saved notice only: reads the flag through `savedNotice`, renders the sentence as given (was `` `${saved} was saved.` ``).
- `frontend/app/planner/page.tsx` — the saved sentence only: its inline ternary replaced by the same `savedNotice` call. Same output as before for every non-blank flag.
- `frontend/__tests__/planner-saved-notice.test.tsx` (new).
- The proof, first as `docs/work/proof/T-311.md` (since overwritten by another task), now this file.

**Writers of `?saved=` for the planner, checked by grep** (`grep -rnE '"saved"|saved=' app components lib __tests__`): exactly two.
`app/planner/compose/page.tsx:126` sends `planned`; `app/planner/meal/[id]/page.tsx:173` sends `meal.eventName || meal.mealKind`
(e.g. `Lunch`, or an event's name). The other hits are `/volunteers` and `/equipment`, which have their own readers and are untouched.

**Commands run and what came back**

One hold of the lock, script `scratchpad/verify-T-311.sh`, log `scratchpad/control-T-311.log`:
`tools/work-lock.sh run verify "$S/verify-T-311.sh"` (backgrounded), exit 0.

tsc — errors only in the other builder's in-flight `MealServices.tsx`, none in my files:
```
--- tsc
components/planner/MealServices.tsx(1422,64): error TS2345: Argument of type 'string | undefined' is not assignable to parameter of type 'string'.
  Type 'undefined' is not assignable to type 'string'.
components/planner/MealServices.tsx(1426,19): error TS2339: Property 'weeksCopied' does not exist on type 'RepeatEventResult'.
components/planner/MealServices.tsx(1426,41): error TS2339: Property 'weeksCopied' does not exist on type 'RepeatEventResult'.
components/planner/MealServices.tsx(1427,19): error TS2551: Property 'copied' does not exist on type 'RepeatEventResult'. Did you mean 'copies'?
components/planner/MealServices.tsx(1427,36): error TS2551: Property 'copied' does not exist on type 'RepeatEventResult'. Did you mean 'copies'?
components/planner/MealServices.tsx(1429,18): error TS2339: Property 'refusedOnFast' does not exist on type 'RepeatEventResult'.
components/planner/MealServices.tsx(1431,21): error TS2339: Property 'refusedOnFast' does not exist on type 'RepeatEventResult'.
tsc exit 2
```
eslint on the four files:
```
--- eslint
eslint exit 0
```
vitest — `planner-saved-notice`, `planner`, `planner-day-routes`, `design-system` (the last three run, not edited). The log was
captured with `tail -25`, so only the last file's line and the summary are in it:
```
 ✓ __tests__/planner-day-routes.test.tsx  (34 tests) 757ms

 Test Files  4 passed (4)
      Tests  100 passed (100)
   Start at  11:59:00
   Duration  2.16s (transform 2.27s, setup 257ms, collect 3.76s, tests 1.68s, environment 1.03s, prepare 160ms)
```
(Above that, the tail also held a React `act(...)` warning from `EditMealScreen` in `planner-day-routes`, a pre-existing test's noise, not a failure.)

**Negative control** (same lock hold): snapshot the fixed day page, `trap` restore on EXIT, copy in the pre-fix day page
(a copy taken before editing, proven equal to `git show HEAD:` with `cmp`), `set -e` guards asserting 0 `savedNotice` and exactly 1
`` was saved.` `` in it, and `cmp -s` proving the file changed before running. Tests as written:
```
=== T-311 negative control ===
control applied: day page is the pre-fix file ()
   × the confirmation after a save reads the same on the day page and the planner > says a new meal was planned on the day page
     → Unable to find an element with the text: The meal was planned.. This could be because the text is broken up by multiple elements. In this case, you can provide a function for your text matcher to make your matcher more flexible.
⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯
 FAIL  __tests__/planner-saved-notice.test.tsx > the confirmation after a save reads the same on the day page and the planner > says a new meal was planned on the day page
      Tests  1 failed | 6 passed (7)
RESTORED byte-identical
work-lock: 'verify' released; command exited 0
```
The empty `()` is `git diff --stat HEAD` printing nothing, correctly: the control file *is* HEAD. `cmp` is the real guard.
Why only 1 of 7 goes red: the defect is on the day page and only for `planned`. The planner-page test, the `Lunch` and event-name
cases (the old day page already said "Lunch was saved."), the no-flag absence test and the two helper unit tests all pass on the old
day page by design. After restore, `grep -c savedNotice` on the day page returned `2`.

**Live, local web :3000 (hot reload), API :8080, Temple Admin `ikms.temple-admin.1` (Karuna Murti Das)**, headless Chromium via
`scratchpad/T-311/live-T-311.js`, 1280×900:

Opened `/planner/compose?date=2026-10-05&from=/planner/2026-10-05`, picked **Dinner** (not Lunch: 5 Oct already had the E2E run's Lunch,
and the flag sent is `planned` whichever kind), 50 adults, one dish (Chitranna), pressed "Save this meal". Read from the DOM:
```
{
 "urlOnArrival": "http://localhost:3000/planner/2026-10-05?saved=planned",
 "urlNow": "http://localhost:3000/planner/2026-10-05",
 "notices": [
  {
   "tag": "P",
   "role": "status",
   "text": "The meal was planned."
  }
 ]
}
```
Screenshot `scratchpad/T-311/day-after-save.png` shows the green banner "The meal was planned." above the day.
Same flags on both pages, read from `main [role=status]`:
```
"/planner?view=day&date=2026-10-05&saved=planned": ["The meal was planned."]
"/planner/2026-10-05?saved=Lunch":                 ["Lunch was saved."]
"/planner?view=day&date=2026-10-05&saved=Lunch":   ["Lunch was saved."]
```
Cleanup: the Dinner I made (meal `80f3cdf9-7993-4055-bf9d-c320dc215a93`) was cancelled through the UI: "Cancel this meal" on the Dinner card,
dialog read "Cancel Dinner? Its preparations come off the plan.", confirmed; the page then said "Dinner was cancelled." and only Lunch remained.
The E2E run's Lunch was left as it was.

**Acceptance criteria**

- Day page with `?saved=planned` shows "The meal was planned." — vitest, and live DOM above.
- Planner page shows the same sentence — vitest, and live DOM above.
- Other saved values read correctly ("Lunch was saved.", "Janmashtami feast was saved.", nothing for blank) — vitest; Lunch also live.
- Test red on the old day page — negative control above, 1 failed, restored byte-identical.
- tsc / eslint / vitest through the lock — above; tsc's only errors are in `MealServices.tsx`, not mine.

**Not done / needs Rajeev**

- Wording kept as the planner already had it ("The meal was planned." / "Lunch was saved."); I did not change the sentence, only made the day page use it.
- Known edge, not fixed (outside contract): an event literally named "planned", edited, would read "The meal was planned." Fixing it
  means the edit screen sending a different flag, and `app/planner/meal/[id]/page.tsx` belongs to another builder.
- Small behaviour change on the planner page: a whitespace-only `?saved=%20` now shows nothing (before: " was saved."). No writer sends that.
- Checked in headless Chromium only, at 1280 wide; not in Rajeev's Chrome or at phone width.
