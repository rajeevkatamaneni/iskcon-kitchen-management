# T-343 — VERIFY3's five findings fixed: delivery history spans its row, shopping list stops scrolling sideways, one pack-size message, "13 weeks", repeat sentence on one baseline

**Files changed:** (all frontend, all inside the contract)

- `frontend/components/DeliveryHistory.tsx` — the history split into `DeliveryHistoryToggle` and `DeliveryHistoryList` (the old `DeliveryHistory` still wraps both for the PO page and the delivered-order card). The toggle's words never wrap. Each piece of a history line stays whole at every width again (T-299's `lg:whitespace-normal` removed, since the history no longer sits in a fitted column).
- `frontend/app/deliveries/page.tsx` — Partly delivered (`PartlyRow`) and Received (`ReceivedRow`): the toggle stays in the Item cell, and the opened history goes in a row of its own under it, `colSpan={6}`, using the PO page's sub-row classes.
- `frontend/components/ds/table.ts` — `settlePadding()`: finishes padding transitions before the fitter measures (the real cause of F-2, see below).
- `frontend/components/ingredient/PackSizes.tsx` — Size box is now `required` + `data-more-than="0"`, and `min="0"` is gone. The page's own "Size must be more than 0" line is gone too, so `Form` says it once. Add clears the server's refusal when pressed.
- `frontend/components/planner/MealServices.tsx` — repeat control only: the unit word now comes from the number in the box, and `PHRASE` changed from `align-middle` to `align-baseline`.
- Tests: `frontend/__tests__/delivery-history.test.tsx`, `deliveries.test.tsx`, `ingredient-page-verify2.test.tsx`, `ingredient-detail.test.tsx`, `repeat-series.test.tsx`.

The PO page (`app/orders/[id]/page.tsx`) was checked but not changed. It already puts the history in a spanning row (T-265).

**Commands run and what came back:**

```
tools/work-lock.sh run verify 'cd frontend && npx tsc --noEmit && echo TSC-OK && npx eslint --max-warnings=0 <5 sources + 5 tests> && echo ESLINT-OK && npx vitest run __tests__/delivery-history.test.tsx __tests__/deliveries.test.tsx __tests__/ingredient-page-verify2.test.tsx __tests__/ingredient-detail.test.tsx __tests__/repeat-series.test.tsx __tests__/po-merged-table.test.tsx __tests__/closing-a-part-delivered-order.test.tsx __tests__/shopping-list-no-vendor.test.tsx __tests__/shopping-list.test.tsx __tests__/design-system.test.ts __tests__/table-rule.test.ts __tests__/planner.test.tsx'
TSC-OK
ESLINT-OK
 ✓ __tests__/table-rule.test.ts  (40 tests)
 ✓ __tests__/closing-a-part-delivered-order.test.tsx  (26 tests)
 ✓ __tests__/design-system.test.ts  (23 tests)
 ✓ __tests__/po-merged-table.test.tsx  (21 tests)
 ✓ __tests__/shopping-list.test.tsx  (22 tests)
 ✓ __tests__/deliveries.test.tsx  (16 tests)
 ✓ __tests__/ingredient-page-verify2.test.tsx  (16 tests)
 ✓ __tests__/planner.test.tsx  (36 tests)
 ✓ __tests__/ingredient-detail.test.tsx  (28 tests)
 ✓ __tests__/delivery-history.test.tsx  (18 tests)
 ✓ __tests__/shopping-list-no-vendor.test.tsx  (10 tests)
 ✓ __tests__/repeat-series.test.tsx  (24 tests)
 Test Files  12 passed (12)
      Tests  280 passed (280)
work-lock: 'verify' released; command exited 0
```

(planner.test.tsx logs an existing duplicate-key warning from `app/planner/page.tsx`. It is not mine and the test passes.)

Negative control: each fix was reverted through the lock, the tests were run, and the files were restored (`cmp` confirmed each one identical afterwards).

```
 × DeliveryHistory > keeps the toggle's words on one line, so '2 deliveries' never splits in a narrow column
 × Deliveries > Partly delivered: what is still owed, and for how long, with the item's history
 × the repeat control > says the unit for the number in the box, even one it refuses (VERIFY3, A3-2)
 × the repeat control > puts every phrase on the sentence's baseline, as the event's name is (VERIFY3, A3-3)
 × VERIFY2-A N1 ... the exact repro: a duplicate, then Box, 0, Kg and Add — only 'Size must be more than 0' shows
 × VERIFY2-A N1 ... each press of Add clears it before the size is checked, with no field touched
 × VERIFY3 A3-1 ... the exact repro: 0 and Add, then -5 and Add, says 'more than 0' once and never 'at least 0'
 × VERIFY3 A3-1 ... the sentence follows each change: gone once the size is right, back when it is wrong again
 Test Files  4 failed (4)
      Tests  8 failed | 66 passed (74)
```

**Measured on the running app** (web :3000, API :8080, headless Chromium 1134 through Playwright, signed in with custom tokens as Kitchen Staff, Kitchen Manager and Temple Admin; `prefers-reduced-motion: reduce`, the same as VERIFY3's harness, unless a line says otherwise):

- **F-1, Partly delivered, VERIFY-B Rice, "2 deliveries".** Before, at 1024: Item cell 115px, row 413px, and the history list 83px wide, each line 2 to 6 text lines. After, the row is 103px, the history row is separate and 80px, and the list is 638px of a 678px table. All 3 lines are 1 text line at 1024, 1060 and 1280, and 1/2/1 at 390 (the phone card). Column widths are identical before and after opening, at every width (1024: 115,123,128,119,112,81 both times). Table scrollWidth equals clientWidth, and the page scrolls 0px sideways at 390, 1024, 1060 and 1280.
- **F-1, Received tab.** A visit with two histories open (VERIFY2-D Rice, VERIFY2-D Green chilli): one spanning row, two lists, each headed with its item, with `aria-controls` matching each list id. Every line is 1 text line at 1024 and 1280. Column widths are unchanged by opening (1024: 84,115,113,140,113,113). 0 overflow at 390, 1024 and 1280. Before the toggle got `whitespace-nowrap`, "2 / deliveries" wrapped onto two lines in a 101px Items column at 1024. After, the toggle is one line (44px tall).
- **F-1, PO page PO-2026-0054.** The list is 590 of 630px at 1024 and 846 of 886px at 1280, all 1 line. At 390, 1/2/1. 0 overflow.
- **F-2, shopping list, "No vendor yet".** Before: scrollWidth 638 against clientWidth 628 at 1024. Cause: with reduce motion on, the stylesheet's reduced-motion rule gives every element a 0.01ms transition on `all`. So when the fitter closes the gaps (`--kms-cell-pad` 12→10px), the padding transitions, and the fitter measures the old 12px and decides the step failed. With `no-preference` the same page already fitted, 628/628 with pad 10px. After `settlePadding`, KM and TA both measure 0px sideways on all 8 tables at 1024, 1100, 1280 and 390, under both `reduce` and `no-preference` (1024: 628/628, 1100: 704/704, 1280: 884/884, 390: 306/306, page scrollWidth = viewport).
- **A3-1, ingredient 646b1f4d…, Temple Admin, 1280.** Add with Size empty shows "Size is required". Size 0 shows "Size must be more than 0" as soon as it is typed, and again on Add. −5 shows "Size must be more than 0", on typing and on Add. That is 1 sentence each time and 0 "at least". Typing 2 shows 0 sentences, and emptying the box shows 0 until the next press. At 1024 and 390, −5 then Add: 1 sentence, 0 elements outside the card, page 1024/390.
- **A3-2, FINAL Gita Reading, 3 Oct, Temple Admin.** 13 reads "weeks" (with the 1-to-12 refusal and Repeat disabled). 0 and empty read "weeks", 2 reads "weeks", 1 reads "week".
- **A3-3.** Before, at 1280: "Repeat FINAL Gita Reading" bottom 399.4, "once every" 401.0. After: name, "once every", "week" and "until" all have bottom 401 at 1280, all 445 at 1024, and at 390 each line's words are level with each other. The two boxes and the Repeat/Close buttons share one centre at 1280 (393.0). A middle-aligned last phrase put the buttons 1.6px lower, so it takes the baseline class as well.

**Acceptance criteria:**

- F-1: the opened history spans the full row on Partly delivered, and on Received and the PO page too. Nothing is squeezed and there is no dead block. Verified by the measurements above, `deliveries.test.tsx` (colspan 6, list in the next row) and `delivery-history.test.tsx`.
- F-2: 0px sideways scroll at 1024, 1100, 1280 and 390 for Kitchen Manager and Temple Admin. Measured above. The fitter doesn't run in jsdom, so there is no vitest for this; the browser run with reduce motion on and off is the evidence.
- A3-1: one message, "more than 0", replaced or cleared on each change. Verified in the browser and by 2 new tests.
- A3-2: the plural follows the number shown, and min 1 / max 12 stay on the box with the inline refusal for anything outside them. Browser and a new test.
- A3-3: one baseline. Measured, with a class guard test.

**Not done / needs Rajeev:**

- Rajeev hasn't smoke-tested these by hand. I drove headless Chromium, not his Chrome, and used the custom-token sign-in.
- Matter of taste: the pack-size sentence sits where `Form` puts every sentence, straight after the Size box. At 1280 that moves Unit and "Add pack size" right while it shows (screenshot `scratchpad/t343/shots/a31-minus5-1280.png`). It is the same placement VERIFY3 saw for the "at least 0" sentence. I didn't change `Form`, which is outside the contract.
- Choice made: on the Received tab, an opened history is headed with its item's name only when the visit has more than one item. With one item the name would just repeat the line above.
- Not asked, still there: in the "No vendor yet" table at 1024, "0 gm" still breaks onto two lines and the "Won't arrive in time" pill onto several. That is the fitter's last resort in a 7-column table in 628px. It no longer overflows.
- Incident, owned: one of my scripted edits cut the Expected and Partly delivered views out of `app/deliveries/page.tsx` (an untracked file, so git had no copy). I recovered the pre-edit source from the Next dev server's source map (`.next/static/webpack/app/deliveries/page.b6a6db63f67d071a.hot-update.js`, 12:01) and reapplied my changes. `diff` against the recovered copy shows only my intended changes, and the page's 16 tests pass. If anyone edited that file between 12:01 and about 12:50 without the dev server recompiling it, that edit would be lost. I have no evidence that happened.
- Note for the next builder: `prefers-reduced-motion` gives every element `transition: all 0.01ms`, so any script that changes a style and measures in the same task reads the old value. It lives in `globals.css`, which is outside this contract. `settlePadding` works around it for the table fitter only.
