# T-226 — the seven approved cross-app term changes, applied across the app

**Files changed.** The task's contract was the whole app except the sidebar, the app shell, `app/layout.tsx` and the reserved shared files. Every file below is inside it. None is a sidebar, shell or layout file. `ErrorCode.java` was not opened (see "Not done").

Frontend source:
- `frontend/lib/vaishnava-day.ts`: new `ekadashiSpelling`, `EKADASHI_FRIENDLY`, `recipeTagLabel(s)`. `ekadashiLabel`, `clean()` and `fastLabel` now say Ekadashi.
- `frontend/lib/calendar-names.ts`: tithi name Ekadasi → Ekadashi.
- `frontend/app/calendar/page.tsx`: legend label, comments.
- `frontend/app/planner/page.tsx`: 4 fallbacks now go through `ekadashiLabel`. Festival text goes through `ekadashiSpelling`.
- `frontend/components/planner/DayView.tsx`: Ekadashi badge and the festival line.
- `frontend/components/planner/MealComposer.tsx`: comment only.
- `frontend/app/today/page.tsx`: today and tomorrow names and the ahead name are respelt. "Items below par" became "Items below reorder level".
- `frontend/app/recipes/page.tsx`: the new notice and its approval comment.
- `frontend/app/recipes/[id]/page.tsx`: badge uses the constant. Tags are mapped and deduplicated. Catering note removed.
- `frontend/app/recipes/library/[id]/page.tsx`: tags mapped, Catering note removed.
- `frontend/components/RecipePeek.tsx`: "Suits a fasting day" became "Ekadashi-friendly". Tags mapped. Catering note dropped.
- `frontend/components/RecipeForm.tsx`: "Catering note" field removed. The existing value is still sent on save, so it is not wiped.
- `frontend/components/planner/MealServices.tsx`: 4 "Consumed" labels became "Eaten".
- `frontend/app/vendors/new/page.tsx`, `frontend/app/vendor-performance/page.tsx` (3), `frontend/app/notices/page.tsx`, `frontend/app/shopping-list/page.tsx`, `frontend/app/orders/new/page.tsx`, `frontend/components/PurchaseOrderEditor.tsx`: supplier → vendor.

Frontend tests: `__tests__/recipes.test.tsx`, `vendors.test.tsx`, `today.test.tsx`, `calendar-names.test.ts`, `calendar.test.tsx`, `shopping-list.test.tsx`, `vaishnava-day.test.ts` (7 new cases), `meal-composer.test.tsx`.

Backend: `backend/src/main/java/org/iskcon/kms/calendar/CalendarService.java` (`ekadashiSpelling`, applied in `viewMapper` to the Ekadashi name and every festival text), `backend/src/main/java/org/iskcon/kms/today/TodayService.java` (fallback "Ekadashi"), `backend/src/test/java/org/iskcon/kms/document/JobCardIT.java`, `backend/src/test/java/org/iskcon/kms/meal/ReusePlanIT.java`.

**Counts per change**
1. Ekadasi → Ekadashi. Two central functions do the conversion: `CalendarService.viewMapper` on the server, which covers the calendar API, Today, the job card and the reuse preview, and `lib/vaishnava-day.ts` on the client. 13 display call sites and 2 literals changed (the legend and the tithi name). The stored rows, the engine and `strings.json` are untouched. The identifiers `"ekadasi"` and `EKADASI` are unchanged.
2. Ekadashi-friendly: 1 string ("Suits a fasting day"), plus the library tag "Ekadashi-safe", which appears on about 100 library recipes, now shown as "Ekadashi-friendly" on 3 screens. The tag is not repeated beside the badge.
3. Eaten: 4 labels, all in the record and correct forms.
4. Vendor: 8 strings in 6 files. No backend user text outside `ErrorCode.java` said "supplier".
5. Catering: 4 removals (form field, detail heading, library detail heading, peek note). The column and API field are kept.
6. Reorder level: 1 label.
7. Recipes notice: 1 notice, its comment, and its exact-words test.

**Commands run and what came back**

`tools/work-lock.sh run verify 'cd frontend && npx tsc --noEmit && echo TSC_OK && npx vitest run 2>&1 | tail -6 && npx eslint app components lib __tests__ && echo ESLINT_OK'`
```
TSC_OK

 Test Files  143 passed (143)
      Tests  1957 passed (1957)
   Start at  12:37:41
   Duration  18.88s (transform 4.85s, setup 6.70s, collect 19.01s, tests 87.69s, environment 31.95s, prepare 5.39s)

ESLINT_OK
work-lock: 'verify' released; command exited 0
```
(The first run failed one case, `calendar.test.tsx` "tells the kitchen what a fasting day changes", because it expected the old spelling. It was updated and the run above is the rerun.)

`tools/work-lock.sh run verify 'cd backend && ./gradlew test --tests "*JobCardIT" --tests "*ReusePlanIT" --tests "*TodayIT" --tests "*CalendarServiceIT" --tests "*CalendarOverrideIT" --tests "*ErrorCodeTest" 2>&1 | tail -40'`
```
  Total:    853
  Passed:   853
  Failed:   0
  Skipped:  0
  Result:   SUCCESS
BUILD SUCCESSFUL in 28s
work-lock: 'verify' released; command exited 0
```

**Acceptance criteria**
- No user-visible "Ekadasi". A grep of app, components and lib finds it only in comments. JobCardIT asserts the card has no "Ekadasi". ReusePlanIT and the today, calendar and vaishnava-day tests assert the new spelling from engine-spelt input.
- The recipe property is called Ekadashi-friendly: covered by the vaishnava-day tag tests and by grep.
- Catering is not shown or edited, and existing values survive a save: the form still carries the value (read in code).
- Tests updated as listed. tsc, vitest and eslint are clean. The backend classes pass, including ErrorCodeTest. No code was changed.

**Not done / needs Rajeev**
- `ErrorCode.java` was not opened, because the builder contract forbids it. Any error message there that says "supplier", "Ekadasi", "consumed" or "par" is unchanged. The main session should grep it.
- The ingredient setting stays "Ekadashi-prohibited". It is the opposite fact, so calling it "Ekadashi-friendly" would mean flipping a safety checkbox. The notice therefore says "Mark each one Ekadashi-prohibited or not".
- The job card PDF's handwritten column is headed "Served" and its sign-off says "served figures". The recording form says "Enter what the job card says", so this is the same number now labelled "Eaten". It was not changed because it never said "Consumed". Rajeev should say whether it becomes "Eaten".
- Stock movement kind `CONSUMPTION` already shows as "Cooked" on inventory. That is stock drawn for cooking, not food eaten, so it was left alone. The audit screen shows raw JSON keys `consumed` and `originalConsumed`. Those are data, so they were left.
- The library's category "Catering sabjis" (`CategoryMapping.java:58`) still creates a category with that name on import. That is stored data plus one mapping, and it was not in the seven.
- The recipe form's Tags box still shows the stored "Ekadashi-safe" text for editing.
- An override Ekadashi name without the word (for example "Nirjala") now shows as "Nirjala Ekadashi" on the planner, as the calendar already did.
- No hand smoke-test in a browser. It was not done because this was a text-only change covered by tests.
