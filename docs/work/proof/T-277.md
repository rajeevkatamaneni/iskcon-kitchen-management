# T-277 — the invoice page's totals as mock D draws them, PageHeader actions that wrap, and the curly-quote test fix

**I did not check this on the running app.** The local API is down and I did not start it. The evidence is vitest, plus isolated renders in headless Chrome 153 at device scale 2. Each render bundles the real page with esbuild, stubs only auth, the router and the API, and uses CSS that Tailwind compiled from the real config and `globals.css`. The harness is in the session scratchpad, `harness-T-277/` (`m.mjs` measures, `d.mjs` compares pixels, `create/` is T-273's create-form harness re-run as it was). **Nothing was created under `frontend/app`**: `ls frontend/app | grep -i "dev-\|harness"` prints nothing (exit 1). **I could not do a hand smoke test.**

## Files changed (all inside the contract)

- `frontend/components/InvoiceTotals.tsx`: `InvoiceTotals` now routes to two components. `EditableTotals` is the form's block, and its output is unchanged. I only removed its dead read-only branches. `ReadOnlyTotals` is new: design D's compact `<dl>`.
- `frontend/components/ds/PageHeader.tsx`: the actions box went from `flex flex-none flex-wrap gap-2` to `flex min-w-0 flex-wrap gap-2`, with a comment explaining why.
- `frontend/app/invoices/[id]/page.tsx`: back on the shared `PageHeader`, with the copied header markup removed. The Items doc comment is updated.
- `frontend/__tests__/invoice-create-form.test.tsx`: one expectation now uses curly quotes, "Add ‘Plastic stool’ as a one-off item". I did not open `ItemCombobox.tsx` for editing.
- `frontend/__tests__/invoice-totals.test.tsx`: the read-only tests are rewritten for the compact list. There are 5 tests: compact labels and figures, zero discount, no note, the red line, and the note and tick.
- `frontend/__tests__/invoice-detail.test.tsx`: added two assertions to the totals test, and a new test that the header actions sit in PageHeader's wrapping box.
- `frontend/__tests__/page-header.test.tsx`: new, 3 tests.
- `docs/work/proof/T-277.md`: this file.

I did not touch `pay-invoice.test.tsx` or `design-system.test.ts`, because neither needed a change. No PageHeader user's own test broke: the full run is below.

## What was built

1. **Read-only totals** have the same markup as mock D's foot:
   - `dl ms-auto grid grid-cols-[auto_auto_1.5rem] gap-x-3 gap-y-2 text-sm`, with the labels at `pe-5`.
   - Every figure has its ₹ sign, and the discount reads "− ₹2.50".
   - There is no "Amount (₹)" heading, and there are no 44px boxes.
   - The tick is the shared `AddsUp`, the same as the form uses.
   - The page's `flex border-t px-6 py-4` wrapper is the mock's own.

   R-INV-5 needs two things that mock D does not draw:
   - **The note** under Other charges. It ends on the figures' edge and is capped at `max-w-[15rem]`, so a long note wraps and cannot stretch the list across the card.
   - **The red line** when the figures do not add up (`role=alert`), with no tick.

   A zero discount shows "₹0", not "− ₹0".
2. **PageHeader.** The actions box can now shrink, so its buttons wrap only when their line is short of room.
3. **The curly quotes** in `invoice-create-form`.

## Commands run and what came back

Final run, through the lock:
```
tools/work-lock.sh run verify 'cd frontend && (npx tsc --noEmit; echo TSC_EXIT=$?); npx vitest run 2>&1 | grep -E "×|FAIL|Test Files|Tests |Duration"; echo VITEST_EXIT=${PIPESTATUS[0]}; npx eslint components/InvoiceTotals.tsx components/ds/PageHeader.tsx "app/invoices/[id]/page.tsx" __tests__/invoice-totals.test.tsx __tests__/invoice-create-form.test.tsx __tests__/invoice-detail.test.tsx __tests__/page-header.test.tsx; echo ESLINT_EXIT=$?'
TSC_EXIT=0
 Test Files  167 passed (167)
      Tests  2289 passed (2289)
   Duration  27.70s (transform 6.12s, setup 9.84s, collect 31.07s, tests 119.84s, environment 54.65s, prepare 7.60s)
VITEST_EXIT=0
ESLINT_EXIT=0
work-lock: 'verify' released; command exited 0
```
The earlier targeted run was `page-header`, `invoice-totals`, `invoice-create-form`, `invoice-detail`, `pay-invoice` and `design-system`. It ended with `Test Files  6 passed (6)` and `Tests  78 passed (78)`. `invoice-create-form` had 17 passing, including the one-off test that used to fail.

**Negative control.** The script is `scratchpad/control-T-277.sh`, run through the lock, with its log in `control-T-277.log`. It breaks two things:
- A: puts `flex-none` back on PageHeader's actions.
- B: makes the read-only figures bare, with no rupee sign.

```
A pageheader anchor count: 1
B totals anchor count: 1
…PageHeader.tsx …control-T-277.pageheader.snapshot.tsx differ: char 1431, line 32
pageheader cmp exit=1
…InvoiceTotals.tsx …control-T-277.totals.snapshot.tsx differ: char 12762, line 281
totals cmp exit=1
grep CONTROL: 1 pageheader, 1 totals
   × PageHeader > lets its actions shrink and wrap, never holding them on one line wider than the screen
   × InvoiceTotals, read-only > is design D's compact list: the same five labels, a rupee sign on every figure, the discount taken off
   × InvoiceTotals, read-only > shows a zero discount as ₹0, not as something taken off
   × an invoice's page (R-INV-7) > shows the bill's totals in the read-only block, with the tick when they add up
   × an invoice's page (R-INV-7) > puts Pay, Record a credit note and Void in the shared header's wrapping actions box
 Test Files  3 failed (3)
      Tests  5 failed | 23 passed (28)
VITEST EXIT=1
RESTORED: identical-to-snapshot
```
After the run, `grep -c "CONTROL T-277"` finds 0 in both files, and `cmp` against the snapshots printed POSTCHECK-IDENTICAL.

**The same control, in the browser.** I rebuilt the invoice page with the old PageHeader aliased in, using the snapshot, in the harness only. At 390, `docScrollWidth` is **481** and Void this bill ends at x 481. With the new one, `docScrollWidth` is **390** and Void drops to the second line (x 16, y 268). At 1280 both give the same three buttons at x 783, 955 and 1133.

## Measurements (DOM geometry and full-resolution pixel comparison; data in `harness-T-277/{before,after}/measure.json`)

**1. Invoice page totals against mock D.** The page was also rendered at the mock card's width (870 at 1280, 324 at 390), so the two compare like for like.

At 1280:
- The foot is 171 on the mock and 171 on the page. It was 342 before this task.
- The Items card is 508 on both. It was 679.
- The list is 192.8×138 on both.
- Every cell is at the same x, y, w and h. For example, Sub total is at 651.2,17 and 102.4×20, and ₹958 ends at 808. The tick is at 820,129.
- Pixels: **0 of 593,712 differ**.

At 390:
- The foot is 171 on both. The Items card is 864 on both; it was 1035.
- Pixels: **0 of 220,248 differ**.

At the page's natural width, the foot is 171 and every figure ends on one right edge (874 of 934; 296 of 356).

The note and the red line appear only when there is one. Mock D has neither, and R-INV-5 requires both.
- A 30-character note keeps to one line. The list goes from 192.8 to 212.2 wide and the foot to 199.
- A 190-character note wraps at 240px. The list is 276 wide and the foot 279.
- **Before the 15rem cap** (at `max-w-xs`), the same long note stretched the list to 356px and its label column to 184px, which pulled every label away from its figure. That is why the cap is there.
- A bill that doesn't add up shows "Adds up to ₹1,030, not ₹1,000" and no tick. The foot is 193.
- No sideways scroll and nothing past the right edge in any state: `docScrollWidth` is 1280 or 390 and `overflowRight` is empty.

**2. The create form's totals are unchanged.** I re-ran T-273's own measure script before and after.
- At 1280 the block is `{x:584, w:384, h:389}` before and after, the five boxes each end at 932 (128×44), and the tick is at 944,1253.
- At 390 the block is `{h:389}`, the boxes end at 338, and the tick is at 350,2344.
- The red line is identical at both widths.
- Pixels, before against after:

| Capture | Differing pixels |
|---|---|
| Totals crop, 1280 | 0 of 597,504 |
| Totals crop, 390 | 0 of 557,048 |
| Mismatch crop, 1280 | 0 of 646,656 |
| Mismatch crop, 390 | 0 of 602,872 |
| Whole form, 1280 | 0 of 7,311,360 |
| Whole form, 390 | 0 of 3,992,040 |

T-273's pixel match with the mock at 1280 therefore still holds.

**3. The invoice header on the shared PageHeader.** The header's pixels are 0 different from T-274's copied markup at 1280, at 390, and at both mock widths. At 390, Pay this invoice (x 0, 164 wide) and Record a credit note (x 171.6, 170 wide) sit on one row at y 82, and Void this bill is at y 134. All three are 44px tall. `docScrollWidth` is 390 and nothing overflows. At 1280 all three sit on one row.

**4. Every other PageHeader page, before and after, at 1280 and 390.** The pages are calendar, issued-from-store, vendor-performance, today, planner, planner/catch-up, planner/[date], cost-per-serving and deliveries.
- Every page's header geometry and its `main` box are identical, compared as JSON, before and after.
- Header and `main` screenshots differ by **0 pixels on all 36 pairs**.
- `docScrollWidth` is 1280 or 390 with no overflow, before and after.
- Only calendar, today and catch-up pass actions. Each of their actions already fitted, so nothing moved.

**Caveat:** in these renders the API returns a 404 stub, so each page's data area shows its error state. The headers are real, but the page bodies were not compared with data in them.

## Acceptance criteria

- **Invoice totals within a few px of mock D:** 0px apart at 1280 and 390 (171 against 171), and pixel-identical. Where they differ, the reason is a requirement: the note and the red line (R-INV-5), shown only when they apply.
- **Create form totals unchanged:** geometry identical, 0 differing pixels (section 2).
- **The invoice page at 390, using the shared PageHeader:** 0 horizontal overflow, and Pay and Record a credit note side by side (section 3). It was 481px on the old component (browser control).
- **Every other PageHeader page measured:** section 4, with nothing moved at 1280 and nothing worse at 390.
- **A PageHeader test pins the wrapping:** `page-header.test.tsx`, which goes red under control A. So does the detail page's header test.
- **invoice-create-form:** green, 17 of 17. Full vitest green (2,289), tsc clean across the tree, eslint clean on my files.
- **Negative control:** red without the fix (above).

## Not done / needs Rajeev

- **No hand smoke test**, and nothing was tried against the real API.
- **Candidates for the Decisions Desk** (judgement calls):
  - The 15rem cap on the note under Other charges.
  - "₹0" rather than "− ₹0" for a zero discount.
- The PageHeader pages were measured with a stubbed API, so their data areas show error states (section 4).
