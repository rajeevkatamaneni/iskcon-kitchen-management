# T-357 — staff belong to a kitchen (API), the kitchen check list, WhoAmI's kitchen fields, kitchens' staff counts, and the planner kitchen guard

State: **built and tested; the planner guard's registration is PARKED, not live in the tree.** Read "Needs the
coordinator" first. Worktree `.claude/worktrees/agent-a93967b8b0080da44`. Nothing committed.

## Needs the coordinator (before anything else)

1. **The guard is written and proven, but switching it on turns 114 tests red in files I do not own.** Almost
   every meal, job-card, Today and costing test signs in as a Kitchen Staff user with **no staff record**, and the
   rule (corrected by the coordinator) refuses exactly that person. Measured with the guard live (run 5 below):
   115 failures in 467, 114 of them the guard's 403 (the 115th is item 2). By class:
   EventIdentityIT 6, JobCardIT 29, MaterialsCostIT 3, MealCorrectionIT 1, MealPlanIT 19, MealRecordingIT 9,
   MealSaveIT 11, SufficiencyIT 11, TodayIT 5, TravelEstimateIT 15, the event-name suggestions class 5.
   The fix in each is one fixture line: give the cook a current staff record in a planner kitchen
   (`MealFixture.plannerKitchen(...)` already finds the kitchen). Those files are T-354's, T-356's, T-358's or
   nobody's this wave, so I did not touch them. Because three builders are running meal/document/today tests in
   this same worktree right now, I left the registration file **parked** so their runs are not broken under them:
   - parked copy: `/private/tmp/claude-501/-Users-Rajeev-Workspace-kitchen-management-system/79bf5e51-4f9e-420c-b419-747d070a92df/scratchpad/PlannerKitchenGuardConfiguration.java.parked`
   - to switch on: `cp <that file> backend/src/main/java/org/iskcon/kms/auth/PlannerKitchenGuardConfiguration.java`
   - every run below was made with it copied in for the length of the lock hold and removed by an EXIT trap
     (`scratchpad/t357_run.sh`).
   - While parked, 4 of `PlannerKitchenAccessIT`'s 9 tests fail (the refusal tests). That is expected and is the
     same result as the negative control.
2. **`tenancy/OwnAccountsAtOtherTemplesIT` (not mine) builds `HireStaffRequest` by position.** Adding `kitchenId`
   broke its compile, which stopped *every* builder's test compile. I added a secondary constructor with the old
   arity (kitchen null) to `HireStaffRequest` so the tree compiles; it is commented as temporary. One test there
   (`hiring 'an existing devotee' cannot name the administrator's own account at another temple`) now fails
   with KMS-400184 at its line 294, because its hire names no kitchen. Fix: pass a kitchen, then delete the
   compat constructor.
3. **`staff/StaffLeaveIT` (7) and `staff/LeaveWithdrawalIT` (15) hire through the API without a kitchen** and now
   get 400 KMS-400184. T-358 owns them. Same fix as my own test files: add `"kitchenId"` to the hire body (see
   `withKitchen` in `StaffEmploymentIT`). `error/PhoneValidationIT` also hires via the API; it passed in run 5,
   presumably because its hires are expected to be refused for the phone first — not investigated further.

## Files changed (all inside the contract)

Main:
- `staff/HireStaffRequest.java` — `kitchenId` (not `@NotNull`, so a missing one is KMS-400184); temporary compat constructor (item 2).
- `staff/UpdateStaffRequest.java` — `kitchenId`.
- `staff/StaffProfileView.java` — `kitchenId`, `kitchenName`, `kitchenNeedsCheck`.
- `staff/StaffEmploymentService.java` — T-350's interim hire code and its kitchen auto-creation removed (the `KitchenService` dependency is gone); `requireUsableKitchen` (400184 / 400108 / 400109; a non-planner kitchen is allowed); hire writes the chosen kitchen with `kitchen_needs_check = false`; update writes it and clears the flag; audit shapes carry `kitchenId`/`kitchenName` read back from the row (lesson 2), and the change sentence names a kitchen move; `SELECT` joins `kitchens`; `kitchenChecks()`, `setKitchen(...)`, `confirmKitchenChecks(...)`; reinstatement refuses 400109 if the person's old kitchen was archived while they were gone.
- `staff/StaffScheduleController.java` — hire checks the kitchen **before** the ban check (so no platform-log check is written for a hire that cannot complete); `GET /api/v1/staff/kitchen-checks`, `PUT /api/v1/staff/members/{id}/kitchen`, `POST /api/v1/staff/kitchen-checks/confirm`, all `MANAGE_STAFF` (the permission the register and edit already use; nothing added).
- New: `staff/StaffKitchenCheckView.java`, `staff/SetStaffKitchenRequest.java`, `staff/ConfirmKitchenChecksRequest.java`.
- `kitchen/KitchenView.java` — `staffCount`.
- `kitchen/KitchenService.java` — `staffCount` (current = ACTIVE employment) in list and detail; archive and delete refuse KMS-400185 with current staff; delete now also counts former staff records and `meal_kitchens` as references (KMS-400107) instead of hitting the RESTRICT foreign key as a 500; list ordered `is_main DESC, lower(name), id` to match `KitchenOrder` (T-350's open point). `seedMainKitchenForCurrentTenant` kept: provisioning and the meal interim still call it.
- `auth/WhoAmIController.java` — `kitchenId`, `kitchenName`, `canPlanMeals` (= holds MANAGE_MEAL_PLANS && `KitchenOrder.mayPlan(userId, role == TEMPLE_ADMIN)`).
- New: `auth/PlannerKitchenGuard.java` (in the tree). `auth/PlannerKitchenGuardConfiguration.java` (**parked**, item 1).
- `kitchen/KitchenController.java`, `ban/*` main files: no change needed. The ban hire-past-a-check path is the same `POST /members` with the same request, so it carries the kitchen.

Tests:
- New `staff/StaffKitchenIT.java` (12), new `auth/PlannerKitchenAccessIT.java` (9).
- `kitchen/KitchenIT.java` — 3 tests added; staff_profiles cleared in teardown.
- `staff/StaffEmploymentIT.java`, `StaffReinstatementIT.java`, `StaffScheduleIT.java`, `StaffPayIT.java`, `StaffConductNoteIT.java`, `ban/EmploymentBanIT.java` — each creates its temple's kitchen and adds it to hire/edit bodies through a `withKitchen(json)` helper. No assertion changed.
- Not needed, untouched: `AccessControlEnforcementIT`, `PermissionBeforeValidationIT` (its permitted role for MANAGE_MEAL_PLANS is the Temple Admin, who passes the guard; it stayed green with the guard live), `NextStepPermissionTest`, `MealPlannerAdoptionIT`. The WhoAmI assertions live in `PlannerKitchenAccessIT`; no existing WhoAmI IT was edited.

## The guard's scope, endpoint by endpoint

Guarded (`/x` and `/x/**` each): `/api/v1/meals` (list, summary, one, save, update, cancel, repeat, repeat-preview,
later-in-series, travel-estimate, record, correct), `/api/v1/meal-plans` (day-context, ekadashi-check,
menu-history, sufficiency, shortfall, outside-commitments, event-names, travel-estimate, reuse, reuse/preview),
`/api/v1/meal-crew` (range, at, suggested), `/api/v1/job-cards` (request, languages, documents, download, print).
Every one of these is called only from `app/planner/**` and `components/planner/**` (grep of each `api.ts`
method's callers).

Left open: `GET /api/v1/meal-kinds` (Settings → Meal kinds also lists them; its writes are MANAGE_TEMPLE_SETTINGS,
the admin's); `/api/v1/today`; `/api/v1/workforce`; leave, staff schedule, occasions, calendar, materials cost,
places. None is the planner. Order: registered at 100, after PermissionFirstInterceptor (0); both in `preHandle`,
before the body is read.

## Commands run and what came back

All through `tools/work-lock.sh run verify`, JAVA_HOME openjdk@21, guard config copied in for the hold and trapped out.

**Run 3 — the new and kitchen/staff-employment classes** (`*StaffKitchenIT`, `*PlannerKitchenAccessIT`, `*KitchenIT`, `*StaffEmploymentIT`; the `*KitchenIT` pattern also picks up `StaffKitchenIT` and `WhichKitchen…` is not matched):
```
  Total:    66
  Passed:   66
  Failed:   0
BUILD SUCCESSFUL in 20s
XML: tests=66 failures=0 errors=0 skipped=0
t357_run: guard config parked again
```

**Run 4 — staff.*, kitchen.*, auth.*, ban.* and the README 3a guards** (BaseQuantityIT, UnitLabelAgreementTest, TempleClockTest, CommunicationSendGuardSourceTest, ErrorCodeTest, FieldErrorMessageTest, NextStepPermissionTest, RolePermissionsTest, RowLevelSecurityIT, TenantLoopMigrationIT, TenantDeletionIT):
```
  Total:    1443
  Passed:   1419
  Failed:   24
XML: tests=1443 failures=24 errors=0 skipped=0
  FAILED StaffLeaveIT > (7 tests)            — all "Status expected:<201> but was:<400>" at hire: no kitchen (T-358's file, item 3)
  FAILED LeaveWithdrawalIT > (15 tests)      — same
  FAILED WhichKitchenMigrationIT > each section carries its own meal's People needed …
         PSQLException: column m.crew_required does not exist
  FAILED WhichKitchenMigrationIT > the two kitchen columns are NOT NULL …, and the old meal columns are still there
         expected: 3L but was: 0L
```
(7 + 15 leave failures + 2 migration failures = 24.) The two
`WhichKitchenMigrationIT` failures are T-350's assertions against V151/V152, which T-354/T-356 have since added
(they drop `meals.crew_required` and the card columns) — not this task's. Everything of mine, and every guard
test, passed.

**Run 5 — fan-out observation with the guard live** (meal.*, document.*, today.*, costing.*, shift.*, OwnAccountsAtOtherTemplesIT, PhoneValidationIT):
```
  Total:    467
  Passed:   351
  Failed:   115
```
Classified from the XML: 114 are the guard's 403 (list in item 1; MealRecordingIT's "No value at JSON path
$.unrecorded" is `/meals/summary` answered 403), 1 is OwnAccountsAtOtherTemplesIT's KMS-400184 (item 2).

**Negative control** — `scratchpad/control-T-357.sh`, inside one lock hold: asserts the anchor occurs once,
makes the guard always allow, runs PlannerKitchenAccessIT, restores through an EXIT trap and `cmp`s:
```
control-T-357: anchor count before patch: 1
72c72
< 		if (templeAdmin || kitchenOrder.mayPlan(user.getUserId(), false)) {
---
> 		if (true) { // CONTROL: always allow
  Total:    9
  Passed:   5
  Failed:   4
XML: tests=9 failures=4 errors=0 skipped=0
  FAILED PlannerKitchenAccessIT > a Kitchen Staff account with no staff record has no kitchen to plan for, and is refused
  FAILED PlannerKitchenAccessIT > the refusal comes before the body is read: an invalid body, even unparseable, is still 403 KMS-400183
  FAILED PlannerKitchenAccessIT > a cook in a kitchen that does not plan its meals here is refused on reading and on saving
  FAILED PlannerKitchenAccessIT > a staff record whose employment has ended gives no kitchen and no planner
control-T-357: restored; cmp says PlannerKitchenGuard.java is byte-identical to the snapshot
control-T-357: anchor count after restore: 1
```
The five that still pass are the ones that should: allowed people, the Volunteer's permission refusal, the open
endpoints and WhoAmI (which does not go through the guard).

## Acceptance criteria

- PlannerKitchenAccessIT (9/9 in run 3): admin with no record allowed (and an admin whose record is in the store kitchen); Kitchen Staff with no record 403 KMS-400183; store-kitchen cook 403 KMS-400183 on read (meals list, one meal, day-context, meal-crew, job-card languages) and save/update/reuse-preview; the same cook with `{}` and with unparseable JSON still 403 KMS-400183; planner-kitchen cook and Kitchen Manager allowed (GETs 200, empty save 400 not 400183); Volunteer 403 KMS-400021; meal-kinds, Today and own schedule still 200 for the store cook; a former employee has no kitchen and is refused. WhoAmI's three fields asserted for all seven people.
- StaffKitchenIT (12/12): hire/update without kitchen 400184 (hire refused before any BAN_CHECK_RUN); archived 400109; unknown and other-temple kitchen 400108; non-planner kitchen allowed; save clears the flag (moved or not); audit before/after names the kitchen as stored; check list = flagged current staff of this temple only; set-kitchen and confirm clear the flag; confirm is all-or-nothing (unknown id 404, nothing changed) and `[]` is 400; Kitchen Manager and Kitchen Staff get 403 KMS-400021 on all three (with a bad body too); temple B sees none of A's and gets 404 on set/confirm with A's records unchanged.
- KitchenIT: staffCount counts current staff only (list and detail); archive and delete with current staff 400185; after the person leaves, archive works; a kitchen only a former employee names is 400107 on delete, not a 500.

## Not done / needs Rajeev

- No hand smoke-test in a browser; this task adds API only, and the guard is parked.
- Open question (as the brief asked): turning a kitchen's planner flag off is unchanged, and its staff lose the planner on their next request with no warning on the kitchen form.
- Reinstating someone whose old kitchen was archived while they were away is refused KMS-400109; the reinstate form has no kitchen field (that request file is not mine). Restore the kitchen, reinstate, then move them. Worth a kitchen field on reinstatement.
- KMS-400107's sentence says "has asked for ingredients before"; it is now also the refusal for a kitchen named by a former employee or a meal. The next step ("archive it instead") is right; the first sentence needs widening in ErrorCode.java (not mine). Likewise KMS-400185 says "can't be archived" and is also used for delete.
- The guard decides "Temple Admin" by role (`role == TEMPLE_ADMIN`), because the rule and `KitchenOrder.mayPlan` are written in those terms and no permission constant was to be added.
