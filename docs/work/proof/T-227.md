# T-227 part 2 — status colours recoloured to Rajeev's rule, borderline rulings applied, DESIGN_SYSTEM v1.10

Rule (Rajeev, 2026-09-18): amber = warning (act or take care); red = serious, needs attention now;
green = only the user's own action succeeding; everything else info (blue) or neutral.

**Files changed** (all inside the contract: the CHANGE files of `T-227-colour-audit.md`, the files of
the BORDERLINE rows the coordinator handed over mid-task, the tests that assert tones, and the three
docs). No backend file changed.

CHANGE group, every row applied (element: before → after):

- `frontend/lib/vaishnava-day.ts` — `kitchenNote` Ekadashi / full-day fast note: warning → info.
- `frontend/app/today/page.tsx` — "Today is a fasting day" and "Tomorrow is a fasting day" banners: warning → info; look-ahead notice: warning-when-fast → info always; meal "Recorded" badge: success → neutral.
- `frontend/app/planner/page.tsx` — Day view Ekadashi notice: warning → info.
- `frontend/components/planner/MealComposer.tsx` — "Fasting day — grain preparations will ask you to confirm": warning → info.
- `frontend/components/planner/DayView.tsx` — Ekadashi badge: accent → info; "This date was corrected by hand": warning → info.
- `frontend/app/calendar/page.tsx` — `BORDER_TONES.ekadasi` border-accent → border-info; `BORDER_TONES.fast` border-warning → border-accent; "Corrected by hand" badge: warning → neutral.
- `frontend/app/planner/reuse/page.tsx` — fast-name badge: danger → info; "not copied" tally: danger → warning; "days left alone" tally: warning → neutral; "Left alone" badge and "happened once" badge: warning → neutral. `Tally` tone type narrowed to `"warning"`.
- `frontend/components/planner/MealServices.tsx` — "Recorded", "Cooked", "Ingredients ready": success → neutral; "Not made": warning → neutral; the volunteer shift pebble when full ("N of N signed up", the chip at audit line 1259): success → neutral.
- `frontend/app/staff-schedule/page.tsx` — "Covered" day chip: success → neutral (`bg-sunken text-ink-secondary`); hours changed by an exception: text-warning → plain (the "Changed"/"Swapped" word carries it); "On … leave" notice: warning → info.
- `frontend/app/leave/page.tsx` — "Approved" success → neutral, "Declined" danger → neutral.
- `frontend/app/profile/page.tsx` — leave-row status text APPROVED/DECLINED → neutral (`statusTone` removed); "✓ You agreed on …" and the ✓ on each always-on category: text-success → text-ink-secondary.
- `frontend/components/IngredientRequestStatus.tsx` — DENIED danger → neutral, ISSUED success → neutral.
- `frontend/app/ingredient-requests/[id]/page.tsx` — "This request was denied, and that is final." warning → info; "The goods have gone over the counter." success → info.
- `frontend/app/ingredient-requests/[id]/edit/page.tsx` — "… has been answered, so it can no longer be changed." warning → info.
- `frontend/app/orders/po-status.tsx` — RECEIVED chip: success → neutral.
- `frontend/app/orders/[id]/page.tsx` — "Sent late", "Never delivered", "Vendor let us down": warning → neutral.
- `frontend/app/vendor-performance/page.tsx` — "N never delivered": warning → neutral.
- `frontend/app/invoices/page.tsx`, `frontend/app/invoices/[id]/page.tsx` — "Paid" chip: success → neutral; "This bill was struck as never owed." warning → info.
- `frontend/app/donations/[id]/page.tsx` — "This gift was struck as wrongly recorded": warning → info.
- `frontend/app/vendors/page.tsx` — "Active": success → neutral. `frontend/app/vendors/[id]/page.tsx` — "Brought back": success → neutral.
- `frontend/app/users/page.tsx` — "Active": text-success → text-ink-secondary.
- `frontend/app/tenants/page.tsx` — 80G "Approved": success → neutral.
- `frontend/app/wishlist/page.tsx` — FULFILLED: success → neutral.
- `frontend/app/shifts/page.tsx` — "You're in": success → neutral.
- `frontend/app/communications/page.tsx` — "Delivered"/"Sent": text-success → text-ink-secondary; "N copies didn't arrive." with retry: warning → danger.
- `frontend/app/communications/composer.tsx` — WhatsApp preview bubble: bg-success-bg → bg-sunken.
- `frontend/components/give/DonatePage.tsx` — "Fully covered" / "₹X to go": success/warning → neutral both.
- `frontend/components/RecipePeek.tsx` — recipe-master badges: success → neutral.
- `frontend/app/ingredients/page.tsx` — both "Prohibited" chips: warning → **info** (the audit offered neutral or info; info chosen because it is Ekadashi's colour everywhere else).
- `frontend/app/operations/page.tsx` — database / worker when up: text-success → text-ink (down stays red).
- `frontend/app/inventory/[id]/page.tsx` — negative movement quantity: text-danger → plain; "Expired" chip and batch "expired" chip: warning → danger ("Expiring soon"/"soon" stay amber).
- `frontend/app/inventory/page.tsx` — "Expired" chip: warning → danger ("Expiring soon" stays amber).
- `frontend/app/donations/new/page.tsx` — both ✕ remove-line buttons: text-danger → text-ink-secondary.

BORDERLINE rows, as Rajeev decided them (relayed by the coordinator):

1. Festival days in the accent colour — **NOT BUILT**, see below.
2. Form-level "fix this" messages red like the field errors: `frontend/components/IngredientRequestForm.tsx` (problem notice), `frontend/app/ingredient-requests/[id]/page.tsx` (both `problem` notices), `frontend/app/register/page.tsx` (message notice): warning → danger. Field errors were already red.
3. "Order today" amber everywhere: `frontend/app/shopping-list/page.tsx` danger → warning; `frontend/components/planner/MealServices.tsx` "Short · order today" danger → warning (same state, third place it appears). Order page was already amber. "Won't arrive in time" stays red.
4. My Schedule "Changed" amber — already amber in `app/my-schedule/page.tsx`; no edit.
5. "Nothing was issued" — `frontend/app/ingredient-requests/[id]/page.tsx`: the red ErrorNotice plus amber note are now one red `role="alert"` block (refusal message, "Nothing was issued", explanation, short lines, next step, action, KMS code). Built inline in the page so the shared `ErrorNotice.tsx` is untouched.
6. "Meta refused this for its formatting" — `frontend/app/whatsapp-templates/page.tsx`: warning → danger.
7. Withdraw-leave confirm — `frontend/app/profile/page.tsx`: red panel/heading/button → `bg-sunken` panel, `text-ink` question, ds `Button` primary.
8. Setup check "Working" — `frontend/app/settings/page.tsx`: `Check` takes `justConfirmed`; "Working" is neutral (`bg-raised text-ink-secondary`, since the checklist itself is sunken) and green only right after Test connection (payments) or Send test (WhatsApp) succeeded in this visit. "Not yet" stays amber. Webhook checks are never green now.

Tests: `frontend/__tests__/staff-schedule.test.tsx` — the covered-day assertion expected one `.bg-success-bg`; now expects none and that "Covered" sits on `bg-sunken`. No other test asserted a changed tone.

Docs: `docs/DESIGN_SYSTEM.md` (v1.9 → v1.10: status line; §3 lead sentence quotes the rule, danger/warning/success meanings rewritten including rulings 2, 3, 6, 7, 8, new `info` and neutral rows, a paragraph on standing states, dated footnote with the old wording; §5 sidebar line 768px → below 1024px / Tailwind `lg`, with dated footnote), `docs/versions/DESIGN_SYSTEM_v1.10.md` (byte copy of the new root file), `docs/CHANGELOG.md` (v1.10 entry after v1.9, same shape).

**Commands run and what came back**

```
$ tools/work-lock.sh run verify 'cd frontend && npx tsc --noEmit; echo "tsc-exit=$?"; npx vitest run > /tmp/kms-t227-vitest.log 2>&1; echo "vitest-exit=$?"; tail -8 /tmp/kms-t227-vitest.log; ...'
tsc-exit=0
vitest-exit=0
 Test Files  143 passed (143)
      Tests  1957 passed (1957)
   Duration  18.35s
work-lock: 'verify' released; command exited 0
```

First run, before the test update, failed exactly one test:
```
 FAIL  __tests__/staff-schedule.test.tsx > staff schedule > colours by shortfall and still states it in text
AssertionError: expected +0 to be 1
 Test Files  1 failed | 142 passed (143)
```

```
$ cd frontend && npx eslint $(git diff --name-only -- . | ... .ts/.tsx); echo "eslint-exit=$?"
/Users/Rajeev/.../frontend/tailwind.config.ts
  0:0  warning  File ignored because no matching configuration was supplied
✖ 1 problem (0 errors, 1 warning)
eslint-exit=0
```
(tailwind.config.ts is another agent's change, listed only because the lint took every modified file.)

`next build` not run, per instruction. No backend tests: no backend file changed.

**Acceptance criteria**

- Every CHANGE row applied: each row above, found by element text; verified by reading each site after the edit and by tsc/vitest/eslint green.
- KEEP rows untouched: no KEEP site edited, except "Short · order today" in MealServices, which the audit had grouped under KEEP and Rajeev's ruling 3 moved to amber.
- BORDERLINE rows: 2, 3, 5, 6, 7, 8 applied; 4 needed no edit; 1 held (below).
- DESIGN_SYSTEM amended, versioned, snapshotted and changelogged like v1.9: diff above; snapshot is a copy made after the last edit.

**Not done / needs Rajeev**

- **Festival days in the accent colour (ruling 1) is not built.** On the calendar the accent already marks an ordinary fasting day (cell, dot, legend, and now the event rule), so festival and fast would draw identically and the legend would show two identical dots. Festival stays green on the calendar and planner until he picks: festivals take the accent and fasts move to something else (say which), or festivals get a fourth non-status colour. The CHANGELOG entry says this is waiting.
- The §5 1024px line is taken from the coordinator's statement that the menu switches at `lg`; I did not open `Sidebar.tsx` (off-limits) to confirm.
- No hand smoke-test in a browser; tests only.
- Judgement calls: ingredient "Prohibited" as info rather than neutral; exception hours in the staff grid plain rather than info; settings "Working" chip on `raised` rather than `sunken` so it shows on the sunken checklist.
