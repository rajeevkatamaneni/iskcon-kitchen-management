# T-291 — Rajeev's Q-1, Q-13, Q-14, Q-15, Q-17 answers recorded in code comments (no behaviour change)

**Files changed (all inside the contract, comments only):**
- backend/src/main/java/org/iskcon/kms/ingredient/PackSizeService.java — two "provisional Q-14" markers now say Rajeev confirmed it as built, 2026-09-19.
- backend/src/main/java/org/iskcon/kms/receiving/DeliveriesController.java — class comment: Kitchen Staff are now listed as holding RECEIVE_DELIVERIES (Q-1 answered 2026-09-19, by default); the grant is in RolePermissions.
- backend/src/main/java/org/iskcon/kms/receiving/ReceivingController.java — class comment: the "refused until Rajeev answers Q-1" paragraph replaced with his answer (by default, 2026-09-19). The quoted question is §12's own wording, not a quote of Rajeev.
- frontend/app/invoices/page.tsx — "conductor, provisional ruling Desk Q-17" becomes "Desk Q-17: Rajeev confirmed it as built, 2026-09-19".
- docs/work/proof/T-291.md (this file).

Source for the decisions: docs/work/PROCUREMENT-PROGRESS.md "Decisions", lines 36 and 47 (a paraphrase, so no quotation of Rajeev is used).

**Commands run and what came back:**

Before-copies were taken from the working tree before editing (to scratchpad), then `git diff --no-index before after` for each file. Check that every changed line is a comment line:
```
grep -E '^[+-][^+-]' diff.txt | grep -vcE '^[+-]\s*(\*|//)'
non-comment changed lines: 0
```
Full diff:
```diff
diff --git a/private/tmp/claude-501/-Users-Rajeev-Workspace-kitchen-management-system/79bf5e51-4f9e-420c-b419-747d070a92df/scratchpad/t291/PackSizeService.java.before b/backend/src/main/java/org/iskcon/kms/ingredient/PackSizeService.java
index 612c975..5caef9e 100644
--- a/private/tmp/claude-501/-Users-Rajeev-Workspace-kitchen-management-system/79bf5e51-4f9e-420c-b419-747d070a92df/scratchpad/t291/PackSizeService.java.before
+++ b/backend/src/main/java/org/iskcon/kms/ingredient/PackSizeService.java
@@ -44,8 +44,9 @@ import org.springframework.transaction.annotation.Transactional;
  *   <li><b>No duplicate</b> ({@code PACK_SIZE_ALREADY_THERE}, KMS-400157) — <em>translated only</em>,
  *       not pre-checked. The unique index {@code ingredient_pack_sizes_no_duplicate} compares the
  *       size in the family's base unit, so "500 gm" and "Pack = 0.5 Kg" collide whatever they are
- *       called (provisional, Q-14). A pre-check here would be a second statement of the same rule
- *       that could only ever agree or be wrong, so the index is asked and its answer translated.
+ *       called (Q-14: Rajeev confirmed this as built, 2026-09-19). A pre-check here would be a
+ *       second statement of the same rule that could only ever agree or be wrong, so the index is
+ *       asked and its answer translated.
  *   <li><b>Same unit family</b> ({@code INCOMPATIBLE_UNIT}, KMS-400013) — <em>both</em>. The
  *       pre-check is {@link IngredientUnits#requireSameFamily}, the one rule every quantity in the
  *       product obeys, and it is there because it says <em>which</em> ingredient and which units in
@@ -151,7 +152,8 @@ public class PackSizeService {
 			String state = sqlState(e);
 			if (UNIQUE_VIOLATION.equals(state)) {
 				// The only unique index a fresh row can meet: the same size in base units as a pack
-				// this ingredient already has, whatever either is called (V144, provisional Q-14).
+				// this ingredient already has, whatever either is called (V144; Q-14, confirmed
+				// by Rajeev as built, 2026-09-19).
 				throw new ApplicationException(ErrorCode.PACK_SIZE_ALREADY_THERE,
 						Map.of("ingredientId", ingredientId, "quantity", request.quantity(), "unit", unit.name()), e);
 			}
diff --git a/private/tmp/claude-501/-Users-Rajeev-Workspace-kitchen-management-system/79bf5e51-4f9e-420c-b419-747d070a92df/scratchpad/t291/DeliveriesController.java.before b/backend/src/main/java/org/iskcon/kms/receiving/DeliveriesController.java
index 987f231..fd47676 100644
--- a/private/tmp/claude-501/-Users-Rajeev-Workspace-kitchen-management-system/79bf5e51-4f9e-420c-b419-747d070a92df/scratchpad/t291/DeliveriesController.java.before
+++ b/backend/src/main/java/org/iskcon/kms/receiving/DeliveriesController.java
@@ -16,10 +16,11 @@ import org.springframework.web.bind.annotation.RequestParam;
 import org.springframework.web.bind.annotation.RestController;
 
 /**
- * The Deliveries screen (R-DEL-1..5), behind {@code RECEIVE_DELIVERIES}: Temple Admin and Kitchen
- * Manager. Kitchen Staff are refused until Rajeev answers open question Q-1. The tenant is the
- * signed-in user's, set on the connection by the security filter; nothing here takes one from the
- * request.
+ * The Deliveries screen (R-DEL-1..5), behind {@code RECEIVE_DELIVERIES}: Temple Admin, Kitchen
+ * Manager and Kitchen Staff. Rajeev answered open question Q-1 on 2026-09-19: Kitchen Staff get
+ * RECEIVE_DELIVERIES by default, not only named staff; the grant is in {@code RolePermissions}.
+ * The tenant is the signed-in user's, set on the connection by the security filter; nothing here
+ * takes one from the request.
  */
 @RestController
 @RequestMapping("/api/v1/deliveries")
diff --git a/private/tmp/claude-501/-Users-Rajeev-Workspace-kitchen-management-system/79bf5e51-4f9e-420c-b419-747d070a92df/scratchpad/t291/ReceivingController.java.before b/backend/src/main/java/org/iskcon/kms/receiving/ReceivingController.java
index b2cf583..ead9347 100644
--- a/private/tmp/claude-501/-Users-Rajeev-Workspace-kitchen-management-system/79bf5e51-4f9e-420c-b419-747d070a92df/scratchpad/t291/ReceivingController.java.before
+++ b/backend/src/main/java/org/iskcon/kms/receiving/ReceivingController.java
@@ -25,10 +25,10 @@ import org.springframework.web.bind.annotation.RestController;
  * {@code MANAGE_PURCHASE_ORDERS}: the order page reads its delivery history from it, and whoever can
  * see an order can see what arrived against it.
  *
- * <p>Kitchen Staff hold {@code MANAGE_PURCHASE_ORDERS} but not {@code RECEIVE_DELIVERIES}, so this
- * takes recording a delivery away from them until Rajeev answers open question Q-1 ("Does Kitchen
- * Staff get RECEIVE_DELIVERIES by default, or only named staff?"). That is the question's to settle,
- * in {@code RolePermissions}, not this controller's.
+ * <p>Kitchen Staff hold both. Open question Q-1 asked "Does Kitchen Staff get RECEIVE_DELIVERIES by
+ * default, or only named staff?", and Rajeev answered it on 2026-09-19: by default. The grant lives
+ * in {@code RolePermissions}, not in this controller, so any later change to who records deliveries
+ * is made there and reaches both screens at once.
  */
 @RestController
 @RequestMapping("/api/v1/purchase-orders/{poId}/receipts")
diff --git a/private/tmp/claude-501/-Users-Rajeev-Workspace-kitchen-management-system/79bf5e51-4f9e-420c-b419-747d070a92df/scratchpad/t291/page.tsx.before b/frontend/app/invoices/page.tsx
index bb4028d..02b9e70 100644
--- a/private/tmp/claude-501/-Users-Rajeev-Workspace-kitchen-management-system/79bf5e51-4f9e-420c-b419-747d070a92df/scratchpad/t291/page.tsx.before
+++ b/frontend/app/invoices/page.tsx
@@ -109,7 +109,7 @@ function owedMatches(filter: Filter, p: PayableView, today: string): boolean {
 
 function InvoicesView() {
   const { appUser } = useAuth();
-  // THE one condition (conductor, provisional ruling Desk Q-17, 2026-09-19): the total owed and the
+  // THE one condition (Desk Q-17: Rajeev confirmed it as built, 2026-09-19): the total owed and the
   // filters that age it are shown only to people who can pay, which is MANAGE_VENDOR_PAYMENTS on the
   // server and, of the roles that open this page, the Temple Admin alone. The same test the invoice's
   // own page makes for its payments. A Kitchen Manager would otherwise be offered figures the API
```

`tools/work-lock.sh run verify "cd backend && ./gradlew compileJava -q ...; cd ../frontend && npx tsc --noEmit ...; npx eslint app/invoices/page.tsx"`:
```
BACKEND_EXIT=0
TSC_EXIT=2
ESLINT_EXIT=0
work-lock: 'verify' released; command exited 0
```
Full tsc error count by file (second run through the lock):
```
total_error_lines=11
5 __tests__/deliveries.test.tsx
2 .next/types/app/dev-deliveries/page.ts
2 .next/types/app/dev-invoices/page.ts
2 .next/types/app/dev-po/page.ts
```
All 11 are the known unrelated ones (stale .next dev-* types; deliveries.test.tsx missing `returns`). None in app/invoices/page.tsx.

**Acceptance criteria:**
- Markers replaced for Q-1, Q-14, Q-17 in the four named files: done, diff above.
- Comment-only: 0 non-comment lines changed (check above).
- Backend compiles: compileJava exit 0. Frontend: no tsc error in invoices/page.tsx, eslint clean.

**Not done / needs Rajeev (markers outside my paths, reported not edited):**
- frontend/app/deliveries/page.tsx:53 — still says whether Kitchen Staff get it "is open with Rajeev (Q-1), so they are refused here".
- frontend/app/orders/[id]/page.tsx:217 and :451 — still say Q-1 is open and Kitchen Staff do not hold RECEIVE_DELIVERIES.
- backend/src/main/resources/db/migration/V144__procurement_data_model.sql:79 — rules marked PROVISIONAL (Q-13, Q-14). Applied migration; must not be edited.
- No Q-13 or Q-15 marker found in backend/src/main, frontend/app, frontend/components, frontend/lib outside migrations. The progress file does not say what Q-15 was ("all eight"), so a Q-15 marker worded differently could have been missed.
- Already done elsewhere: RolePermissions.java:147, nav.ts:94, ErrorCode.java:1049.
- No hand smoke test: comments only, nothing a user can see changed.
