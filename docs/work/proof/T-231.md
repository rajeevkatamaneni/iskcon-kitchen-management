# T-231 — tighter meal-kind gap on the meal edit screen; recipe cost said as "₹8,000 per batch (270 L)"

**Files changed:** (all frontend, all named by the task or the new test for it; the first four already
carried other builders' uncommitted edits, which are untouched)

- `frontend/components/planner/MealComposer.tsx`: the first step-1 row gets `mt-3` on edit and keeps `mt-11` on a new meal. Comment explains the 24px.
- `frontend/lib/format.ts`: new `batchCost(cost, qty, unit, currency="INR")`, built from `money` (en-IN grouping) and `cooksQuantity`.
- `frontend/components/RecipeForm.tsx`: label is now "Rough cost of one batch (₹)", with a live "for 270 L" beside the box. It is hidden while "This recipe makes" is empty or 0. The API field is still `indicativeCost`.
- `frontend/app/recipes/library/[id]/page.tsx`: the fact now reads "Rough cost" and shows "₹8,000 per batch (270 L)".
- `frontend/__tests__/recipe-batch-cost.test.tsx` (new file).
- `docs/work/proof/T-231.md` (this file).

**Commands run and what came back:**

`tools/work-lock.sh run verify 'cd frontend && npx vitest run __tests__/recipe-batch-cost.test.tsx __tests__/recipe-form-units.test.tsx __tests__/design-system.test.ts __tests__/meal-composer.test.tsx'`
```
 ✓ __tests__/meal-composer.test.tsx  (64 tests) 4524ms
 Test Files  4 passed (4)
      Tests  106 passed (106)
work-lock: 'verify' released; command exited 0
```

`tools/work-lock.sh run verify 'cd frontend && npx tsc --noEmit; echo TSC_EXIT=$?; npx vitest run | grep ...; npx eslint <the five files>; echo ESLINT_EXIT=$?'`
```
TSC_EXIT=0
 Test Files  145 passed (145)
      Tests  1985 passed (1985)
ESLINT_EXIT=0
work-lock: 'verify' released; command exited 0
```

**Acceptance criteria:**

- The edit-screen gap is tightened and the new-meal screen is unchanged. Checked in Chrome on `/planner/meal/cf25562c-…`. The step title's bottom is at 156, the badge runs 171–191, and the "Ready by" label's top is at 218, so the gap is about 24px. It was 56px (12 + 44). The new-meal screen keeps `mt-11`, and the design-system tests pass.
- The form label and the live batch size work. Checked in Chrome on Basmati Ghee Rice's edit form: the label reads "Rough cost of one batch (₹)", the box holds 8000, and "for 100 L" sits beside it. The new tests cover the text following a change, and hiding while the batch size is empty.
- The display format is right. The `batchCost` tests cover "₹8,000 per batch (270 L)", "₹1,50,000 per batch (40 Kg)", 0.5 Kg shown as "500 gm", no size, and no cost.
- The API field name is unchanged. A test asserts the submit still sends `indicativeCost: 8000`.

**Not done / needs Rajeev:**

- The cost appears on only one screen: the library recipe page. `grep indicativeCost` finds no display of it on the temple recipe page, the peek or the print. Basmati Ghee Rice has ₹8,000 saved, but its recipe page shows no cost at all. I did not add it there, because that would be a new line on the page and needs his yes. If he says yes, it is a one-line `batchCost(...)` fact in `app/recipes/[id]/page.tsx`.
- I did not open the library recipe page in the browser, because I could not find a link to a library recipe quickly. It uses the same tested helper.
