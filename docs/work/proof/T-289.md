# T-289 — "create" for making an order everywhere (D-4), the order page matched to mock E (D-5 a–c), and the Deliveries link for Kitchen Staff (Q-1)

**Files changed (all inside the contract):**
- `frontend/app/orders/[id]/page.tsx`
- `frontend/app/shopping-list/page.tsx` (wording, one comment, and the state variable `raised` renamed `created`)
- `frontend/__tests__/po-merged-table.test.tsx`
- `frontend/__tests__/shopping-list.test.tsx`
- `docs/work/proof/T-289.md`

The other test files in the contract needed no change (they pass as they were; output below).

## What changed

**D-4, wording.** Before → after:
- Shopping list confirmation: "PO-2026-0054 raised for VERIFY-B Grains." → "PO-2026-0054 was created for VERIFY-B Grains." (matches /orders' "…was created").
- Shopping list subtitle: "…then raise that vendor’s purchase order." → "…then create that vendor’s purchase order."
- Shopping list error fallback: "We couldn’t raise that order." → "We couldn’t create that order."
- Order page, draft editor intro: "…Cancel this order at the foot of the page and raise it against the right one." → "…and create a new one for the right vendor."
- Order page, draft editor date error: "That date is before the order was raised." → "…before the order was created." (a third user-facing one the grep found).
- Order page, cancel panel: "It cannot be undone — raise a new order if it is needed again." → "…— create a new order if it is needed again."

Grep after the change (every remaining hit is a code comment, a quotation of Rajeev in a comment, or the `bg-raised` colour class; none is shown to a user):
```
$ grep -n -i "\braise\|\braised\b" "app/orders/[id]/page.tsx" app/shopping-list/page.tsx
app/orders/[id]/page.tsx:704:                      as a span — raised then, wanted by then — and until 2026-09-05 the first half
app/orders/[id]/page.tsx:719:                      when the order was raised and whether it has gone are facts the form does not
app/orders/[id]/page.tsx:925:                    <ul className="grid gap-1 rounded border border-hairline bg-raised px-5 py-4 text-sm">
app/orders/[id]/page.tsx:1313:                PO that raised and sent the 500KG rice order and it should sit in a partially
app/shopping-list/page.tsx:148: * one button at the top raised four orders at once without ever showing which was which.
app/shopping-list/page.tsx:181:  // The catalogue, for the picker below the table. Its own failure is deliberately not raised as
app/shopping-list/page.tsx:342:   * The server used to offer {@code POST /purchase-orders/generate}, which raised one order per
app/shopping-list/page.tsx:349:   * <p>The notes line is the one the generator used to write, kept deliberately: an order raised
app/shopping-list/page.tsx:396:              One button at the top raised an order to every vendor at once: "one Generate button at
app/shopping-list/page.tsx:415:                  "Was created", not "raised" (T-289, D-4): R-PO-1 named the act "Create", and
app/shopping-list/page.tsx:831: * The purchase order a tile is about to raise, over the list it came from (T-134).
```

Not changed, as instructed: the WhatsApp purchase-order template's "It was raised on" (approved Meta template). It lives in `backend/src/main/java/org/iskcon/kms/notification/NotificationTemplate.java`, tested in `NotificationTemplateTest.java` and `PurchaseOrderWhatsAppIT.java`, and is recorded in `docs/CHANGELOG.md`. `frontend/components/PurchaseOrderEditor.tsx` also matches "was raised on", in a comment (line 159); T-288's file, not touched.

**D-5, the order page against mock E** (`docs/work/mocks/dev-po.page.tsx`, `DesignE`, lines 1325–1460):
- (a) Status chip: now semibold, 20px high. The chip is `statusChip()` in `app/orders/po-status.tsx`, shared with the orders list and the invoice's order picker. Changing it there would change those two screens, so per the brief it is fixed for this page only: the page wraps it in a span with `[&>span]:py-0.5 [&>span]:font-semibold`. Why not the design system's `Badge`, which the mock uses: `Badge` has no tone for Cancelled's muted text (`text-ink-muted`), so switching would make a cancelled order's chip a different colour here than on the list. The wrapper keeps every status's colours identical to the list and changes only weight and height. Recorded in a comment beside it. The shared chip on the list and invoice picker is still regular weight, 24px; putting that in step belongs to whoever owns `po-status.tsx`.
- (b) "Items": `font-medium` (500). It stays an `h2`: the mock uses an `h3`, whose base style is medium, but under this page's `h1` the section heading is an h2, so the weight is set on it directly.
- (c) The card is the mock's `card py-5` with `px-6` from 1024px. The heading row uses the mock's classes (`flex flex-wrap items-center justify-between gap-3`), and the table's wrapper is `mt-3`, as the mock's table is.
  - Below 1024px the card's side padding stays off. That is a judgement call against the mock's literal classes, and the lesser evil: there the rows turn into cards with their own 24px inset, so with the mock's padding as well the item names start 49px in, 24px right of "Items" (measured on the mock, below), and a 358px phone card loses 48px for nothing. The layout rule's intent is names lined up under their heading, and that is kept: 25 against 25.
  - The `overflow-x-auto` wrapper stays, so the table can never push the page sideways.
- (d) The "Close this order, part delivered" panel is untouched (Rajeev's call).

**The Deliveries link (Q-1).** Shown to Temple Admin, Kitchen Manager and Kitchen Staff (Rajeev's answer to Q-1, 2026-09-19). Both stale "Q-1 is open" comments now record his decision and its date. The "Q-8 is open" comment now records his decision: no per-item record button. None was added.

## Measurements (DOM, running app, signed in as Karuna Murti Das, Temple admin, PO-2026-0054 "Part delivered")

Chrome's port 9222 was not listening, so the Chrome extension was used. The window would not resize (`innerWidth` stayed 1470), so each width was measured in a same-origin iframe of exactly that width loading the real page (media queries follow the iframe's width).

The mock's figures come from its own markup (the header line, Badge classes, `card px-6 py-5`, the heading row, `RULED_TABLE mt-3`), rendered in an iframe with the running app's own stylesheets. The badge and heading figures match VERIFY-B's measurement of the mock (600/20, 500).

| | mock 1280 | real 1280 | mock 1024 | real 1024 | mock 390 | real 390 |
|---|---|---|---|---|---|---|
| badge weight / height | 600 / 20 | 600 / 20 | 600 / 20 | 600 / 20 | 600 / 20 | 600 / 20 |
| "Items" weight / size | 500 / 18px | 500 / 18px | 500 / 18px | 500 / 18px | 500 / 18px | 500 / 18px |
| heading x in card | 25 | 25 | 25 | 25 | 25 | 25 |
| table left / right inset in card | 25 / 25 | 25 / 25 | 25 / 25 | 25 / 25 | 25 / 25 | 1 / 1 (deviation above) |
| first item name x | 45 | 45 | 45 | 45 | 49 | 25 |
| table top below heading row | 12 | 12 | 12 | 12 | 12 | 12 |

Before, real 1280 (same page, same session): badge 400 / 24, "Items" 600, table x 1 and right gap 1 (flush).

Nothing cut off at any width. At all three widths `document.scrollWidth` equals the viewport (1280 / 1024 / 390). The table wrapper's scrollWidth equals its clientWidth (886/886, 630/630, 356/356), and no cell, link, button or heading lies outside the card. Screenshots at 1024 and 1280 were checked by eye as well.

At 1024 the 48px inset costs the Item column: it is 97px, against 122px before. Names wrap to two or three lines at word breaks ("VERIFY-B / Flour / 100kg"); no word is broken. This is the mock's own consequence at that width.

The shopping list subtitle was read off the running page: "…check the lines in a tile, then create that vendor’s purchase order." The confirmation and the error were not shown live, because creating an order writes to the database; both are covered by tests.

## Commands run and what came back

```
tools/work-lock.sh run verify 'cd .../frontend && npx vitest run __tests__/po-merged-table.test.tsx __tests__/order-detail.test.tsx __tests__/closing-a-part-delivered-order.test.tsx __tests__/goods-return.test.tsx __tests__/shopping-list.test.tsx __tests__/shopping-list-add.test.tsx __tests__/shopping-list-packs.test.tsx __tests__/shopping-list-no-vendor.test.tsx | grep ...; npx tsc --noEmit | grep "error TS" | sed | sort | uniq -c; npx eslint <4 touched source/test files>'

 ✓ __tests__/shopping-list-add.test.tsx  (7 tests) 313ms
 ✓ __tests__/goods-return.test.tsx  (7 tests) 472ms
 ✓ __tests__/shopping-list-packs.test.tsx  (13 tests) 548ms
 ✓ __tests__/closing-a-part-delivered-order.test.tsx  (26 tests) 543ms
 ✓ __tests__/shopping-list-no-vendor.test.tsx  (10 tests) 616ms
 ✓ __tests__/po-merged-table.test.tsx  (20 tests) 657ms
 ✓ __tests__/shopping-list.test.tsx  (21 tests) 664ms
 ✓ __tests__/order-detail.test.tsx  (27 tests) 766ms
 Test Files  8 passed (8)
      Tests  131 passed (131)
--- tsc
   5 __tests__/deliveries.test.tsx
   2 .next/types/app/dev-invoices/page.ts
   2 .next/types/app/dev-po/page.ts
--- eslint
eslint exit 0
work-lock: 'verify' released; command exited 0
```
tsc: only the allowed stale `.next/types/app/dev-*` files and `__tests__/deliveries.test.tsx`; none in a touched file.

**Control (red, then restored).** Both source files backed up and hashed. Then: Kitchen Staff taken out of the link check, the chip wrapper's classes removed, `font-medium` removed, "create a new order" put back to "raise a new order", and the confirmation put back to "raised for". Run through the lock:
```
   × shopping list > a tile per vendor > creates one order for that vendor on save, and says so by name
   × ... > sets the status chip as mock E's badge: semibold and 20px high, on this page
   × ... > gives 'Items' the mock's medium weight and insets the table inside the card from 1024px
   × ... > calls creating an order 'create', never 'raise', when it offers a new one
   × ... > links across to the Deliveries screen, on this order, for every kitchen role
   × ... > shows the link to Kitchen Staff, who record deliveries since Rajeev's answer to Q-1
 Test Files  2 failed (2)
      Tests  6 failed | 35 passed (41)
work-lock: 'verify' released; command exited 0
```
Restore: the backups copied back, and `shasum` diffed against the hashes taken before the control. The diff was empty, and the check printed "restored byte-identical".

## Tests changed
- `po-merged-table`:
  - The link test now covers all three roles.
  - "does not show the link to Kitchen Staff…" is flipped: they see it, with `href=/deliveries?order=po1`.
  - New: the chip's wrapper classes, with the shared chip left as `py-1`.
  - New: "Items" is `font-medium`, the card is `card py-5 lg:px-6`, and the table's wrapper is `mt-3`.
  - New: the cancel text says "create a new order", and there is no "raise" anywhere on a Sent order's page.
- `shopping-list`: the confirmation is now asserted as exactly "PO-2026-0041 was created for Heritage Fresh Dairy.", and its absence as `/was created for/i`.

## Acceptance criteria
- No user-facing "raise/raised" about orders in the two files: the grep above.
- D-5 numbers match the mock at 1280, and at 1024. At 390 the inset is deliberately different (reasoned above), and nothing is cut off at any width: the measurements table.
- Tests updated for the wording and the link, with the control red and the restore byte-identical: above.
- tsc, eslint and vitest on the touched files: above.

## Not done / needs Rajeev
- **The Kitchen Staff link leads to a refusal today.** `frontend/app/deliveries/page.tsx` still gates the screen with `RequireRole roles={["TEMPLE_ADMIN", "KITCHEN_MANAGER"]}`, and its comment still says Q-1 is open. So a Kitchen Staff member who presses the new link is told they cannot open that screen. That file, the menu's roles (`nav.ts`) and the server's `RolePermissions` are outside this contract. I did not open `RolePermissions.java`, so I have not checked whether the server grant is in yet. Someone owning those files must let Kitchen Staff in, or the link is a door that refuses them.
- The orders list and the invoice picker still show the status chip at regular weight, 24px high: `po-status.tsx` is not mine (see (a)).
- The shopping list tile's button still reads "Generate purchase order", a third word for the same act, and the order page's header says "Generated <date>". Neither is "raise", so both were left. Flagged for the D-4 consistency question.
- Kitchen Staff's view of the page was checked in tests only, not on the running app; I was signed in as Temple Admin only.
- No hand smoke-test of the shopping list confirmation and error on the running app (it would create an order in the database).
