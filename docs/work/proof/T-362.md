# T-362 — the standard menu Rajeev settled on 2026-09-19, and "Issued from store" renamed "Issued to kitchens"

**Files changed** (every one inside the contract):

- `frontend/lib/nav.ts` — the seven groups in his order, sentence case, first group unheaded; the
  header comment now says the order is his specification and points at
  `docs/work/MENU-LAYOUT-2026-09-19.md`.
- `frontend/app/issued-from-store/page.tsx` — page title/heading `Issued to kitchens`, and the
  file comment records the rename and why the address did not change.
- `frontend/__tests__/nav.test.ts` — the whole menu asserted per role, plus the rename and the
  Cost-per-serving placement; two old tests rewritten where his order contradicted them.
- `frontend/__tests__/issued-from-store.test.tsx` — heading and describe name.
- `docs/uat/UAT-076-issued-from-the-temple-store.md` — start line, menu step, heading step, pass box.
- `docs/uat/UAT-075-cost-per-serving.md` — it is no longer beside *Issued from store*; it is last in
  the top group.
- `docs/uat/UAT-067-set-up-the-temples-kitchens.md` — the menu step now reads the **Kitchens** group,
  and the item is **All kitchens**.
- `docs/uat/UAT-068-ask-the-store-for-ingredients.md`, `docs/uat/UAT-084-when-the-grinder-is-due.md`,
  `docs/uat/UAT-066-write-to-the-community.md` — group names on the "Start at" line.
- `docs/stories/EPIC-10-kitchens-and-issuing.md`, `docs/stories/EPIC-3-inventory-management.md` — the
  two story bullets that named the old label and the old group.
- `docs/work/proof/T-362.md` — this file.

**Not changed, and confirmed by reading them:** `frontend/components/Sidebar.tsx` and
`frontend/components/RequireRole.tsx` needed nothing — Sidebar renders whatever `navForRole` returns
and already drops an untitled group's heading; RequireRole only reads `plannerRefused` and
`isMealPlannerPath`, both untouched. Both show as `M` in `git status` from **T-359's** Epic 12 work,
not from me (`git diff` on them is the `canPlanMeals` plumbing, which I left exactly as found).

**The route decision: `/issued-from-store` stays.** Only the words change. Renaming the route would
break every existing link and bookmark, the backend path `/api/v1/issued-from-store`, and the
`IssuedFromStoreController`/`IssuedFromStoreIT` names, for no gain a reader can see — a reader sees
the heading, not the address bar. It is written into the nav item's comment and the page's comment so
the next person does not "fix" the mismatch.

---

## Commands run, and what came back

### 1. Types, lint and the tests that touch the menu (through the lock)

```
tools/work-lock.sh run verify 'cd frontend && npx tsc --noEmit; echo TSC_EXIT=$?; npx eslint --max-warnings=0 lib/nav.ts components/Sidebar.tsx components/RequireRole.tsx app/issued-from-store/page.tsx __tests__/nav.test.ts __tests__/issued-from-store.test.tsx; echo ESLINT_EXIT=$?; npx vitest run __tests__/nav.test.ts __tests__/issued-from-store.test.tsx __tests__/phone-menu.test.tsx __tests__/sidebar-scroll.test.tsx __tests__/sidebar-names.test.tsx __tests__/role-refusals.test.tsx __tests__/refusal-has-a-way-out.test.tsx __tests__/design-system.test.ts 2>&1 | tail -30; echo VITEST_EXIT=$?'
```

```
TSC_EXIT=0
ESLINT_EXIT=0
The CJS build of Vite's Node API is deprecated. See https://vitejs.dev/guide/troubleshooting.html#vite-cjs-node-api-deprecated for more details.

 RUN  v2.1.2 /Users/Rajeev/Workspace/kitchen-management-system/.claude/worktrees/agent-a93967b8b0080da44/frontend

 ✓ __tests__/nav.test.ts  (24 tests) 7ms
 ✓ __tests__/sidebar-scroll.test.tsx  (5 tests) 48ms
 ✓ __tests__/sidebar-names.test.tsx  (8 tests) 76ms
stderr | __tests__/phone-menu.test.tsx
Error: Not implemented: navigation (except hash changes)
    at module.exports (…/jsdom/lib/jsdom/browser/not-implemented.js:9:17)
    …
 ✓ __tests__/phone-menu.test.tsx  (8 tests) 152ms
 ✓ __tests__/issued-from-store.test.tsx  (10 tests) 177ms
 ✓ __tests__/refusal-has-a-way-out.test.tsx  (9 tests) 144ms
 ✓ __tests__/role-refusals.test.tsx  (15 tests) 230ms
 ✓ __tests__/design-system.test.ts  (23 tests) 214ms

 Test Files  8 passed (8)
      Tests  102 passed (102)
   Start at  16:50:28
   Duration  1.94s (transform 2.82s, setup 604ms, collect 4.66s, tests 1.05s, environment 2.45s, prepare 378ms)

VITEST_EXIT=0
work-lock: 'verify' released; command exited 0
```

The jsdom "Not implemented: navigation" line is `phone-menu.test.tsx`'s own, pre-existing, and the
file passes.

### 2. Negative control — `control-T-362.sh`

The script is at
`/private/tmp/claude-501/-Users-Rajeev-Workspace-kitchen-management-system/79bf5e51-4f9e-420c-b419-747d070a92df/scratchpad/control-T-362.sh`.
It copies `nav.ts` out, puts **Recipes above Inventory** in *Inventory & Recipes* (one group back to
its old order), runs `nav.test.ts`, then restores from the copy under a trap and `cmp`s.

```
tools/work-lock.sh run verify …/control-T-362.sh
```

```
control: swapped Inventory and Recipes
--- nav.test.ts with the group reordered (must FAIL) ---
 ❯ __tests__/nav.test.ts:59:38
     57|     // Every destination under Temple is the admin's, so for a cook th…
     59|     expect(menuFor("KITCHEN_STAFF")).toEqual([
 Test Files  1 failed (1)
      Tests  3 failed | 21 passed (24)
CONTROL_VITEST_EXIT=1
--- restored; cmp against the copy taken before the control ---
CMP_IDENTICAL=yes
--- nav.test.ts restored (must PASS) ---
 ✓ __tests__/nav.test.ts  (24 tests) 4ms
 Test Files  1 passed (1)
      Tests  24 passed (24)
work-lock: 'verify' released; command exited 0
```

Three tests catch a single swapped pair (the temple admin's whole menu, the manager's, the cook's),
`cmp` says the file came back byte-identical, and the restored run is green.

### 3. The grep: no user-facing "Issued from store" survives

```
grep -rn "Issued from store\|issued from store" --include=*.ts --include=*.tsx --include=*.md . \
  | grep -v node_modules | grep -v docs/work/ | grep -v docs/CHANGELOG.md
```

```
frontend/components/ds/PeriodNav.tsx:36: *               (issued from store, vendor performance, cost per serving) are read a period at a
frontend/__tests__/nav.test.ts:72:  it("renames Issued from store to Issued to kitchens, keeping the address (2026-09-19)", () => {
frontend/__tests__/nav.test.ts:79:      expect(items.map((i) => i.label)).not.toContain("Issued from store");
frontend/lib/nav.ts:166:      // Renamed from "Issued from store" (Rajeev, 2026-09-19, chosen over "Out of the store"). Same
docs/stories/EPIC-10-kitchens-and-issuing.md:351:  *(Renamed from* Issued from store *and moved out of the old* Kitchen *group by Rajeev's standard
```

Every survivor is a code comment, a test asserting the old name is *gone*, or a doc recording the
rename. Nothing a user reads. The excluded paths are `docs/work/` (proofs and ledgers, which are
dated records of what was true then) and `docs/CHANGELOG.md`, which the contract forbids.

---

## Measured: the sidebar at 1280 and at 390

**How.** The Sidebar's own markup was dumped from the real component (rendered through
`@testing-library/react` as a Temple Admin, closed and with the drawer open), wrapped in the app's
document shell, and served same-origin with the running dev server's **real** stylesheet
(`/_next/static/css/app/layout.css` on :3100, proxied so the webfonts load under CORS). Measured in
headless Chromium 1134 at `deviceScaleFactor: 1` with `getBoundingClientRect` and `Range` geometry —
no eyeballing, and the screenshots below are full-resolution.

Scripts and output in the scratchpad: `measure.mjs`, `measure2.mjs`, `measure3.mjs`,
`T-362-measurements.json`, `T-362-measurements-bottom.json`.

| | 1280 (column) | 390 (drawer open) | 390 (closed) |
|---|---|---|---|
| Menu width | 280px | 280px | 0 — the column is `display: none`, as designed |
| Scroller viewport | 271 × 674 | 271 × 618 | — |
| Content height | 1844px | 1844px | — |
| Scroll needed | 1170px | 1226px | — |
| Gaps between the 7 groups | 24, 24, 24, 24, 24, 24 | 24, 24, 24, 24, 24, 24 | — |
| Items | 33 | 33 | — |
| Labels on more than one line | none | none | — |
| Labels clipped (`scrollWidth − clientWidth`) | 0px on all 33 | 0px on all 33 | — |
| Horizontal page scroll | 0 | 0 | — |

Group boxes at both widths, top-of-content offsets: `(no heading)` 0–236 · `Ordering` 260–568 ·
`Inventory & Recipes` 592–852 · `Kitchens` 876–1040 · `People` 1064–1372 ·
`Giving & Outreach` 1396–1560 · `Temple` 1584–1844. Even 24px between every pair, no exception.

Widest label: **Vendor performance**, 138.2px of text in a 247px row — 108px of slack, which is why
nothing wraps. A row is 44px (`min-h-touch`) and a heading block costs 24px (heading + `mb-1` +
`gap-1`), both measured.

**The drawer reaches everything.** Scrolled to the end at 390: `scrollTop 1226 = maxScroll`, last
item **Settings**, wholly inside the scroller (bottom 748.5 against the scroller's 748 — half a pixel
of sub-pixel rounding, not a clip). Same at 1280: `scrollTop 1170 = maxScroll`, Settings whole.

**Screenshots** (in the scratchpad):
`T-362-1280-closed.png` (top of the column), `T-362-1280-kitchens.png` (the *Inventory & Recipes* and
*Kitchens* groups, showing **Issued to kitchens** and **All kitchens**), `T-362-1280-bottom.png`,
`T-362-390-drawer.png`, `T-362-390-drawer-kitchens.png`, `T-362-390-drawer-bottom.png`,
`T-362-390-closed.png`.

**One number worth Rajeev's eye, not a defect:** the temple admin's menu is 1844px of content in a
674px window, so it scrolls about 2.7 screens. His order added one group heading (seven groups where
there were six), which is 24px of heading plus 24px of gap — **48px longer than before**, measured
from the heading and gap boxes above. The menu already scrolled; the position is remembered
(`SIDEBAR_SCROLL_KEY`) and Today is at the top after sign-in.

---

## Acceptance criteria

1. **His order and grouping, sentence case, first group unheaded.** Built, and
   `nav.test.ts` "is the menu Rajeev settled on 2026-09-19" asserts all seven groups and all 33
   labels for a temple admin in one assertion. Screenshots above show it rendered.
2. **Every item keeps its page's own roles.** Checked each of the 33 destinations' `RequireRole`
   against its row; all agree, and nothing was changed. `/deliveries` reads its roles from
   `RECEIVE_DELIVERIES_HOLDERS = ["TEMPLE_ADMIN","KITCHEN_MANAGER","KITCHEN_STAFF"]`, which matches.
   No disagreement found, so nothing to report under this head.
3. **Empty groups dropped.** `nav.test.ts` "drops the Temple group for a kitchen cook rather than
   heading an empty list" asserts the cook's whole menu, and the older "never leaves a group heading
   over an empty group" still runs across every role.
4. **Planner items still disappear when `canPlanMeals` is false.** The two existing Epic 12 tests
   pass unchanged, and a new one holds *Cost per serving* on the other side of that filter — it is
   not a planner destination and must survive.
5. **Operator and volunteer menus unchanged.** `hrefsFor("SUPER_ADMIN")` and `hrefsFor("VOLUNTEER")`
   assert the same arrays as before my change, and a volunteer still gets exactly one group.
6. **Rename done everywhere a user reads it.** Menu item, page title and heading, the UAT scripts and
   the two story bullets. Grep above.
7. **`tsc`, `eslint --max-warnings=0`, `vitest`** — all zero, output above, all through the lock.
8. **Negative control** — output above; trapped restore, `cmp` identical.
9. **Measured at 1280 and 390** — table above, screenshots named.

---

## Not done / needs Rajeev

- **No hand smoke-test of the deployed app.** I measured the real component against the real
  stylesheet in a real browser, but I did not sign in and click the menu on a running app: T-359 owns
  the dev server on :3100 and its backend on :8180 in this same worktree, and standing up a second
  Next server would have fought over `.next/`. What is **not** verified by me: that the menu looks
  right with a real session's `whoami` (a different temple name length), and that each renamed row
  actually navigates. The rows' `href`s are unchanged, so navigation is the same code path it was.
- **`docs/uat/UAT-093-kitchens-in-the-meal-planner.md` needs nothing.** T-359 holds it and I did not
  open it for editing, but I read it: it names `/kitchens` by address and *Meal planner* by label,
  and neither changed. No wording to hand over.
- **`frontend/components/ds/PeriodNav.tsx:36`** still says "issued from store" in a comment listing
  the screens that use the period stepper. It is not user-facing and the file is not in my contract
  (it is shared by several screens), so I left it. One-word fix for whoever next touches that file.
- **`/kitchens` still has the `h1` "Kitchens"** while the menu row now reads "All kitchens". The row
  is worded that way so it is not its own group heading repeated; the page heading has no group above
  it and reads correctly alone. If Rajeev wants them identical, the page's `h1` is the change, and it
  is his call, not mine to make silently.
- **The page's total row already says "All kitchens"** (the tfoot on `/issued-from-store`), which is
  now also a menu destination's name. In a table footer it plainly means "all of them added up". Left
  as is; flagging it because the two words now appear twice in one screen.
- **`docs/work/*` and `docs/CHANGELOG.md`** still contain the old label. Deliberate: the ledger and
  the proofs are dated records, and the changelog is outside the contract.
